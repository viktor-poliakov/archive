import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-process-docs',
  imports: [CodeBlock, RouterLink],
  templateUrl: './docs.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemProcessDocs {
  protected readonly whyDocsRot = `
═══ ПОЧЕМУ ДОКУМЕНТАЦИЯ ГНИЁТ ═══

«Гниёт» — это не красивое словцо. Текст сам по себе не портится: в нём не
заводится плесень, буквы не выцветают. Портит его время, за которое код ушёл
вперёд, а описание кода осталось стоять на месте. Через полгода расхождение
такое, что читать документ уже опаснее, чем не читать: он уверенно врёт.

АНАЛОГИЯ ПРО ИНСТРУКЦИЮ.
Вы купили стиральную машину. В коробке лежала инструкция. Вы её достали и
убрали в ящик комода — в другой комнате, вместе с гарантийными талонами от
чайника и микроволновки. Через год машина замигала непонятным значком.
Вы не пошли к комоду. Вы сфотографировали значок и вбили в поиск.
Заметьте: инструкция даже не устарела — она просто оказалась дальше от
проблемы, чем любой другой источник. Документация проекта живёт хуже:
она и лежит в другом ящике, И устаревает.


═══ КАК ОБЫЧНО ДЕЛАЮТ: ИСТОРИЯ ОДНОЙ ВИКИ ═══

ЯНВАРЬ. Команда заводит вики — отдельный сайт со страницами.
Заводит с энтузиазмом: «наконец у нас будет порядок».
Пишут страницу «Как поднять проект локально». Она честная и рабочая.

МАРТ. Кто-то переносит базу с MySQL на PostgreSQL. Меняет строку подключения
в коде, правит docker-compose, всё запускается. Страницу в вики он не трогает —
не потому что злодей, а потому что задача была «перенести базу», а вики
открыта не была. Никакая проверка не упала: вики и код друг про друга не знают.

МАЙ. Приходит новый разработчик. Открывает вики, делает по шагам,
получает ошибку подключения, тратит полдня, идёт в чат.
Ему отвечают: «А, там устарело, у нас теперь постгрес, вот команда».
Он делает по команде из чата — всё работает. Страницу он не правит:
он тут три дня, ему неловко, и он не уверен, что понял всё правильно.

СЕНТЯБРЬ. В вики шестьдесят страниц. Примерно половина врёт.
Проблема не в том, что половина врёт. Проблема в том, что НЕИЗВЕСТНО, какая
именно половина. Читатель не может отличить свежую страницу от протухшей:
обе выглядят одинаково солидно. Поэтому команда перестаёт открывать вики
вообще и спрашивает в чате. Чат становится настоящей документацией — только
он не ищется, не хранится, не читается новичком в воскресенье вечером
и исчезает вместе с историей сообщений.

ДЕКАБРЬ. Кто-то предлагает «навести порядок в вики».
Заводят задачу «актуализировать документацию».
Задача висит четырнадцать месяцев и закрывается как неактуальная.


═══ ЧТО СЛУЧИТСЯ ═══

БЕДА 1. ДОВЕРИЕ ПАДАЕТ ДО НУЛЯ, И ЭТО ПОЧТИ НЕОБРАТИМО.
  Одна страница, которая соврала, отравляет все остальные.
  Девяносто процентов правды и десять процентов вранья на практике
  работают как ноль процентов пользы: проверять всё равно приходится всё.
  А чтобы вернуть доверие, мало починить страницы — надо ещё убедить людей
  снова туда ходить, а они уже привыкли спрашивать в чате.

БЕДА 2. НИКТО НЕ ОБЯЗАН ОБНОВЛЯТЬ.
  Это ключевая причина, всё остальное — следствия. Код обязан собираться.
  Тесты обязаны проходить (см. страницу про тесты). Линтер обязан молчать
  (см. страницу про линтеры). А документация не обязана ничего.
  Нет механизма, который скажет «ты поменял ручку API, а описание ручки
  осталось прежним». Всё, что не проверяется автоматом, со временем
  расходится с реальностью — это закон, а не невезение.

БЕДА 3. РАССТОЯНИЕ ДО КОДА = ВЕРОЯТНОСТЬ ЗАБЫТЬ.
  Если описание лежит в соседнем файле того же репозитория, вы видите его
  в списке изменений и в ревью. Если оно лежит на отдельном сайте под другим
  логином — вы про него вспомните, только если случайно откроете.
  Каждый лишний клик до документа — минус несколько процентов к шансу,
  что документ вообще обновят.

БЕДА 4. ЗНАНИЕ ПЕРЕЕЗЖАЕТ В ГОЛОВЫ ДВУХ ЧЕЛОВЕК.
  В команде появляются «те, кто знает». К ним ходят с вопросами.
  Они устают, потом уходят в отпуск, потом уходят совсем.
  Проект внезапно обнаруживает, что половина знаний была устной,
  и восстанавливает её чтением кода — это месяцы.

БЕДА 5. РЕВЬЮ НЕ ЛОВИТ ВРАНЬЁ.
  На ревью смотрят на код. Никому не приходит в голову спросить
  «а где страница вики, которую этот код только что сделал неправдой?».
  Спрашивать это вручную и каждый раз — бесполезно: человек забудет.
  Ловить должно то, что не забывает: проверка в пайплайне.


═══ КАК НАДО ═══

ПРАВИЛО 1. ДОКУМЕНТАЦИЯ ЖИВЁТ РЯДОМ С КОДОМ.
  Тот же репозиторий, те же ветки, тот же pull request, то же ревью.
  Поменял ручку API — правишь описание в том же наборе изменений.
  Ревьюер видит и то и другое на одном экране и может спросить.
  Это не «ещё одно требование», это единственное известное лекарство.

ПРАВИЛО 2. ЧТО МОЖНО — НЕ ПИШЕМ РУКАМИ, А ВЫВОДИМ ИЗ КОДА.
  Описание HTTP-API можно получить из тех же схем, которыми сервер
  проверяет входящие запросы. Витрину компонентов интерфейса — из самих
  компонентов. Справочник типов библиотеки — из типов.
  Всё, что выведено из кода, не может разойтись с кодом: оно и есть код.

ПРАВИЛО 3. ЧТО НЕЛЬЗЯ ВЫВЕСТИ — ПРОВЕРЯЕМ АВТОМАТОМ.
  Ссылки проверяем на битость. Примеры кода проверяем на то, что они
  компилируются и запускаются. Описание API сверяем с настоящими ответами
  сервера. Не проверяется — значит, рано или поздно соврёт.

ПРАВИЛО 4. ПИШЕМ «ПОЧЕМУ», А НЕ «ЧТО».
  «Что» устаревает каждую неделю и его видно в коде. «Почему» не устаревает
  почти никогда и в коде его не видно НИКОГДА. Писать стоит именно «почему».

ПРАВИЛО 5. МАЛЕНЬКИЙ ЖИВОЙ ДОКУМЕНТ ЛУЧШЕ БОЛЬШОГО МЁРТВОГО.
  Страница на тридцать строк, которую читают и правят, полезнее
  портала на триста страниц, которому никто не верит.


═══ ЧЕСТНАЯ ЦЕНА ═══

Держать документацию рядом с кодом — значит, что каждый pull request станет
чуть толще, а ревью чуть длиннее. Это настоящая плата, и её не надо
маскировать. Взамен вы получаете единственный вариант, при котором
документация вообще выживает: она меняется вместе с тем, что описывает.

И отдельно, самое неприятное. Часть документации всё равно придётся писать
руками, и никакой инструмент за вас этого не сделает: «почему мы выбрали
именно это» из кода не выводится. Ровно поэтому дальше на странице будет
разговор про ADR — способ записывать решения так, чтобы через год
не переспрашивать и не переделывать по кругу.
`;

  protected readonly fourReaders = `
═══ У ДОКУМЕНТАЦИИ ЧЕТЫРЕ РАЗНЫХ ЧИТАТЕЛЯ ═══

Главная ошибка новичка — писать «документацию вообще». Такой документ
не подходит никому: он одновременно слишком подробный и слишком поверхностный.
Читателей четверо, у каждого свой вопрос, и для каждого есть свой инструмент.
Разберём по одному — и станет видно, что писать, а что можно не писать.


═══ ЧИТАТЕЛЬ 1. ТОТ, КТО ВЫЗЫВАЕТ ВАШ API ═══

КТО ЭТО. Разработчик мобильного приложения. Коллега из соседней команды.
Партнёрская компания. Иногда — вы сами, только с другой стороны провода.
Он ваш код не видел и видеть не хочет. Он видит только адрес и ответы.

ЧТО ЕМУ НУЖНО, ДОСЛОВНО:
  — какие есть адреса (пути) и что каждый делает;
  — какие параметры обязательны, какие нет, какого они типа;
  — как выглядит успешный ответ ПОЛЕ ЗА ПОЛЕМ, с примером;
  — какие бывают ошибки и как их различать;
  — как авторизоваться;
  — что изменилось с прошлой недели и не сломает ли это его код.

ЧЕГО ЕМУ НЕ НУЖНО СОВСЕМ:
  устройство ваших сервисов, названия классов, схема базы данных,
  ваши внутренние соглашения об именовании файлов.

ИНСТРУМЕНТ: машиночитаемое описание API — OpenAPI для обычных HTTP-ручек,
AsyncAPI для событийных интерфейсов. Из одного файла получаются и страница
документации с песочницей, и типы для его фронтенда, и проверка обратной
совместимости при каждом изменении. Подробный разбор — ниже на этой странице.

АНАЛОГИЯ: меню в кафе. Гостю нужны название блюда, состав, цена и время
приготовления. Ему не нужна планировка кухни и график смен поваров.


═══ ЧИТАТЕЛЬ 2. ТОТ, КТО СОБИРАЕТ ИНТЕРФЕЙС ИЗ ВАШИХ КОМПОНЕНТОВ ═══

КТО ЭТО. Верстальщик соседней страницы. Новый человек в команде фронтенда.
Дизайнер, который хочет проверить, как его макет выглядит вживую.

ЧТО ЕМУ НУЖНО:
  — какие вообще есть готовые компоненты (кнопка, поле, модальное окно,
    таблица) — чтобы не написать двенадцатую кнопку;
  — как выглядит каждый ВО ВСЕХ СОСТОЯНИЯХ: обычное, наведение, нажатое,
    отключённое, загрузка, ошибка, пустой список, очень длинный текст;
  — какие у компонента входные параметры и что каждый делает;
  — можно ли потрогать компонент руками, не запуская всё приложение
    и не проходя заново регистрацию и три экрана до нужного места.

ЧЕГО ЕМУ НЕ НУЖНО: как компонент устроен внутри.

ИНСТРУМЕНТ: Storybook — витрина компонентов. Каждый компонент показан
отдельно и во всех состояниях сразу, кликается, есть панель параметров.
Подробно — ниже на этой странице.

АНАЛОГИЯ: витрина с образцами тканей в ателье. Не надо шить платье,
чтобы понять, как ткань выглядит, тянется и мнётся.


═══ ЧИТАТЕЛЬ 3. ТОТ, КТО ПРИШЁЛ В ПРОЕКТ И НЕ ПОНИМАЕТ, ПОЧЕМУ ТАК ═══

КТО ЭТО. Новый разработчик на второй неделе. Он уже разобрался, ЧТО делает
код — это видно. Он не понимает, ПОЧЕМУ выбрали такую странную конструкцию.

ЧТО ЕМУ НУЖНО:
  — как запустить проект локально за пятнадцать минут (README);
  — общая карта: какие есть части и кто с кем разговаривает (диаграммы);
  — почему приняты именно эти решения и какие альтернативы отвергли (ADR).

ЧТО С НИМ ПРОИСХОДИТ БЕЗ ЭТОГО.
  Он видит странность, считает её ошибкой и предлагает «просто переписать».
  А эту странность полгода назад добавили, потому что без неё падал импорт
  на файлах больше ста мегабайт. Об этом никто не написал — поэтому он
  наступит на те же грабли, потратит две недели и вернёт странность обратно.
  Это не гипотетическая история, это самая частая история в индустрии.

ИНСТРУМЕНТЫ: README, ADR (короткие записи решений), диаграммы текстом.
Всё это — ниже на странице.

АНАЛОГИЯ: протокол собрания жильцов. Через год никто не помнит, почему
шлагбаум поставили именно с этой стороны, — а в протоколе написано:
с другой стороны не разрешила пожарная служба.


═══ ЧИТАТЕЛЬ 4. ВЫ САМИ ЧЕРЕЗ ПОЛГОДА ═══

Это самый недооценённый читатель, потому что кажется, что его не существует:
«ну я-то помню». Не помните. Проверено всеми.

ЧТО ЕМУ НУЖНО:
  — почему тут стоит вот эта странная проверка, которую страшно удалять;
  — какие ещё варианты пробовали и чем они кончились;
  — где включается флаг, который вы настраивали один раз и забыли;
  — что именно означает вот этот параметр «3» в конфиге.

ИНСТРУМЕНТЫ: комментарий «почему» прямо в коде, ADR, аккуратные
сообщения коммитов (см. страницу про Git).

ПРОСТОЙ ТЕСТ ПЕРЕД ЗАКРЫТИЕМ ЗАДАЧИ.
  Спросите себя: если я открою этот файл через шесть месяцев и не вспомню
  вообще ничего — пойму ли я, почему тут так? Если нет — вы только что
  нашли место ровно для одной строчки комментария. Одной, не десяти.


═══ ЧТО ЗАПОМНИТЬ ═══

  читатель API          →  OpenAPI + сайт документации с песочницей
  читатель интерфейса   →  Storybook
  новый в команде       →  README + ADR + диаграммы
  вы сами через полгода →  комментарии «почему» + ADR + история коммитов

Все четыре набора не заменяют друг друга. Идеальный OpenAPI не поможет
новичку понять, почему база устроена именно так. Идеальные ADR не помогут
мобильному разработчику собрать запрос. Пишите под конкретного читателя —
и половина вопросов «а надо ли это описывать» отпадёт сама.
`;

  protected readonly whatNotToDocument = `
// ═══ ЧЕГО ПИСАТЬ НЕ НАДО ═══
//
// Начнём с неожиданного: половина работы над документацией — это НЕ ПИСАТЬ
// лишнего. Лишний текст не нейтрален. Он занимает место, отвлекает,
// устаревает и создаёт ложное ощущение, что проект задокументирован.
//
// ГЛАВНОЕ ПРАВИЛО ВСЕЙ СТРАНИЦЫ:
//   код уже говорит ЧТО он делает — это видно прямо в коде;
//   документация нужна для ПОЧЕМУ — этого в коде не видно НИКОГДА.

// ═══ КАК ОБЫЧНО ДЕЛАЮТ: КОММЕНТАРИЙ-ПОПУГАЙ ═══
//
// Человеку сказали «комментируй код». Он понял буквально, и получилось так.

// увеличиваем счётчик на единицу
counter = counter + 1;

// проверяем, что пользователь не пустой
if (user) {
  // получаем корзину пользователя
  const cart = user.cart;
  // если корзина пустая — выходим
  if (cart.items.length === 0) {
    return;
  }
}

// функция считает сумму
function calculateTotal(items) {
  // объявляем переменную sum
  let sum = 0;
  // проходим по всем товарам
  for (const item of items) {
    // прибавляем цену
    sum = sum + item.price;
  }
  // возвращаем сумму
  return sum;
}

// ═══ ЧТО С ЭТИМ СЛУЧИТСЯ ═══
//
// БЕДА 1. КОММЕНТАРИЙ НЕ ДОБАВИЛ НИ ЕДИНОГО БИТА ИНФОРМАЦИИ.
//   Строка counter = counter + 1 читается ровно как «увеличиваем счётчик
//   на единицу». Это пересказ, а не объяснение. Читатель потратил время
//   дважды и не узнал ничего нового.
//
// БЕДА 2. КОММЕНТАРИЙ НАЧНЁТ ВРАТЬ ПЕРВЫМ.
//   Через месяц кто-то поменяет шаг с 1 на 2 — комментарий останется старым.
//   Теперь у вас в коде написана неправда, причём НАПЕЧАТАННАЯ словами,
//   а печатному тексту читатель верит больше, чем коду.
//   Врущий комментарий хуже, чем отсутствие комментария.
//
// БЕДА 3. ШУМ ПРЯЧЕТ СИГНАЛ.
//   Когда комментарии стоят у каждой второй строки, глаз перестаёт их видеть.
//   И один по-настоящему важный комментарий («не трогать: тут обход бага
//   платёжного шлюза») теряется среди «объявляем переменную sum».
//
// БЕДА 4. КОММЕНТАРИЙ ПРИКРЫВАЕТ ПЛОХОЕ ИМЯ.
//   Если функции нужен комментарий «функция считает сумму» — значит,
//   она названа неудачно. Хорошее имя — это тоже документация,
//   только та, которая не может устареть: её видно на каждом вызове,
//   и переименование правит все места сразу.

// ═══ КАК НАДО: КОММЕНТАРИЙ ОБЪЯСНЯЕТ ПРИЧИНУ ═══
//
// Ниже три пары «плохо / хорошо» на одном и том же месте кода.

// ПЛОХО — пересказ строки:
// умножаем на 100
const amount = order.total * 100;

// ХОРОШО — причина, которой в коде не видно:
// Платёжный шлюз принимает сумму в копейках целым числом:
// 149.90 руб. -> 14990. Если отправить дробное, шлюз молча округлит вниз
// и клиент недоплатит копейку — ловили в проде 12 марта, задача PAY-418.
const amountInKopecks = Math.round(order.total * 100);

// ПЛОХО — пересказ:
// ждём 300 миллисекунд
await sleep(300);

// ХОРОШО — причина и откуда взялось число:
// Сервис доставки отвечает кодом 429 («слишком много запросов»), если слать
// чаще трёх раз в секунду. 300 мс — их же рекомендация из письма поддержки
// от 04.02. Меньше ставить нельзя, больше — просто медленнее.
await sleep(300);

// ПЛОХО — пересказ:
// сортируем задом наперёд
list.sort((a, b) => b.createdAt - a.createdAt);

// ХОРОШО — причина, почему выбран НЕ очевидный вариант:
// Сортируем по дате СОЗДАНИЯ, а не по дате изменения: продукт просил,
// чтобы карточка не прыгала наверх от правки одной опечатки.
list.sort((a, b) => b.createdAt - a.createdAt);

// ═══ ОТДЕЛЬНЫЙ СЛУЧАЙ: README НА СОРОК ЭКРАНОВ ═══
//
// Вторая по частоте болезнь. Человек старался и вывалил в README всё:
// историю проекта, философию команды, полный список переменных окружения,
// описание каждого модуля, планы на будущее и раздел «благодарности».
//
// ЧТО СЛУЧИТСЯ:
//   1. Никто не дочитает. Читатель ищет одну вещь — как запустить.
//      Он пролистает три экрана, не найдёт и пойдёт спрашивать в чат.
//   2. Устареет всё сразу. Сорок экранов невозможно поддерживать;
//      через квартал половина неправда, и снова падает доверие.
//   3. Важное утонет. Команда запуска, ради которой всё затевалось,
//      окажется на двадцать восьмом экране между «нашими ценностями»
//      и «списком будущих улучшений».
//
// КАК НАДО: README — это входная дверь, а не квартира.
//   Первый экран отвечает на два вопроса: что это и как запустить.
//   Всё остальное — ссылками на отдельные документы.
//   Полный пример хорошего README — следующий блок на этой странице.

// ═══ ЧЕСТНАЯ ЦЕНА ═══
//
// Правило «пиши почему, а не что» требует думать, а пересказывать строку —
// не требует. Поэтому хороших комментариев всегда меньше, и это нормально:
// в здоровом файле их может быть три штуки на двести строк.
// Мало комментариев — не признак лени. Признак лени — сорок комментариев,
// из которых сорок пересказывают соседнюю строку.
`;

  protected readonly readmeTemplate = `
# ═══ README — ЭТО ВХОДНАЯ ДВЕРЬ ═══
#
# README (читается «ридми», от read me — «прочти меня») — файл README.md
# в корне репозитория. Это первое, что видит человек, открывший проект:
# и GitHub, и GitLab показывают его прямо под списком файлов.
#
# Единственная задача README — довести незнакомого человека от «я вижу
# незнакомую папку» до «у меня всё запустилось» без единого вопроса в чат.
# Всё остальное — не задача README, а задача документов, на которые он ссылается.

# ═══ КАК ОБЫЧНО ДЕЛАЮТ ═══
#
# Вариант А. README, созданный автоматически и не тронутый ни разу:
#
#   # my-service
#   This project was generated with create-app version 5.
#
#   Полезной информации: ноль.
#
# Вариант Б. README на сорок экранов, где команда запуска — на двадцать восьмом,
# между разделом «наши ценности» и разделом «дорожная карта на 2027 год».
#
# Вариант В. README, который был правдой полтора года назад:
#   написано npm install, а проект давно на pnpm; написано «нужен Node 18»,
# а сборка падает на всём, кроме 22.

# ═══ ЧТО СЛУЧИТСЯ ═══
#
# БЕДА 1. НОВИЧОК ТЕРЯЕТ ПЕРВЫЙ ДЕНЬ.
#   Он не может запуститься, стесняется спросить, гуглит, пробует наугад,
#   ломает себе окружение. К вечеру у него нет ни результата, ни настроения.
#
# БЕДА 2. КОМАНДА ТЕРЯЕТ БОЛЬШЕ, ЧЕМ НОВИЧОК.
#   Каждый вопрос «а как это запустить» отрывает кого-то от работы.
#   Двадцать минут ответа умножьте на количество новых людей за год.
#
# БЕДА 3. ПРОЕКТ ВЫГЛЯДИТ ЗАБРОШЕННЫМ.
#   Пустой README читается как «тут никто ни за что не отвечает».
#   Для внешнего проекта это прямая потеря пользователей.

# ═══ КАК НАДО: ПОРЯДОК РАЗДЕЛОВ ═══
#
# Порядок не случайный — он повторяет порядок вопросов в голове читателя.
#
#   1. ЧТО ЭТО. Одно-два предложения. Человеческим языком, без аббревиатур.
#   2. ЗАЧЕМ ЭТО. Одна фраза: какую задачу решает, кто пользуется.
#   3. ЧТО НУЖНО ИМЕТЬ. Версии Node, пакетного менеджера, Docker — точные.
#   4. КАК ЗАПУСТИТЬ ЗА ПЯТЬ МИНУТ. Команды подряд, копируются целиком.
#   5. КАК ЗАПУСТИТЬ ТЕСТЫ И ПРОВЕРКИ. Одна команда на каждое.
#   6. КАК УСТРОЕНО В ОДИН АБЗАЦ. Три-четыре предложения, не больше.
#   7. КУДА ИДТИ ДАЛЬШЕ. Ссылки на документы. Тут заканчивается README.
#
# Правило проверки: команда запуска должна попадать на ПЕРВЫЙ экран.
# Если не попадает — что-то выше лишнее.

# ═══ ПОЛНЫЙ ПРИМЕР ХОРОШЕГО README НЕБОЛЬШОГО СЕРВИСА ═══
#
# Ниже — файл целиком. Команда cat с «heredoc» просто записывает всё, что
# идёт до строки MD, в файл README.md. Смотрите на содержимое, не на cat.

cat > README.md <<'MD'
# notify-service

Сервис отправки уведомлений: принимает событие по HTTP и доставляет его
пользователю письмом или пуш-сообщением. Используется биллингом и складом,
чтобы не писать отправку писем у себя.

Спрашивать про сервис: команда Platform, канал #platform-notify.

## Что нужно установить

- Node.js 22 или новее (\`node --version\`)
- pnpm 10 (\`corepack enable\` включит нужную версию сам)
- Docker — для локальных Postgres и Redis

## Запуск за пять минут

\`\`\`bash
git clone git@github.com:acme/notify-service.git
cd notify-service
pnpm install
cp .env.example .env          # значения по умолчанию рабочие, править не надо
docker compose up -d          # поднимет postgres:16 и redis:7
pnpm db:migrate               # создаст таблицы
pnpm dev                      # сервис на http://localhost:3000
\`\`\`

Проверить, что живой:

\`\`\`bash
curl http://localhost:3000/health
# {"status":"ok","db":"ok","redis":"ok"}
\`\`\`

Документация API с песочницей: http://localhost:3000/docs

## Тесты и проверки

\`\`\`bash
pnpm test          # модульные тесты
pnpm test:e2e      # сквозные, поднимают контейнеры сами, идут ~2 минуты
pnpm lint          # линтер и форматтер
pnpm typecheck     # проверка типов
\`\`\`

Перед созданием pull request достаточно \`pnpm verify\` — он запускает всё выше.

## Как устроено

Один процесс на Node.js с фреймворком Fastify. HTTP принимает событие,
кладёт его в очередь в Redis, отдельный воркер забирает и отправляет через
внешнего провайдера. Postgres хранит журнал доставок и настройки подписок.
Описание HTTP-API лежит в \`openapi.yaml\` и собирается из тех же схем,
которыми сервис проверяет входящие запросы.

## Куда идти дальше

- [Архитектура и схемы](docs/architecture.md)
- [Решения и почему они такие (ADR)](docs/adr/)
- [Как выкатить на стенд и в прод](docs/deploy.md)
- [Что делать, если сервис лёг (runbook)](docs/runbook.md)
- [Как вносить изменения](CONTRIBUTING.md)
MD

# ═══ ЧТО ЗДЕСЬ ГЛАВНОЕ ═══
#
# 1. Первая строка объясняет назначение человеку, который слышит про сервис
#    впервые. Не «микросервис нотификаций», а «принимает событие и доставляет».
#
# 2. Указано, КОГО спрашивать. Это спасает больше времени, чем кажется:
#    когда владелец не назван, вопрос летит в общий чат и тонет.
#
# 3. Блок запуска копируется целиком, без «замените X на своё».
#    Файл .env.example лежит в репозитории и содержит рабочие значения
#    для локального запуска — секретов там нет и быть не должно.
#
# 4. Сразу после запуска дана проверка живости с ОЖИДАЕМЫМ ВЫВОДОМ.
#    Новичок должен понимать, что он увидит, если всё хорошо.
#
# 5. Раздел «Как устроено» — ровно один абзац. Как только он разрастается,
#    его выносят в docs/architecture.md и оставляют ссылку.
#
# 6. Последний раздел — только ссылки. README не пытается рассказать всё.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
#
# README всё равно устаревает: он написан руками и из кода не выводится.
# Поэтому его подпирают двумя дешёвыми привычками.
#
#   ПРИВЫЧКА 1. Каждый новый человек в команде проходит README дословно
#   и правит первое же место, где он соврал. Это его первый pull request.
#   Лучше момента не будет: только у новичка ещё нет знания «в голове».
#
#   ПРИВЫЧКА 2. Проверка битых ссылок в пайплайне — ссылки гниют быстрее
#   текста. Как это включить, разобрано ниже, в блоке про автоматизацию.
`;

  protected readonly openapiMinimal = `
# ═══ OPENAPI ПРОСТЫМИ СЛОВАМИ ═══
#
# OpenAPI — это формат, в котором HTTP-API описывают так, чтобы описание
# читал не только человек, но и программа. Файл в YAML или JSON, лежит
# в репозитории рядом с кодом. Раньше формат назывался Swagger; сейчас
# спецификацию ведёт OpenAPI Initiative — проект под управлением Linux
# Foundation, а имя Swagger осталось за инструментами компании SmartBear.
#
# АНАЛОГИЯ. Есть два кафе.
#   В первом на стене написано «спросите у официанта». Чтобы узнать состав
#   блюда, надо поймать человека, дождаться и поверить на слово.
#   Во втором — меню, где у каждого блюда написан точный состав, вес и цена.
#   OpenAPI — это второе кафе. Причём меню такое, что по нему робот может
#   сам собрать заказ и заранее сказать, что вы забыли указать столик.
#
# ЗАЧЕМ ФОРМАТУ БЫТЬ МАШИНОЧИТАЕМЫМ. Потому что из него тогда получаются
# бесплатно: сайт документации, типы для фронтенда, заглушка сервера,
# проверка запросов и проверка обратной совместимости. Разберём ниже.
#
# ВЕРСИИ, ЧТОБЫ НЕ ПУТАТЬСЯ (на август 2026):
#   3.0.x — старая линия, поддержана всеми инструментами без исключения;
#   3.1.x — практический дефолт сегодня; с 3.1.0 (февраль 2021) описание
#           схем полностью совместимо с JSON Schema draft 2020-12;
#   3.2.0 — вышла 19 сентября 2025, обратно совместима с 3.1, добавила
#           иерархию тегов, метод QUERY, описание стриминга (SSE, JSON Lines)
#           и Device Authorization Flow для OAuth 2.0;
#   4.0 («Moonwalk») — в разработке, есть рабочая группа и черновики,
#           даты релиза нет, продакшн-инструментов нет; сам репозиторий
#           рабочей группы советует пока брать 3.x.
#
# ВЫВОД ДЛЯ НОВИЧКА: пишите 3.1.0. Если нужен стриминг или иерархия тегов
# и ваши инструменты это уже понимают — 3.2.0.

# ═══ МИНИМАЛЬНЫЙ ФАЙЛ, РАЗОБРАННЫЙ ПОСТРОЧНО ═══

openapi: 3.1.0
# ^ Версия САМОЙ СПЕЦИФИКАЦИИ, а не вашего сервиса. Обязательное поле,
#   всегда первое. По нему инструменты понимают, как читать всё остальное.

info:
  # Блок info — «шапка». Попадает на первый экран сайта документации.
  title: Notify API
  # ^ Человеческое имя API. Не имя репозитория и не имя контейнера.
  version: 1.4.0
  # ^ А вот это версия ВАШЕГО API. Меняется, когда меняется сам API.
  description: |
    Отправка уведомлений пользователям.
    Символ «|» означает многострочный текст: всё, что с отступом, — описание.
    Поддерживается markdown, поэтому тут можно давать списки и ссылки.

servers:
  # Где это API живёт. Список, потому что адресов обычно несколько.
  - url: https://api.acme.com/v1
    description: продакшн
  - url: https://api.staging.acme.com/v1
    description: тестовый стенд
  # Эти адреса подставляются в песочницу на сайте документации:
  # читатель выбирает стенд из списка и жмёт «выполнить».

paths:
  # ═══ ПУТЬ ═══
  # Путь (path) — часть адреса после адреса сервера. Полный адрес получается
  # склейкой: https://api.acme.com/v1 + /notifications.
  /notifications:

    # ═══ ОПЕРАЦИЯ ═══
    # Операция (operation) — это «путь + HTTP-метод». Один путь может иметь
    # несколько операций: get — прочитать, post — создать, delete — удалить.
    # Метод — глагол, путь — существительное. Подробнее про методы —
    # на странице про HTTP в разделе «API».
    post:
      summary: Отправить уведомление
      # ^ Одна строка. Именно её видно в списке ручек на сайте документации.
      operationId: sendNotification
      # ^ Уникальное имя операции. Из него генераторы делают имя функции
      #   в клиенте: api.sendNotification(...). Пишется в camelCase,
      #   меняется крайне неохотно — переименование ломает чужой код.
      tags: [notifications]
      # ^ Тег — папка в оглавлении. Ручки с одним тегом идут одной группой.

      requestBody:
        # Тело запроса — то, что клиент отправляет.
        required: true
        content:
          application/json:
            # Ключ content — это тип содержимого. Их может быть несколько:
            # application/json, multipart/form-data для файлов и так далее.
            schema:
              # ═══ СХЕМА ═══
              # Схема — описание формы данных: какие поля, каких типов,
              # какие обязательны. В 3.1 это ровно JSON Schema 2020-12.
              $ref: '#/components/schemas/NotificationInput'
              # ^ $ref — ссылка на кусок, описанный ниже, чтобы не повторять
              #   одно и то же в десяти местах. Читается как «смотри там».

      responses:
        # Ответы. Ключ — код состояния HTTP. Кавычки обязательны:
        # без них YAML прочитает 201 как число, а нужна строка.
        '201':
          description: Уведомление принято в очередь
          # ^ description у ответа ОБЯЗАТЕЛЕН. Забыли — файл невалиден.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Notification'
        '400':
          description: В теле запроса ошибка — например, нет обязательного поля
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Нет или неверный токен

    get:
      summary: Список отправленных уведомлений
      operationId: listNotifications
      tags: [notifications]
      parameters:
        # ═══ ПАРАМЕТР ═══
        # Параметр — то, что клиент передаёт НЕ в теле запроса.
        # in: query  — в строке запроса: /notifications?limit=50
        # in: path   — прямо в адресе: /notifications/{id}
        # in: header — в заголовке
        - name: limit
          in: query
          description: Сколько записей вернуть за раз
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
          # ^ Ограничения — часть схемы. Инструмент проверки отклонит
          #   limit=500 сам, вам не придётся писать эту проверку руками.
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [queued, sent, failed]
            # ^ enum — список допустимых значений и одновременно
            #   выпадающий список в песочнице на сайте документации.
      responses:
        '200':
          description: Массив уведомлений, новые сверху
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Notification'

components:
  # Общая кладовка кусков, на которые ссылались через $ref.
  # Всё, что встречается больше одного раза, живёт здесь.
  schemas:

    NotificationInput:
      type: object
      required: [userId, channel, text]
      # ^ Обязательные поля перечисляются отдельным списком. Всё, чего
      #   нет в required, — необязательное. Это частая ошибка новичков:
      #   они думают, что перечисление в properties уже делает поле нужным.
      properties:
        userId:
          type: string
          format: uuid
          description: Кому отправляем
        channel:
          type: string
          enum: [email, push]
        text:
          type: string
          maxLength: 500
        scheduledAt:
          type: string
          format: date-time
          description: Когда отправить. Не указано — отправляем сразу.
      # Небольшой пример помогает и человеку, и песочнице:
      examples:
        - userId: 8f14e45f-ceea-467a-9c1c-1a1f3d7fa2b0
          channel: email
          text: Ваш заказ №4412 отправлен

    Notification:
      type: object
      required: [id, userId, channel, text, status, createdAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        channel:
          type: string
          enum: [email, push]
        text:
          type: string
        status:
          type: string
          enum: [queued, sent, failed]
        createdAt:
          type: string
          format: date-time

    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          description: Машиночитаемый код, например VALIDATION_FAILED
        message:
          type: string
          description: Человеческое пояснение, показывать пользователю нельзя

  securitySchemes:
    # Как авторизоваться. Описывается один раз и применяется ко всему API.
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
# ^ Пустой список в значении означает «никаких дополнительных областей
#   доступа не требуется». Строчка на верхнем уровне включает авторизацию
#   для всех операций сразу; отдельной операции её можно переопределить.

# ═══ ЧТО ЗАПОМНИТЬ ═══
#
# ПУТЬ      — адрес: /notifications
# ОПЕРАЦИЯ  — путь + метод: POST /notifications
# ПАРАМЕТР  — то, что не в теле: ?limit=50, /notifications/{id}
# СХЕМА     — форма данных: какие поля, типы, что обязательно
# $ref      — ссылка на кусок из components, чтобы не повторяться
# TAG       — группа в оглавлении
#
# Файл целиком читается сверху вниз и весь состоит из этих пяти понятий.
# Всё остальное в спецификации — их разновидности и уточнения.
`;

  protected readonly specFirstVsCodeFirst = `
# ═══ ДВА ПУТИ: СПЕЦИФИКАЦИЯ СНАЧАЛА ИЛИ КОД СНАЧАЛА ═══
#
# Файл OpenAPI можно получить двумя способами, и они противоположны.
#
#   SPEC-FIRST (спецификация сначала, её же называют design-first):
#     вы пишете openapi.yaml руками ДО того, как написан код.
#     Файл — источник истины, код обязан ему соответствовать.
#
#   CODE-FIRST (код сначала): вы пишете обработчики со схемами,
#     а файл openapi.yaml сервер отдаёт сам, выводя из этих схем.
#     Код — источник истины, файл — артефакт сборки.
#
# АНАЛОГИЯ. Spec-first — это чертёж дома до стройки.
# Code-first — это обмерный план готового дома. И то и другое бывает нужно;
# ужасно только третье — «дом построили, а план нарисовали по памяти».

# ═══ SPEC-FIRST: КАК ЭТО ВЫГЛЯДИТ ═══
#
# Шаг 1. Обсуждаем API на бумаге, пишем openapi.yaml, отправляем в pull request.
# Шаг 2. Ревью спецификации: фронтенд и мобильные смотрят и спорят ДО кода.
# Шаг 3. Из файла поднимают заглушку сервера — фронтенд начинает работать
#        сразу, не дожидаясь бэкенда.
# Шаг 4. Бэкенд пишет реализацию, тест сверяет её со спецификацией.

# Фрагмент такого файла — он написан человеком и обсуждаем:
openapi: 3.1.0
info:
  title: Orders API
  version: 0.1.0        # 0.x, потому что API ещё обсуждается
paths:
  /orders/{orderId}/cancel:
    post:
      summary: Отменить заказ
      operationId: cancelOrder
      description: |
        Спорный момент, вынесенный на обсуждение: что вернуть, если заказ
        уже уехал. Вариант А — 409 Conflict. Вариант Б — 200 с полем
        cancelled: false. Команда мобильных просит вариант А.
      responses:
        '200': { description: Заказ отменён }
        '409': { description: Заказ уже отправлен, отменить нельзя }

# ЧЕМ ХОРОШ SPEC-FIRST:
#   + Спорят до кода, а не после. Переставить строчку в YAML стоит минуту,
#     переделать готовую ручку — неделю.
#   + Фронтенд и бэкенд стартуют одновременно, а не по очереди.
#   + Спецификация получается продуманной, а не «что вышло, то и описали».
#   + Видно API целиком: заметно, что три ручки называются по-разному
#     об одном и том же.
#
# ЧЕМ ПЛОХ SPEC-FIRST:
#   - Нужна дисциплина: никто не мешает написать код, который расходится
#     со спецификацией. Без автоматической сверки в пайплайне файл
#     превращается в такую же вики, только в YAML.
#   - Надо уметь писать YAML на 700 строк и не путаться в $ref.
#   - Дольше стартовать: пока обсуждаем — не написана ни строчка кода.

# ═══ CODE-FIRST: КАК ЭТО ВЫГЛЯДИТ ═══
#
# Вы описываете форму данных один раз — той же схемой, которой сервер
# проверяет входящий запрос. Спецификация выводится из неё автоматически.
# Инструменты (на август 2026):
#   NestJS   — пакет @nestjs/swagger, описание из декораторов, «из коробки»
#   Fastify  — @fastify/swagger и @fastify/swagger-ui, из JSON Schema;
#              со схемами на Zod — fastify-zod-openapi
#   Hono     — @hono/zod-openapi
#   Express  — zod-to-openapi или swagger-jsdoc
#   Python   — во фреймворке FastAPI генерация встроена
#
# Псевдокод одного обработчика, чтобы стало понятно (реальный синтаксис
# зависит от фреймворка):
#
#   const CreateOrder = z.object({
#     items: z.array(z.string()).min(1),
#     comment: z.string().max(200).optional(),
#   });
#
#   app.post('/orders', { schema: { body: CreateOrder } }, handler);
#
# Одна и та же схема CreateOrder делает ТРИ вещи сразу:
#   1) отклоняет кривой запрос на входе;
#   2) даёт типы в коде обработчика;
#   3) попадает в openapi.yaml.
# Разойтись они не могут — это буквально один объект.
#
# ЧЕМ ХОРОШ CODE-FIRST:
#   + Дрейф структурно невозможен: описание выведено из того, что работает.
#   + Ничего не надо поддерживать отдельно — меньше работы.
#   + Быстрый старт: пишете код и сразу получаете документацию.
#
# ЧЕМ ПЛОХ CODE-FIRST:
#   - Обсуждать нечего, пока код не написан. API проектируется по факту,
#     и переделывать его уже дорого.
#   - Спецификация выходит технически точной, но бедной на смысл:
#     генератор не знает, ЗАЧЕМ нужна ручка. Описания всё равно
#     приходится дописывать руками в аннотациях.
#   - Внешний потребитель узнаёт об изменении, только когда оно уже в коде.

# ═══ ЧТО ВЫБРАТЬ НОВИЧКУ ═══
#
# CODE-FIRST. Причина простая и честная: spec-first требует дисциплины,
# которой у начинающей команды ещё нет, а нарушенный spec-first хуже
# честного code-first — вы получаете красивый файл, которому нельзя верить.
#
# Code-first же даёт правду при нулевой дисциплине: спецификация выводится
# из работающего кода и врать не может в принципе.
#
# КОГДА ПЕРЕХОДИТЬ НА SPEC-FIRST:
#   — API публичный, у него внешние потребители;
#   — над одним API работают несколько команд;
#   — фронтенд регулярно ждёт бэкенд и простаивает.
# Тогда цена обсуждения до кода окупается.

# ═══ ТРЕТИЙ ПУТЬ: ОПИСАНИЕ НА ОТДЕЛЬНОМ ЯЗЫКЕ ═══
#
# Есть промежуточный вариант — TypeSpec от Microsoft. Это отдельный язык
# описания API, из которого генерируются OpenAPI 3, JSON Schema, Protobuf
# и клиенты. Версия 1.0 достигнута; компилятор и эмиттеры openapi3
# и json-schema считаются готовыми к продакшну, генераторы клиентов
# и серверов для .NET, JS, Java и Python — пока в предварительном виде.
#
# Идея: OpenAPI перестаёт быть исходником и становится артефактом сборки,
# а исходник — компактный типизированный файл, который приятно читать.
# Насколько это распространено за пределами Microsoft и Azure —
# по открытым данным судить трудно, широкой картины нет.
#
# ЧЕСТНАЯ ЦЕНА ТРЕТЬЕГО ПУТИ: ещё один язык, который надо выучить всей
# команде, и ещё один шаг сборки. Для первого проекта — избыточно.
# Знать о нём стоит; начинать с него — нет.

# ═══ ГЛАВНОЕ ═══
#
# Плохи не spec-first и не code-first. Плох любой вариант, в котором
# описание API не проверяется автоматически против настоящего сервера.
# Как настроить такую проверку — в блоке про автоматизацию ниже.
`;

  protected readonly openapiGenerates = `
# ═══ ЧТО СПЕЦИФИКАЦИЯ ДАЁТ БЕСПЛАТНО ═══
#
# Главный аргумент за OpenAPI не «будет красивая документация».
# Главный аргумент: из одного файла ЧЕТЫРЕ вещи получаются сами.
#
#   1) сайт документации с песочницей — для того, кто вызывает API;
#   2) типы и готовый клиент — для фронтенда;
#   3) заглушка сервера — чтобы фронтенд не ждал бэкенд;
#   4) проверка, что сервер не разошёлся с обещанием.
#
# Ниже — команды и их вывод. Вывод показан, чтобы вы узнали его,
# когда увидите у себя. Все версии — на август 2026.

# ═══ 1. САЙТ ДОКУМЕНТАЦИИ С ПЕСОЧНИЦЕЙ ═══
#
# «Песочница» — форма прямо на странице: заполнил параметры, нажал кнопку,
# увидел настоящий ответ сервера. Читателю не нужен ни curl, ни Postman.
#
# Просмотрщиков три основных, все с открытым кодом:
#
#   Swagger UI — самый распространённый и самый старый (пакет
#     swagger-ui-dist, лицензия Apache-2.0, сопровождает SmartBear).
#     Живой. По вторичным оценкам, поддержка OAS 3.1 у него местами
#     неполная — если увидите странности на 3.1, дело может быть в этом.
#
#   Redoc — трёхколоночная подача, только чтение, без песочницы
#     (лицензия MIT, живой). Отдельно есть Redocly CLI с открытым кодом:
#     он умеет lint, bundle и preview. Платные у Redocly — хостинг
#     и визуальный редактор, сам CLI бесплатен.
#
#   Scalar — самый активный сегодня (@scalar/api-reference, MIT,
#     коммиты ежедневно). Справочник плюс встроенный API-клиент
#     и генерация сниппетов на разных языках. По вторичным оценкам,
#     в 2026 его часто берут дефолтом для новых проектов.
#
#   Отдельно: Stoplight Elements. SmartBear владеет Stoplight с 2023 года
#     и переносит их инструменты в SwaggerHub; Elements обновляется
#     в основном по зависимостям. Не заброшен, но стагнирует —
#     для нового проекта лучше взять что-то из тройки выше.

# Посмотреть спецификацию локально, ничего не устанавливая насовсем:
npx @redocly/cli preview-docs openapi.yaml

#   Preview server running: http://127.0.0.1:8080
#   Using Redoc community edition.
#   Bundling openapi.yaml...
#   ✓ bundled successfully in 214 ms

# Встроить Scalar прямо в сервис, чтобы документация ехала вместе с ним:
pnpm add @scalar/api-reference

#   В коде сервера: отдать на пути /docs страницу, указав ей адрес
#   вашего openapi.json. После этого http://localhost:3000/docs — рабочая
#   документация, которая обновляется вместе с сервисом.

# ═══ 2. ТИПЫ И КЛИЕНТ ДЛЯ ФРОНТЕНДА ═══
#
# Тут выбор зависит от того, сколько вам нужно.
#
#   openapi-typescript — генерирует ТОЛЬКО типы TypeScript,
#     плюс крошечная обёртка openapi-fetch для запросов. MIT, живой,
#     самый быстрый на больших спецификациях. Берут, когда не хочется
#     тащить в проект чужой сгенерированный клиент.
#
#   @hey-api/openapi-ts — полноценный SDK с плагинной архитектурой.
#     MIT, живой; преемник старого openapi-typescript-codegen.
#
#   orval — «всё включено»: клиент, хуки React Query или SWR,
#     схемы валидации Zod, моки для MSW. MIT, живой.
#
#   kubb — модульный: включаете только нужные генераторы
#     (типы, Zod, хуки, моки). MIT, живой.
#
#   openapi-generator — старый тяжёлый генератор на Java, 100+ языков,
#     релизы ежемесячно. Берут, когда нужен клиент на Java, Go, C# и так
#     далее. Для чистого фронтенда — слишком громоздкий.

# Только типы — минимальный вариант:
npx openapi-typescript openapi.yaml -o src/api/schema.d.ts

#   ✨ openapi-typescript
#   🚀 openapi.yaml -> src/api/schema.d.ts [312ms]

# Что получилось (фрагмент сгенерированного файла):
#
#   export interface paths {
#     "/notifications": {
#       post: operations["sendNotification"];
#       get: operations["listNotifications"];
#     };
#   }
#
# Дальше в коде запросы становятся типизированными: опечатка в имени поля
# или забытый обязательный параметр подсвечиваются прямо в редакторе.
# Про типы подробно — на странице про статический анализ.

# Полный клиент со всеми обвязками:
npx orval --config orval.config.ts

#   🍻 Start orval v7
#   🎉 notify - Your OpenAPI spec has been converted into ready to use orval!
#   generated: src/api/notify.ts, src/api/notify.schemas.ts, src/api/notify.msw.ts

# ═══ 3. ЗАГЛУШКА СЕРВЕРА ═══
#
# Заглушка (mock server) — поддельный сервер, который отвечает по вашей
# спецификации выдуманными, но правильными по форме данными.
# Зачем: фронтенд начинает работать в тот день, когда согласована
# спецификация, а не в тот, когда бэкенд дописал код.

npx @stoplight/prism-cli mock openapi.yaml

#   [CLI] …  awaiting  Starting Prism…
#   [CLI] ℹ  info      POST       http://127.0.0.1:4010/notifications
#   [CLI] ℹ  info      GET        http://127.0.0.1:4010/notifications
#   [CLI] ▶  start     Prism is listening on http://127.0.0.1:4010

curl -s -X POST http://127.0.0.1:4010/notifications \\
  -H 'content-type: application/json' \\
  -d '{"userId":"8f14e45f-ceea-467a-9c1c-1a1f3d7fa2b0","channel":"email","text":"тест"}'

#   {"id":"c0a8...","userId":"8f14e45f-...","channel":"email",
#    "text":"тест","status":"queued","createdAt":"2026-08-27T10:00:00Z"}
#
# Данные выдуманы, но форма ровно та, что описана в схеме.
# Замечание про Prism: он тоже из семейства Stoplight, которое теперь
# принадлежит SmartBear. Альтернатива на фронтенде — моки MSW,
# которые умеет генерировать orval или kubb.

# ═══ 4. ПРОВЕРКА, ЧТО СЕРВЕР НЕ РАЗОШЁЛСЯ С ОБЕЩАНИЕМ ═══
#
# Это самый ценный пункт и его чаще всего пропускают. Без него
# спецификация — просто ещё один текст, который может соврать.
#
# Два подхода, и они не исключают друг друга.
#
# ПОДХОД А. Сервер сам проверяет запросы и ответы по схеме.
#   Если вы работаете code-first (см. блок выше), это уже так:
#   одна и та же схема валидирует вход и попадает в спецификацию.
#
# ПОДХОД Б. Отдельный тест в пайплайне бьёт по настоящему сервису
#   и сверяет ответ со схемой из openapi.yaml. Проще всего сделать это
#   в тех тестах, которые у вас уже есть: взять схему из файла,
#   прогнать через любой валидатор JSON Schema и упасть при расхождении.
#   Про то, как устроены такие тесты, — на странице про тесты.

# ═══ ЧТО ЗАПОМНИТЬ ═══
#
# Один файл openapi.yaml окупается ЧЕТЫРЕЖДЫ:
#   сайт с песочницей → внешним потребителям не нужен ваш чат;
#   типы и клиент     → фронтенд не пишет запросы руками и не опечатывается;
#   заглушка          → команды работают параллельно, а не по очереди;
#   проверка          → описание не может тихо разойтись с реальностью.
#
# ЧЕСТНАЯ ЦЕНА. Сгенерированный код надо куда-то класть и не редактировать
# руками — иначе при следующей генерации правки затрутся. Обычно папку
# с генерацией кладут в .gitignore и генерируют на сборке, а если кладут
# в репозиторий — то с заголовком «сгенерировано, не править» и запретом
# в правилах ревью. Второй расход: ещё один шаг сборки, который может
# сломаться. Оба расхода несопоставимы с выигрышем.
`;

  protected readonly openapiLinting = `
# ═══ СПЕЦИФИКАЦИЮ ТОЖЕ НАДО ПРОВЕРЯТЬ ═══
#
# Файл openapi.yaml — такой же исходник, как код. Его пишут руками
# (или подправляют аннотациями), а значит, в нём заводятся те же болезни:
# разнобой в именовании, забытые описания, скопированные куски.
# И, что гораздо страшнее, — незаметные изменения, ломающие чужие приложения.
#
# Проверок ровно две, и они про разное.
#
#   ЛИНТЕР СТИЛЯ    — «файл написан по нашим правилам?»
#                     Смотрит только на текущий файл.
#   ПРОВЕРКА СОВМЕСТИМОСТИ — «новая версия не сломает тех, кто уже вызывает?»
#                     Сравнивает файл со старым файлом.
#
# Первая бережёт читателя. Вторая бережёт чужой прод. Нужны обе.

# ═══ ЧАСТЬ 1. ЛИНТЕР СПЕЦИФИКАЦИИ ═══
#
# Стандарт де-факто — Spectral (сделан в Stoplight, теперь SmartBear).
# Живой, активно развивается. Умеет OpenAPI, AsyncAPI и вообще любой
# JSON или YAML. Правила пишутся своими, готовые наборы подключаются.
# Redocly CLI тоже умеет линтить — если вы уже взяли его для превью,
# отдельный линтер можно не ставить.
#
# Файл .spectral.yaml в корне репозитория:

extends: ['spectral:oas']
# ^ Берём готовый набор правил для OpenAPI. Он проверяет базовое:
#   валидность структуры, обязательные поля, корректность $ref.

rules:
  # Дальше — правила под вашу команду. Каждое читается сверху вниз:
  # «в каком месте файла смотрим» → «что там должно быть».

  operation-summary-required:
    description: У каждой операции должен быть summary
    message: 'У операции нет summary — она попадёт в оглавление как безымянная'
    given: $.paths[*][get,post,put,patch,delete]
    # ^ given — путь внутри файла на языке JSONPath.
    #   Читается: «у всех путей, у всех перечисленных методов».
    severity: error
    # ^ error валит проверку, warn только предупреждает.
    then:
      field: summary
      function: truthy
      # ^ truthy — «поле должно существовать и быть непустым»

  operation-id-camel-case:
    description: operationId пишется в camelCase
    message: '{{property}} должен быть в camelCase: sendNotification, не send_notification'
    given: $.paths[*][*].operationId
    severity: error
    then:
      function: pattern
      functionOptions:
        match: '^[a-z][a-zA-Z0-9]*$'
    # ЗАЧЕМ. Из operationId генераторы делают имена функций в клиенте.
    # Разнобой в стиле сразу вылезает у всех потребителей сразу.

  no-trailing-slash:
    description: Пути не заканчиваются косой чертой
    message: 'Путь {{property}} заканчивается на / — уберите'
    given: $.paths
    severity: error
    then:
      field: '@key'
      function: pattern
      functionOptions:
        notMatch: '.+\\/$'
    # ЗАЧЕМ. /orders и /orders/ — два разных пути для многих клиентов.
    # Один лишний символ порождает половину багов «404 непонятно почему».

  schema-property-description:
    description: У каждого поля схемы есть описание
    message: 'Поле без description — читателю придётся догадываться'
    given: $.components.schemas[*].properties[*]
    severity: warn
    # ^ warn, а не error: включать это сразу как error на существующем
    #   проекте — значит получить триста ошибок и выключить линтер совсем.
    #   Про то, как вводить правила без бунта, — на странице про линтеры.
    then:
      field: description
      function: truthy

  response-error-schema:
    description: Ответы 4xx и 5xx описывают тело ошибки
    message: 'Ошибка без схемы тела — клиент не знает, что разбирать'
    given: $.paths[*][*].responses[?(@property.match(/^[45]/))]
    severity: warn
    then:
      field: content
      function: truthy

# Запуск:
#   npx spectral lint openapi.yaml
#
# Вывод выглядит так:
#
#   openapi.yaml
#     14:7  error  operation-summary-required  У операции нет summary
#     22:7  error  operation-id-camel-case     send_notification должен быть в camelCase
#     51:9   warn  schema-property-description Поле без description
#
#   ✖ 3 problems (2 errors, 1 warning)
#
# Ставится в пайплайн рядом с линтером кода. Ошибка — сборка красная.

# ═══ ЧАСТЬ 2. ПРОВЕРКА ОБРАТНОЙ СОВМЕСТИМОСТИ ═══
#
# Это про другое. Файл может быть безупречен по стилю и при этом
# уничтожить три мобильных приложения в проде.
#
# ЧТО ЛОМАЕТ КЛИЕНТОВ (breaking changes):
#   — удалили путь или операцию;
#   — сделали ранее необязательный параметр обязательным;
#   — удалили поле из ответа (клиент его читал и получит undefined);
#   — сузили тип: было string — стало enum из трёх значений;
#   — переименовали поле (это удаление плюс добавление);
#   — убрали значение из enum, которое клиент отправлял;
#   — ужесточили ограничение: было maxLength 500 — стало 200.
#
# ЧТО НЕ ЛОМАЕТ (safe changes):
#   — добавили новый путь или новую операцию;
#   — добавили НЕобязательный параметр;
#   — добавили поле в ответ (аккуратные клиенты игнорируют лишнее);
#   — расширили enum в ОТВЕТЕ — но осторожно: клиент со списком из трёх
#     значений может не знать, что делать с четвёртым;
#   — ослабили ограничение: было maxLength 200 — стало 500;
#   — добавили описание, пример, тег.
#
# ЗАПОМНИТЬ ПРОСТЫМ ПРАВИЛОМ:
#   можно РАЗРЕШАТЬ БОЛЬШЕ на входе и ОБЕЩАТЬ БОЛЬШЕ на выходе.
#   Нельзя ТРЕБОВАТЬ БОЛЬШЕ на входе и ДАВАТЬ МЕНЬШЕ на выходе.

# ИНСТРУМЕНТ: oasdiff. Лицензия Apache-2.0, живой, активно обновляется.
# Считает разницу двух спецификаций и отдельно — список ломающих изменений.
#
#   oasdiff breaking openapi-main.yaml openapi.yaml
#
#   3 breaking changes: 2 error, 1 warning
#   error  [request-parameter-became-required] at openapi.yaml
#          in API POST /notifications
#          the 'query' request parameter 'dedupeKey' became required
#   error  [response-property-removed] at openapi.yaml
#          in API GET /notifications 200 response
#          the response property 'legacyId' was removed
#   warning [response-property-enum-value-added] …
#
# В пайплайне это выглядит так: на каждый pull request скачиваем
# спецификацию из основной ветки, сравниваем с новой, и если найдено
# хоть одно ломающее изменение — проверка красная. Снять красноту можно
# только осознанно: пометив изменение как намеренное и выпустив новую
# версию API. Про пайплайны — на странице про Git и на странице
# про CI/CD в разделе «Инфраструктура».

# ═══ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ ПРО СТАРЫЕ СОВЕТЫ ═══
#
# В статьях 2023–2024 годов в этой роли часто советуют Optic.
# Не берите: 8 января 2026 года репозиторий Optic архивирован.
# (Atlassian купила проект весной 2024-го, дальше он развития не получил.)
# Архивированный репозиторий — это только чтение: ни исправлений,
# ни новых версий. Живая замена — oasdiff для сравнения в пайплайне
# и Spectral для линта.
#
# Это хорошая иллюстрация общего правила: прежде чем взять инструмент
# из статьи, посмотрите дату последнего коммита. Про то, как отличать
# живой проект от мёртвого, — на странице «Как выбирать» в разделе
# «Введение в экосистему».

# ═══ ЧЕСТНАЯ ЦЕНА ═══
#
# Проверка совместимости будет иногда останавливать вас на изменении,
# которое вы считаете безобидным. Это раздражает — ровно до первого раза,
# когда она поймает удаление поля, которым пользуется чужое приложение.
# Дешевле пять минут спора в pull request, чем ночной звонок из поддержки.
`;

  protected readonly storybookWhat = `
// ═══ STORYBOOK ПРОСТЫМИ СЛОВАМИ ═══
//
// Storybook — отдельное маленькое приложение, которое запускается рядом
// с вашим и показывает КАЖДЫЙ компонент интерфейса отдельно от остальных,
// сразу во всех состояниях. Слева список компонентов, справа один компонент
// на пустом фоне, снизу панель, где можно крутить его параметры руками.
//
// АНАЛОГИЯ. Ателье. Чтобы понять, как ведёт себя ткань, не шьют платье —
// смотрят витрину с образцами: вот эта ткань на свету, вот в тени,
// вот мятая, вот растянутая. Storybook — витрина образцов для интерфейса.
// Кнопка отдельно. Кнопка в состоянии загрузки отдельно. Кнопка
// с очень длинным текстом отдельно. Всё видно за пять секунд.
//
// Проект живой и самый активный в своей нише: версия 10.0 вышла
// 28 октября 2025, лицензия MIT, поддерживаются React, Vue, Angular,
// Svelte, Web Components и Next.js.

// ═══ КАК ОБЫЧНО ДЕЛАЮТ БЕЗ НЕГО ═══
//
// Задача: «в кнопке во время отправки формы должен крутиться индикатор».
// Разработчик правит компонент Button и хочет посмотреть, что вышло.
// Чтобы увидеть кнопку в состоянии загрузки, ему надо:
//
//   1) запустить всё приложение;
//   2) залогиниться (или дождаться, пока восстановится сессия);
//   3) дойти кликами до корзины — это четыре экрана;
//   4) положить товар, потому что на пустой корзине кнопки нет;
//   5) нажать «оформить» и успеть разглядеть индикатор за 200 миллисекунд.
//
// Чтобы посмотреть состояние «ошибка сервера», надо ещё и сломать сервер.
// Чтобы посмотреть «очень длинный текст» — завести товар с длинным именем.
// Итого: одна правка стилей на две минуты требует двадцати минут кликов.

// ═══ ЧТО СЛУЧИТСЯ ═══
//
// БЕДА 1. РЕДКИЕ СОСТОЯНИЯ НЕ ПРОВЕРЯЮТ НИКОГДА.
//   «Загрузка» длится доли секунды, «ошибка» случается раз в месяц,
//   «пустой список» бывает только у нового пользователя. До них дорого
//   добираться — значит, до них не добираются. Значит, они сломаны.
//   Классика жанра: индикатор загрузки белый на белом фоне,
//   и это выясняется через полгода от пользователя.
//
// БЕДА 2. КОМПОНЕНТЫ ДУБЛИРУЮТСЯ.
//   Новый человек не знает, что кнопка с иконкой уже есть: она спрятана
//   на четвёртом экране за логином. Он пишет свою. Через год в проекте
//   шесть кнопок, которые выглядят почти одинаково и ведут себя по-разному.
//
// БЕДА 3. ДИЗАЙНЕР НЕ ВИДИТ РЕЗУЛЬТАТ.
//   Он смотрит на макет, а не на живой компонент. Расхождение всплывает
//   на демонстрации перед заказчиком — в самый неподходящий момент.
//
// БЕДА 4. ВЁРСТКА ЛОМАЕТСЯ НЕЗАМЕТНО.
//   Поправили общий стиль — сломалась кнопка на странице, куда никто
//   не заходил три недели. Узнают об этом пользователи.

// ═══ КАК НАДО ═══
//
// Рядом с компонентом кладётся файл с «историями» (stories) — это просто
// описания вида «вот такой компонент с вот такими параметрами».
// Storybook собирает из них витрину.
//
// Что меняется на практике:
//
//   1. РАЗРАБОТКА БЕЗ КЛИКОВ. Правите кнопку — видите её сразу,
//      во всех пяти состояниях одновременно, без логина и корзины.
//      Цикл «правка → результат» сокращается с минут до секунды.
//
//   2. ВСЕ СОСТОЯНИЯ ВИДНЫ ОДНОВРЕМЕННО. Загрузка, ошибка, длинный
//      текст, отключённое состояние — рядом на одном экране.
//      Белый индикатор на белом фоне становится очевиден сразу.
//
//   3. ОБЩИЙ ЯЗЫК С ДИЗАЙНЕРОМ. Вы даёте ссылку на витрину.
//      Дизайнер смотрит живой компонент, а не картинку, и говорит
//      «отступ слева на четыре пикселя больше» до того, как всё собрано.
//
//   4. КАТАЛОГ ТОГО, ЧТО УЖЕ ЕСТЬ. Новый человек за десять минут видит
//      весь набор компонентов и перестаёт писать седьмую кнопку.
//
//   5. ФУНДАМЕНТ ДЛЯ ПРОВЕРОК. История — это готовый сценарий,
//      по которому можно прогнать тест взаимодействия, проверку
//      доступности и сравнение картинок между сборками.
//      Об этом отдельно, в следующем блоке.

// ═══ ЧТО ТАКОЕ «ИСТОРИЯ», ЕСЛИ СОВСЕМ ПРОСТО ═══
//
// История — это ответ на вопрос «покажи мне компонент вот в таком виде».
// Файл Button.stories.js лежит рядом с Button.jsx и выглядит примерно так
// (полный разбор — в следующем блоке):

// import { Button } from './Button';
//
// export default { component: Button };
//
// export const Primary = { args: { label: 'Купить', variant: 'primary' } };
// export const Loading = { args: { label: 'Купить', loading: true } };
// export const Disabled = { args: { label: 'Купить', disabled: true } };

// Три экспортированные константы — три карточки в витрине.
// Никакого волшебства: это описание набора параметров, не более.

// ═══ ЧЕСТНАЯ ЦЕНА ═══
//
// Storybook — это ещё одна сборка в проекте. Её надо настраивать,
// обновлять и чинить, когда она отвалится. Истории надо писать
// и поддерживать: устаревшая история показывает компонент, которого
// больше нет, и врёт ровно как устаревшая вики.
//
// Отдельно про версию 10: она перешла на ESM-модули, и это единственное
// ломающее изменение при переходе с девятой. Взамен установка стала
// заметно легче, появилось мокирование модулей и поддержка свежих
// Next и Vitest. Требуется современный Node — 20.16 и новее.
//
// Когда эта цена не окупается — разобрано ниже отдельным блоком.
`;

  protected readonly storybookStory = `
// ═══ ИСТОРИЯ КОМПОНЕНТА, РАЗОБРАННАЯ ПОСТРОЧНО ═══
//
// Файл лежит РЯДОМ с компонентом: src/ui/Button/Button.stories.js.
// Имя файла обязано содержать .stories — по нему Storybook их и находит.
// Формат называется CSF (Component Story Format) — «просто экспорты».
//
// Дальше — полный файл с комментариями к каждой части.

import { Button } from './Button';

// ═══ ЧАСТЬ 1. ЭКСПОРТ ПО УМОЛЧАНИЮ — «ПАСПОРТ» ФАЙЛА ═══
//
// Он описывает не отдельную историю, а весь файл целиком:
// какой компонент показываем и что общего у всех историй ниже.

export default {
  title: 'UI/Button',
  // ^ Место в дереве слева. Косая черта делает папку: раздел UI,
  //   внутри него Button. Если title не написать, Storybook соберёт
  //   его сам из пути к файлу — обычно это и лучше.

  component: Button,
  // ^ Какой компонент показываем. Отсюда Storybook сам вытащит список
  //   свойств и построит панель управления внизу экрана.

  args: {
    // ^ args — это входные параметры компонента (props). То, что написано
    //   здесь, попадает во ВСЕ истории файла как значения по умолчанию.
    //   В каждой отдельной истории их можно переопределить.
    label: 'Оформить заказ',
    variant: 'primary',
  },

  argTypes: {
    // ^ argTypes настраивает панель управления: каким виджетом крутить
    //   каждый параметр. Без этого Storybook угадывает сам,
    //   но угадывает не всегда удачно.
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'danger'],
      description: 'Внешний вид кнопки',
    },
    onClick: { action: 'clicked' },
    // ^ action: клики будут писаться в панель «Actions» внизу.
    //   Это удобно: видно, что обработчик вообще вызвался.
  },

  parameters: {
    layout: 'centered',
    // ^ Как расположить компонент на холсте: centered (по центру),
    //   fullscreen (во всю ширину), padded (с отступами, это по умолчанию).
  },

  tags: ['autodocs'],
  // ^ Тег autodocs говорит: собрать по этому файлу ещё и страницу
  //   документации — таблицу свойств плюс все истории подряд.
  //   Получается справочник компонента бесплатно.
};

// ═══ ЧАСТЬ 2. САМИ ИСТОРИИ ═══
//
// Каждый именованный экспорт — одна карточка в витрине.
// Имя экспорта становится подписью в дереве слева.

// ─── Обычное состояние ───
export const Default = {};
// ^ Пустой объект: берём все args из «паспорта» и ничего не меняем.
//   Это базовый вид кнопки, с которого начинают смотреть.

// ─── Второстепенная кнопка ───
export const Secondary = {
  args: { variant: 'secondary' },
  // ^ Меняем один параметр, остальные наследуются. Именно так
  //   и надо: история отличается от базовой минимально,
  //   чтобы было понятно, ЧТО она показывает.
};

// ─── Опасное действие ───
export const Danger = {
  args: { variant: 'danger', label: 'Удалить навсегда' },
};

// ─── Загрузка ───
export const Loading = {
  args: { loading: true },
  // ^ ЗАЧЕМ ЭТА ИСТОРИЯ НУЖНА БОЛЬШЕ ВСЕХ. В живом приложении это
  //   состояние длится долю секунды и его невозможно разглядеть.
  //   Здесь оно висит вечно: видно, не прыгает ли ширина кнопки,
  //   виден ли индикатор, не потерялся ли текст.
  parameters: {
    docs: { description: { story: 'Кнопка на время отправки формы. Ширина не должна меняться, иначе форма дёргается.' } },
  },
};

// ─── Ошибка ───
export const WithError = {
  args: { error: 'Не удалось отправить. Попробуйте ещё раз.' },
  // ^ В приложении это состояние воспроизводится только сломанным
  //   сервером. Здесь — одной строчкой.
};

// ─── Очень длинный текст ───
export const LongLabel = {
  args: {
    label: 'Оформить заказ и перейти к оплате банковской картой или через сервис рассрочки',
  },
  // ^ САМАЯ НЕДООЦЕНЁННАЯ ИСТОРИЯ. Половина поломок вёрстки — это
  //   текст, который оказался длиннее ожидаемого: перевод на немецкий,
  //   длинное имя товара, фамилия из трёх частей. Проверять это
  //   вручную никто не будет — а история проверяет сама, каждый раз.
};

// ─── Отключённая ───
export const Disabled = {
  args: { disabled: true },
  // ^ Проверяем не только серый цвет: курсор, отсутствие реакции
  //   на наведение и — важно — читаемость текста. Слишком бледный
  //   серый на белом не проходит проверку доступности.
};

// ─── Совсем без текста, только иконка ───
export const IconOnly = {
  args: { label: '', icon: 'trash' },
  // ^ Крайний случай. Тут всплывает вопрос: а как эту кнопку прочитает
  //   программа экранного доступа? Нужен aria-label. Проверка
  //   доступности из следующего блока поймает это автоматически.
};

// ═══ ЧТО МЫ ПОЛУЧИЛИ ═══
//
// Восемь строчек-историй дают:
//   — восемь карточек в витрине, каждая открывается по ссылке;
//   — автоматическую страницу справочника со всеми свойствами;
//   — панель, где параметры крутятся мышкой без правки кода;
//   — восемь готовых сценариев для тестов и сравнения картинок.
//
// ПРАВИЛО ПРО СОСТАВ ИСТОРИЙ. Минимальный набор для любого компонента:
//   обычное, загрузка, ошибка, пустое/отключённое, крайний случай
//   (очень длинный текст или очень много элементов).
// Если хотя бы одного из пяти нет — скорее всего, оно и сломано.
//
// ЧЕСТНАЯ ЦЕНА. Эти восемь историй надо написать (минут пятнадцать)
// и потом поддерживать: переименовали свойство loading в isLoading —
// история сломается. Storybook при этом сломается ЗАМЕТНО, красным,
// а не тихо, как вики. Это важное отличие: врущая история видна сразу.
`;

  protected readonly storybookBeyond = `
// ═══ ЧТО STORYBOOK ДАЁТ СВЕРХ ВИТРИНЫ ═══
//
// Витрина — это только вход. Настоящая ценность в том, что каждая
// история уже описывает компонент в конкретном состоянии, а значит,
// её можно не только смотреть глазами, но и проверять автоматически.
// Три вещи сверху: тесты взаимодействия, доступность, сравнение картинок.
//
// Общий контекст про тесты — на странице про тесты в этом же подразделе.
// Здесь только то, что специфично для витрины компонентов.

// ═══ 1. ТЕСТ ВЗАИМОДЕЙСТВИЯ ПРЯМО В ИСТОРИИ ═══
//
// К истории дописывается функция play — сценарий «что делает пользователь
// после того, как компонент отрисовался». Storybook выполняет её при
// открытии истории и рисует шаги в отдельной панели: видно, на каком
// шаге упало, и можно отмотать назад.

import { expect, userEvent, within } from 'storybook/test';
import { LoginForm } from './LoginForm';

export default { component: LoginForm };

export const FailsOnEmptyPassword = {
  play: async ({ canvasElement }) => {
    // canvasElement — кусок страницы, где отрисован компонент.
    // within ограничивает поиск этим куском, чтобы не зацепить
    // элементы самого интерфейса Storybook.
    const canvas = within(canvasElement);

    // Ищем поля так же, как их видит пользователь: по подписи,
    // а не по классу или идентификатору. Тогда тест не сломается
    // от переименования класса и заодно проверит, что подпись есть.
    await userEvent.type(canvas.getByLabelText('Электронная почта'), 'user@acme.com');

    // Пароль намеренно не заполняем.
    await userEvent.click(canvas.getByRole('button', { name: 'Войти' }));

    // Ожидание: появилось сообщение об ошибке.
    await expect(canvas.getByText('Введите пароль')).toBeInTheDocument();
  },
};

// ЧЕМ ЭТО ЛУЧШЕ ОБЫЧНОГО ТЕСТА. Обычный тест падает строкой в консоли.
// Тест в истории падает В ВИТРИНЕ: вы видите компонент ровно в том
// состоянии, на котором сломалось, и можете щёлкать по нему мышкой.
// Отладка ускоряется в разы.

// ═══ 2. ПРОВЕРКА ДОСТУПНОСТИ ═══
//
// Доступность (accessibility, часто пишут a11y) — то, насколько
// интерфейсом можно пользоваться с клавиатуры и через программу
// экранного доступа. Storybook прогоняет по каждой истории набор
// автоматических правил (движок axe) и показывает нарушения в панели.
//
// Что ловится без единой строчки с вашей стороны:
//   — контраст текста и фона ниже нормы (тот самый бледно-серый);
//   — кнопка-иконка без текстовой подписи для экранного диктора;
//   — поле ввода без связанного с ним label;
//   — картинка без alt;
//   — неправильная вложенность заголовков.
//
// Панель доступности была переработана в девятой версии; в десятой
// проверки можно запускать и из интерфейса, и из командной строки.
//
// ЧЕГО ЭТО НЕ ДЕЛАЕТ. Автоматика ловит примерно треть проблем.
// Она не скажет, понятен ли текст ошибки и логичен ли порядок обхода
// по клавише Tab. Это всё равно проверяется руками.

// ═══ 3. СРАВНЕНИЕ КАРТИНОК МЕЖДУ СБОРКАМИ ═══
//
// Идея: снять картинку каждой истории, сохранить, и на следующей сборке
// снять заново и сравнить попиксельно. Отличается — показать оба
// изображения рядом и спросить человека: это поломка или так и хотели.
//
// ЗАЧЕМ. Это единственный способ поймать «поправили общий отступ,
// и на семи страницах поехала вёрстка». Ни линтер, ни тесты
// такого не видят: код формально верный.
//
// ЧЕМ ДЕЛАТЬ:
//   — Chromatic. Платный сервис от той же команды, что делает
//     Storybook; понимает истории напрямую. Актуальные цены и что
//     входит в бесплатный тариф — смотрите на их сайте, они меняются.
//   — Playwright со встроенным сравнением снимков (toHaveScreenshot).
//     Бесплатно, но снимки хранятся у вас в репозитории и их надо
//     обновлять руками при каждом намеренном изменении.
//   — BackstopJS. Открытый, лицензия MIT, отдельный инструмент.
//
// ЧЕСТНАЯ ЦЕНА СРАВНЕНИЯ КАРТИНОК. Ложные срабатывания. Другая версия
// шрифта, другая версия браузера, мигающий курсор, анимация, дата
// «сегодня» в карточке — и картинка отличается на два пикселя, а сборка
// красная. Лечится фиксацией шрифтов и времени и отключением анимаций,
// но настраивать это придётся, и не за один вечер.

// ═══ ВАЖНОЕ ПРО ЗАПУСК ТЕСТОВ: ЧТО ИЗМЕНИЛОСЬ ═══
//
// В статьях 2023–2024 годов вам посоветуют @storybook/test-runner —
// отдельный запускальщик на Jest и Playwright. Он ещё публикуется,
// но считается устаревшим путём.
//
// Актуальный путь — аддон для Vitest (браузерный режим). Он даёт
// тесты взаимодействия, проверки доступности и покрытие кода прямо
// из интерфейса Storybook и из командной строки одним запуском.
// Требование: сборщик Vite. Это React, Vue, Svelte, Preact,
// Web Components и Next.js на Vite.
//
// Если у вас проект на Webpack и переехать нельзя — старый запускальщик
// пока работает, но закладывайтесь на переход.
//
// Про Vitest как таковой и про то, чем он отличается от Jest, —
// на странице про тесты.

// ═══ ЧТО ЗАПОМНИТЬ ═══
//
// Витрина      → видно все состояния сразу, не кликая приложение
// play-функция → тест взаимодействия, падающий видимо и наглядно
// axe          → треть проблем доступности бесплатно, остальное руками
// снимки       → единственная защита от «поехало на семи страницах»
//
// И главное отличие Storybook от вики: он ломается ЗАМЕТНО.
// Устаревшая история подсвечивается красным, а устаревшая
// страница вики выглядит ровно так же, как свежая.
`;

  protected readonly storybookWhenNot = `
═══ КОГДА STORYBOOK НЕ НУЖЕН ═══

Про инструменты обычно пишут, чем они хороши. Про то, когда их не надо
брать, пишут редко — а это решение принимается чаще.


═══ ПРИЗНАК 1. КОМПОНЕНТОВ МАЛО ═══

Три-пять своих компонентов, остальное — готовая библиотека вроде
Material или Ant Design. Витрина будет пустой, а поддерживать сборку
всё равно придётся. Библиотека уже принесла свою документацию,
и она полнее вашей.

Порог, после которого витрина обычно окупается: примерно с пятнадцати
своих компонентов, которые переиспользуются в разных местах.
Это не строгое число, это ориентир.


═══ ПРИЗНАК 2. НЕТ ДИЗАЙН-СИСТЕМЫ И НЕ ПЛАНИРУЕТСЯ ═══

Дизайн-система — это общий набор кнопок, полей, отступов и цветов,
которым пользуются все страницы. Если её нет и каждая страница верстается
как получится, то и показывать в витрине нечего: у вас не компоненты,
у вас куски конкретных страниц. Они не переиспользуются,
и смотреть их отдельно от страницы бессмысленно.


═══ ПРИЗНАК 3. ПРОЕКТ ЖИВЁТ ТРИ МЕСЯЦА ═══

Прототип, внутренняя утилита на десять человек, эксперимент, который
закроют. Настройка витрины окупается на горизонте от полугода,
а не на горизонте демонстрации через две недели.


═══ ПРИЗНАК 4. КОМАНДА ИЗ ОДНОГО ЧЕЛОВЕКА, И ЭТО НАДОЛГО ═══

Часть ценности витрины — общий язык между людьми: между разработчиками,
с дизайнером, с новичком. Когда людей нет, остаётся только удобство
разработки. Оно реально, но одно оно цену обычно не окупает.


═══ ЧЕСТНО ПРО ЦЕНУ ═══

Storybook — это не файл. Это отдельная сборка со своим конфигом,
своими зависимостями и своими поломками. Что придётся делать:

  — настроить, чтобы витрина видела ваши псевдонимы путей, стили,
    шрифты, переменные окружения и провайдеры контекста;
  — обновлять вместе с проектом; крупные версии иногда ломают
    настройку — например, десятая перешла на ESM-модули,
    и это потребовало правок у всех, кто обновлялся с девятой;
  — писать истории на новые компоненты и править на изменённые;
  — чинить сборку витрины, когда она отвалилась, — а она отваливается
    отдельно от приложения, и починка не приближает вас к задаче.


═══ ЗАБРОШЕННЫЙ STORYBOOK ХУЖЕ, ЧЕМ ЕГО ОТСУТСТВИЕ ═══

Это главная мысль блока. Витрина, где половина историй показывает
компоненты годичной давности, работает точно так же, как вики,
где половина страниц врёт: ей нельзя верить, а значит, ей не пользуются.

Плюс к этому вы платите за неё каждый день временем сборки и обновлений.
Отсутствие витрины стоит ноль. Заброшенная витрина стоит денег
и не приносит пользы — это худшая из трёх позиций.

ПРОСТОЕ ПРАВИЛО. Если через два месяца после установки в витрине нет
историй на компоненты, добавленные за эти два месяца, — витрина
не прижилась. Не уговаривайте себя. Удалите её и вернитесь к вопросу
позже, когда компонентов станет заметно больше.


═══ А ЕСТЬ ЛИ ЧТО-ТО ПОЛЕГЧЕ ═══

Да, но выбор небогатый, и это стоит знать заранее.

  Ladle — живой, быстрый старт, но только React.
    Разумный вариант, если пугает вес Storybook, а стек React.

  Histoire — ориентирован на Vue и Svelte, но стагнирует:
    коммиты редкие и в основном мелкие исправления.
    Брать в новый проект не советую.

Ни один из них заметной доли у Storybook не отобрал. Практический вывод:
либо Storybook, либо ничего. Промежуточные варианты живут хуже,
чем оба края.


═══ ЧТО ЗАПОМНИТЬ ═══

  меньше пятнадцати своих компонентов  → скорее всего, не нужен
  нет дизайн-системы                   → нечего показывать
  проект на три месяца                 → не окупится
  один человек навсегда                → польза только личная

  много переиспользуемых компонентов   → нужен
  есть дизайнер, с которым спорят      → нужен
  библиотеку используют другие команды → нужен обязательно
`;

  protected readonly adrWhatAndWhy = `
═══ ADR: КОРОТКАЯ ЗАПИСЬ «МЫ РЕШИЛИ ТАК, ПОТОМУ ЧТО» ═══

ADR расшифровывается как Architecture Decision Record — «запись
архитектурного решения». Звучит внушительно, а на деле это текстовый файл
на одну-две страницы, который отвечает на четыре вопроса:

  ЧТО было не так (контекст),
  ЧТО мы решили (решение),
  ЧТО из этого выйдет — хорошего и плохого (последствия),
  ЧТО ещё рассматривали и почему отвергли (варианты).

Файл кладут в репозиторий рядом с кодом, в папку docs/adr, и больше
не трогают. Именно «не трогают» — это ключевое, разберём ниже.

Практику предложил Майкл Найгард в 2011 году в статье «Documenting
Architecture Decisions». В ноябре 2017-го технологический радар
ThoughtWorks поставил лёгкие ADR в кольцо Adopt — то есть «берите
и пользуйтесь». С тех пор это устоявшаяся, ничем не заменённая практика.


═══ КАКУЮ БОЛЬ ЭТО ЛЕЧИТ ═══

История, которая происходит в каждой второй команде.

СЕНТЯБРЬ 2025. Команда выбирает, где хранить сессии пользователей.
Спорят два дня. Пробуют Redis, натыкаются на то, что в их окружении
он не переживает переезд между зонами без потери данных, а бизнесу
нужно, чтобы человека не выкидывало из личного кабинета. Решают хранить
сессии в PostgreSQL — медленнее, зато переживает всё. Расходятся довольные.

Никто ничего не записал. Все всё помнят: разговор был вчера.

МАЙ 2026. Из трёх участников спора один в другой компании, второй
на другом проекте, третий помнит только, что «что-то было с Redis».
Приходит новый разработчик, видит сессии в базе, читает статью
«почему сессии надо держать в Redis» и пишет в чат:
«Ребята, у нас тут какая-то дичь, давайте я переделаю на Redis,
это два дня работы и будет в десять раз быстрее».

Ему никто не может внятно возразить. Аргумент «мы это уже обсуждали»
без деталей звучит как «мы просто ленимся». Он делает.

ИЮНЬ 2026. Переезд между зонами. Всех пользователей выкидывает
из личного кабинета в середине оформления заказа. Три дня разбирательств,
откат, извинения. Ровно те же грабли, ровно с тем же результатом.

Всё это стоило бы двадцати минут на написание одного файла в сентябре.


═══ АНАЛОГИЯ: ПРОТОКОЛ СОБРАНИЯ ═══

Собрание жильцов решает, с какой стороны двора ставить шлагбаум.
Спорят час, взвешивают, решают — с восточной. Через год новый жилец
возмущается: «с западной было бы удобнее всем, кто ставит машину».
И он прав — с западной удобнее. Но в протоколе написано:
с западной не согласовала пожарная служба, там проезд для машины.

Протокол не запрещает пересматривать решение. Он избавляет от
пересматривания ВСЛЕПУЮ. Разница огромна: в первом случае вы спорите
с новым знанием на руках, во втором — заново проходите весь путь.


═══ ЧТО ADR НЕ ЕСТЬ ═══

НЕ ДОКУМЕНТАЦИЯ АРХИТЕКТУРЫ. Она описывает, как устроено СЕЙЧАС,
и должна поддерживаться. ADR описывает один момент в прошлом
и не поддерживается принципиально.

НЕ ПРОТОКОЛ КАЖДОЙ ВСТРЕЧИ. Записывают решения, которые дорого
менять: выбор базы, способ авторизации, формат обмена между сервисами,
отказ от библиотеки, переход на другой протокол.
Выбор имени переменной в ADR не пишут.

НЕ ДОКУМЕНТ ДЛЯ НАЧАЛЬСТВА. Это письмо будущему разработчику,
в том числе вам самому. Пишется человеческим языком, без парадности.

НЕ ТРЕБУЕТ ОДОБРЕНИЯ СВЕРХУ. ADR со статусом «предложено» —
нормальный способ начать обсуждение: вместо часового созвона
коллеги читают полторы страницы и отвечают комментариями в pull request.


═══ ПОЧЕМУ ЭТО ВООБЩЕ РАБОТАЕТ ═══

ПРИЧИНА 1. ПИСАТЬ ДЁШЕВО. Полторы страницы, двадцать минут.
Это не «внедрение процесса», это один файл.

ПРИЧИНА 2. НЕ ТРЕБУЕТ ПОДДЕРЖКИ. Написанное решение не устаревает:
оно и было решением на тот момент. Устареть может только сама система,
но тогда пишут НОВЫЙ ADR, а старый помечают заменённым.
Это единственный вид документации, который не гниёт по определению.

ПРИЧИНА 3. НАПИСАНИЕ САМО УЛУЧШАЕТ РЕШЕНИЕ. Половина ADR
не дописывается до конца, потому что на разделе «варианты»
автор понимает, что второй вариант лучше. Это не провал, это польза:
дешевле передумать в текстовом файле, чем в коде.

ПРИЧИНА 4. ЛЕЖИТ РЯДОМ С КОДОМ. Тот же репозиторий, то же ревью.
Не надо помнить пароль от портала и не надо искать, где это было.


═══ ЧЕСТНАЯ ЦЕНА ═══

Основной расход — дисциплина: в момент, когда решение принято,
всем хочется идти писать код, а не текст. Если не написать сразу,
не напишешь никогда: через неделю аргументы уже забылись.

Второй расход — легко скатиться в бюрократию. Команда, требующая ADR
на каждый чих, получает двести файлов, которые никто не читает.
Лечится простым порогом: пишем ADR, только если решение будет дорого
переиграть через год. Двадцать записей за год для проекта средних
размеров — это нормально. Двести — это болезнь.
`;

  protected readonly adrExample = `
# ═══ ПОЛНЫЙ ПРИМЕР ОДНОГО ADR ═══
#
# Это тот самый файл про сессии из предыдущего блока — написанный так,
# как надо было написать в сентябре. Ниже он целиком.
#
# Формат взят из MADR (Markdown Any Decision Records) — самого
# распространённого шаблона. Актуальная версия шаблона — 4.0.0.
# Шаблон есть в вариантах «полный» и «минимальный»; здесь показан
# полный, потому что решение дорогое.
#
# Команда cat записывает всё до строки MD в файл. Смотрите на содержимое.

cat > docs/adr/0007-store-sessions-in-postgres.md <<'MD'
---
status: accepted
date: 2025-09-18
decision-makers: [a.ivanov, m.petrova, s.kim]
consulted: [команда инфраструктуры]
informed: [команда мобильных приложений]
---

# 7. Хранить сессии пользователей в PostgreSQL, а не в Redis

## Контекст и постановка задачи

Нам нужно где-то хранить сессии — записи вида «этот токен принадлежит
этому пользователю и действителен до такого-то времени». Сейчас сессии
лежат в памяти процесса, поэтому при каждом выкате всех разлогинивает.
За август поддержка получила 340 обращений «меня выкинуло из кабинета».

Ограничения, в которых мы находимся:

- Нагрузка: около 900 проверок сессии в секунду в пике.
- Приложение живёт в двух зонах доступности, и переезд между зонами
  случается сам, без нашего участия, примерно раз в месяц.
- Бизнес прямо просит: человека не должно выкидывать из кабинета
  посреди оформления заказа, даже ценой замедления.
- В команде из четырёх человек нет никого с опытом эксплуатации Redis.

## Рассмотренные варианты

- Redis как хранилище сессий
- PostgreSQL, отдельная таблица sessions
- Подписанные токены JWT без хранилища
- Внешний сервис авторизации

## Решение

Храним сессии в PostgreSQL, в отдельной таблице \`sessions\`,
с индексом по токену и фоновой чисткой просроченных записей раз в час.

Причина выбора: это единственный вариант, который переживает переезд
между зонами без потери сессий и не требует новой для нас технологии
в эксплуатации. Замедление мы посчитали и признали приемлемым.

## Последствия

### Хорошие

- Сессии переживают и выкат, и переезд между зонами: данные лежат
  в базе, которая и так реплицируется и бэкапится.
- Никакой новой инфраструктуры: не надо учиться эксплуатировать Redis,
  настраивать его отказоустойчивость и мониторинг.
- Сессию видно обычным SQL-запросом. Поддержка сможет ответить
  на вопрос «почему меня разлогинило» без нас.
- Отзыв сессии мгновенный: удалили строку — токен больше не работает.

### Плохие, и мы на них сознательно идём

- Проверка сессии становится запросом в базу: +4..7 мс на каждый
  запрос пользователя. При 900 проверках в секунду это заметная
  дополнительная нагрузка на основную базу.
- Таблица растёт быстро: около 2 млн строк в месяц. Нужна фоновая
  чистка, и если она сломается, база распухнет незаметно.
- Мы кладём горячую нагрузку на ту же базу, где лежат заказы.
  Если база просядет, ляжет не только вход, но и всё остальное.

### Что мы сделаем, чтобы плохое не выстрелило

- Мониторинг времени проверки сессии, порог 15 мс (см. страницу про
  метрики в разделе «Наблюдаемость»).
- Отдельный алерт на размер таблицы sessions.
- Кеш в памяти процесса на 30 секунд поверх базы — снимает
  большую часть чтений и не мешает мгновенному отзыву при выходе.

## Почему отвергли остальные варианты

### Redis как хранилище сессий

Быстрее в разы и это самый популярный ответ в статьях. Но в нашей
конфигурации переезд между зонами теряет данные: реплика асинхронная,
и последние секунды записей пропадают. Мы это воспроизвели на стенде
12 сентября — при переезде разлогинило 100% активных пользователей.
Настраиваемая синхронная репликация Redis существует, но требует
компетенции, которой у команды нет, и отдельного дежурства.

Вернуться к варианту стоит, если появится выделенная команда
инфраструктуры или управляемый Redis с гарантией сохранности.

### Подписанные токены JWT без хранилища

Заманчиво: нулевая нагрузка, ничего не хранить. Отвергли из-за отзыва:
подписанный токен нельзя признать недействительным до истечения срока,
не заведя чёрный список — то есть всё то же хранилище, только сложнее.
Требование «нажал выйти на всех устройствах — вышло немедленно»
у нас есть, оно в требованиях безопасности.
Подробнее про этот компромисс — страница про сессии и JWT
в разделе «Безопасность».

### Внешний сервис авторизации

Снимает задачу целиком, но это отдельный контракт, отдельные деньги,
перенос всех существующих пользователей и зависимость от чужой
доступности. Слишком крупное изменение ради одной подзадачи.
Не отвергнуто навсегда: если мы будем делать вход через соцсети
и единый вход для сотрудников, вернёмся к этому вопросу целиком.

## Ссылки

- Замеры задержки на стенде: docs/research/2025-09-12-session-latency.md
- ADR-0003: почему основная база — PostgreSQL
- Задача в трекере: PLAT-882
MD

# ═══ РАЗБОР: ПОЧЕМУ ЭТОТ ФАЙЛ ХОРОШИЙ ═══
#
# 1. НОМЕР И ИМЯ. 0007-store-sessions-in-postgres.md — номер даёт порядок
#    и позволяет ссылаться («см. ADR-0007»), имя латиницей и словами
#    позволяет найти файл глазами в списке.
#
# 2. ЗАГОЛОВОК — ЭТО РЕШЕНИЕ, А НЕ ТЕМА. Не «Вопрос хранения сессий»,
#    а «Хранить сессии в PostgreSQL, а не в Redis». Читатель понимает
#    суть по одной строчке в списке файлов, не открывая.
#
# 3. КОНТЕКСТ С ЦИФРАМИ. 340 обращений, 900 проверок в секунду,
#    четыре человека в команде. Без цифр контекст превращается
#    в «нам показалось» и через год ничего не объясняет.
#
# 4. РАЗДЕЛ «ПЛОХИЕ ПОСЛЕДСТВИЯ» — САМЫЙ ЦЕННЫЙ. Именно он отличает
#    ADR от рекламной заметки. Если в вашем ADR плохих последствий нет,
#    вы либо не додумали, либо себя обманываете: у любого решения
#    есть цена. Читатель через год первым делом идёт именно сюда.
#
# 5. ОТВЕРГНУТЫЕ ВАРИАНТЫ С ПРИЧИНОЙ И С УСЛОВИЕМ ВОЗВРАТА.
#    «Вернуться стоит, если появится выделенная команда» — это подарок
#    будущему читателю: он сразу видит, изменились обстоятельства или нет.
#
# 6. ССЫЛКА НА ЗАМЕРЫ. «Мы это воспроизвели на стенде 12 сентября»
#    превращает мнение в факт. Спорить с фактом дороже, и это правильно.
#
# ═══ ЧЕГО В ХОРОШЕМ ADR НЕТ ═══
#
#   — нет пересказа документации Redis и PostgreSQL: читатель загуглит;
#   — нет кода: ADR про решение, а не про реализацию;
#   — нет обтекаемых формулировок «было принято решение» — написано,
#     КТО решил, это поле decision-makers;
#   — нет попытки выглядеть умно: язык простой, как в разговоре.
`;

  protected readonly adrWorkflow = `
# ═══ КАК ВЕСТИ ADR НА ПРАКТИКЕ ═══
#
# Правил всего четыре, и они простые. Три минуты чтения — и вы умеете.

# ═══ ПРАВИЛО 1. ФАЙЛЫ ЛЕЖАТ В РЕПОЗИТОРИИ РЯДОМ С КОДОМ ═══
#
# Не в вики, не в облачном документе, не в закреплённом сообщении чата.
# В той же папке, что и код, в том же pull request, под тем же ревью.
# Причина ровно та, с которой началась эта страница: расстояние
# до кода прямо влияет на вероятность, что документ прочитают и обновят.

tree docs/

#   docs/
#   ├── adr/
#   │   ├── README.md                                  # оглавление и правила
#   │   ├── 0000-template.md                           # шаблон для копирования
#   │   ├── 0001-use-postgresql-as-main-database.md
#   │   ├── 0002-monorepo-with-pnpm-workspaces.md
#   │   ├── 0003-rest-instead-of-graphql.md
#   │   ├── 0004-use-redis-for-sessions.md             # superseded by 0007
#   │   ├── 0005-conventional-commits.md
#   │   ├── 0006-drop-ie11-support.md
#   │   └── 0007-store-sessions-in-postgres.md
#   ├── architecture.md
#   └── runbook.md

# ═══ ПРАВИЛО 2. НУМЕРАЦИЯ СКВОЗНАЯ И НАВСЕГДА ═══
#
# Четыре цифры с ведущими нулями: 0001, 0002 — чтобы файлы сортировались
# по порядку и в терминале, и в интерфейсе репозитория.
# Номер, однажды выданный, не переиспользуется НИКОГДА, даже если
# решение отменили. Иначе ссылка «см. ADR-0004» начнёт указывать
# на другой документ, и это худший вид вранья — незаметный.
#
# Файлы не удаляют и не переименовывают. Отменённое решение остаётся
# лежать со статусом «заменено».

# ═══ ПРАВИЛО 3. СТАТУСЫ ═══
#
#   proposed   — предложено. Автор написал, команда обсуждает
#                прямо в комментариях pull request. Решение ещё не принято.
#   accepted   — принято. Pull request влит. Это действующее решение.
#   rejected   — отвергнуто. ВАЖНО: такой файл тоже вливают и оставляют.
#                «Мы обсуждали переход на GraphQL и отказались, вот почему» —
#                это ценно ровно так же, как принятое решение.
#   deprecated — устарело само по себе, замены не потребовалось.
#                Например, решение про поддержку старого браузера,
#                который больше не существует.
#   superseded by 0007 — заменено другим решением, с явной ссылкой.
#
# Статус — единственное поле, которое в старом файле МОЖНО поправить,
# и то только при замене.

# ═══ ПРАВИЛО 4. СТАРОЕ НЕ РЕДАКТИРУЕМ — ПИШЕМ НОВОЕ ВЗАМЕН ═══
#
# Это главное правило и самое неочевидное для новичка.
# Соблазн такой: решение изменилось, открою старый файл и перепишу.
# Так делать НЕЛЬЗЯ.
#
# ПОЧЕМУ. ADR — это не описание текущего состояния. Это запись о том,
# что было решено в конкретный момент и почему. Переписав старый файл,
# вы стираете причину прошлого выбора — а именно она и была ценностью.
# Через год кто-то снова предложит Redis, и в файле будет написано
# «мы выбрали Redis», без единого слова про переезд между зонами.
#
# КАК ПРАВИЛЬНО: пишем новый ADR, в нём ссылаемся на старый,
# в старом меняем ОДНУ строку — статус.

# ═══ ПРИМЕР ЗАМЕНЫ ОДНОГО РЕШЕНИЯ ДРУГИМ ═══

# Шаг 1. Заводим ветку — как для обычной задачи (см. страницу про Git).
git switch -c adr/0007-sessions-in-postgres

# Шаг 2. Копируем шаблон под новым номером.
cp docs/adr/0000-template.md docs/adr/0007-store-sessions-in-postgres.md

# Шаг 3. Пишем новый ADR. В нём обязательно есть строчка про предка:
#
#   ---
#   status: proposed
#   date: 2025-09-18
#   ---
#
#   # 7. Хранить сессии пользователей в PostgreSQL, а не в Redis
#
#   Заменяет [ADR-0004](0004-use-redis-for-sessions.md).

# Шаг 4. В СТАРОМ файле меняем ровно одну строку — статус.
#   Было:
#     status: accepted
#   Стало:
#     status: superseded by [ADR-0007](0007-store-sessions-in-postgres.md)
#
#   Больше в старом файле не трогаем НИЧЕГО. Ни контекст, ни последствия.
#   Пусть остаётся памятником: вот как мы думали в марте.

# Шаг 5. Отправляем на обсуждение вместе с кодом, если код уже есть.
git add docs/adr/
git commit -m "docs(adr): 0007 sessions in postgres, supersedes 0004"
git push -u origin adr/0007-sessions-in-postgres

# Шаг 6. Обсуждение идёт комментариями в pull request. Это лучше созвона:
#   аргументы остаются написанными и попадают в итоговый файл.
#   Когда договорились — меняем status на accepted и вливаем.

# ═══ ЧТО ПОЛОЖИТЬ В docs/adr/README.md ═══
#
#   # Записи архитектурных решений
#
#   Пишем ADR, если решение будет дорого переиграть через год:
#   выбор хранилища, протокола, способа авторизации, отказ
#   от библиотеки, крупная смена подхода.
#   НЕ пишем ADR на выбор имени переменной или библиотеки для дат.
#
#   Как добавить: скопировать 0000-template.md под следующим номером,
#   заполнить, отправить pull request со статусом proposed.
#
#   Старые записи не редактируем: пишем новую и меняем статус старой.
#
#   | №    | Решение                          | Статус             |
#   |------|----------------------------------|--------------------|
#   | 0004 | Сессии в Redis                   | заменено на 0007   |
#   | 0007 | Сессии в PostgreSQL              | принято            |

# ═══ ПРО ИНСТРУМЕНТЫ: ЧЕСТНО ═══
#
# Для ADR инструменты почти не нужны — это markdown-файлы и git.
# Из того, что есть, стоит знать три вещи.
#
#   MADR — не программа, а ШАБЛОН. Самый распространённый.
#     Актуальная версия 4.0.0 (сентябрь 2024), с тех пор релизов не было,
#     и это нормально: шаблон и не должен меняться каждый месяц.
#     Даёт варианты «полный» и «минимальный», плюс поля в заголовке
#     файла: статус, дата, кто решал, с кем советовались, кого известили.
#
#   adr-tools (скрипты на bash от npryce) — ЗАБРОШЕН, последний коммит
#     в марте 2020. Во многих старых статьях его советуют; не берите.
#
#   log4brains (сайт из ADR) — стагнирует, последний коммит
#     в декабре 2024. Работает, но развития нет.
#
# ПРАКТИЧЕСКИЙ ВЫВОД: возьмите шаблон MADR, положите файлы в docs/adr,
# добавьте оглавление руками. Инструмент вам не нужен —
# и это тот редкий случай, когда отсутствие инструмента является плюсом:
# ломаться нечему.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
#
# Оглавление в README придётся вести руками, и оно будет отставать.
# Лечится либо простым скриптом, который пересобирает таблицу из файлов,
# либо смирением: список файлов в папке и так отсортирован по номеру.
#
# И второе: первые два-три ADR писать неловко — кажется, что это
# бюрократия ради бюрократии. Ощущение проходит ровно в тот день,
# когда вы сами открываете чужой ADR годичной давности и находите
# ответ на вопрос, который собирались задать в чате.
`;

  protected readonly diagramsAsCode = `
═══ ДИАГРАММЫ, КОТОРЫЕ НЕ ГНИЮТ ═══

Схема — самый быстрый способ объяснить устройство системы. Один рисунок
экономит три страницы текста. Проблема не в схемах, а в том, ГДЕ они живут.


═══ КАК ОБЫЧНО ДЕЛАЮТ ═══

Кто-то открывает онлайн-сервис для рисования, тратит час, получает
красивую схему, делает снимок экрана и вставляет картинку в вики.
Иногда рядом кладёт ссылку на редактируемый оригинал.


═══ ЧТО СЛУЧИТСЯ ═══

БЕДА 1. ССЫЛКА НА ОРИГИНАЛ ОДНАЖДЫ ПЕРЕСТАНЕТ ОТКРЫВАТЬСЯ.
  Автор ушёл из компании, и схема лежала на его личном аккаунте.
  Или сервис сменил тариф, и бесплатных документов теперь три.
  Или сервис закрылся. Или ссылка просто потерялась.
  Остаётся картинка, которую нельзя изменить — только перерисовать с нуля.

БЕДА 2. КАРТИНКУ НЕЛЬЗЯ ПОСМОТРЕТЬ В ИСТОРИИ ИЗМЕНЕНИЙ.
  Git видит картинку как набор байтов. Он не покажет, что именно
  поменялось между версиями, и не поможет слить две правки.
  Двое поправили схему параллельно — один из них перерисовывает заново.

БЕДА 3. ПРАВИТЬ ДОРОГО, ПОЭТОМУ НЕ ПРАВЯТ.
  Добавить один блок — значит открыть редактор, вспомнить, где что,
  подвинуть стрелки, снова снять экран, снова вставить. Пятнадцать минут
  вместо одной. Поэтому схему не правят, и через год она врёт.

БЕДА 4. У КАЖДОГО СВОЙ СЕРВИС.
  Одна схема нарисована в одном редакторе, вторая в другом,
  третья вообще в презентации. Единого места нет.


═══ КАК НАДО: СХЕМА — ЭТО ТЕКСТ ═══

Диаграмма описывается несколькими строчками текста, файл лежит
в репозитории, а картинка рисуется автоматически при показе.
Что меняется:

  — правка схемы — это правка текста, минута;
  — история изменений читается построчно, как код;
  — конфликты сливаются, как в обычном файле;
  — ревьюер видит в pull request, что именно поменялось;
  — ссылке некуда протухнуть: схема в том же репозитории, что и код.


═══ ПРИМЕР: MERMAID ═══

Mermaid — самый практичный вариант для начала. Лицензия MIT, живой.
Главное его преимущество не в возможностях, а в том, ГДЕ он рисуется сам:
GitHub показывает такие схемы прямо в markdown-файлах (с февраля 2022),
GitLab тоже, а из редакторов — Notion и Obsidian.
То есть вы кладёте текст в README, и в браузере видна картинка.
Ничего устанавливать не нужно.

Вот схема, целиком, в файле docs/architecture.md:

\`\`\`mermaid
flowchart LR
    User[Пользователь] --> Web[Веб-приложение]
    Web --> API[API-шлюз]
    API --> Orders[Сервис заказов]
    API --> Notify[Сервис уведомлений]
    Orders --> DB[(PostgreSQL)]
    Notify --> Queue[(Redis очередь)]
    Notify --> Mail[Внешний почтовый провайдер]
\`\`\`

Читается построчно, как обычный текст:
  flowchart LR — блок-схема, слева направо (LR = left to right);
  A --> B — стрелка от A к B;
  [текст] — прямоугольник, [(текст)] — цилиндр (так принято рисовать
  хранилища данных), {текст} — ромб (условие).

Добавить новый сервис — дописать одну строчку. Переименовать —
поправить слово. В pull request будет видно ровно одну изменённую строку.

Что ещё умеет Mermaid, кроме блок-схем: диаграммы последовательности
(кто кому и в каком порядке шлёт запросы), диаграммы состояний,
диаграммы сущностей базы данных, диаграммы классов, диаграммы Ганта,
графы веток git и несколько видов графиков.


═══ ЧЕМ ЕЩЁ РИСУЮТ, ЕСЛИ MERMAID МАЛО ═══

PlantUML. Живой, релизы регулярные. Java, максимальный охват типов UML.
  Берут, когда нужны строгие UML-диаграммы, которых нет в Mermaid.
  Цена: нужна Java и отдельный шаг рендеринга — сам собой на GitHub
  он не покажется.

D2. Живой, очень активный, лицензия MPL-2.0. Красивая раскладка,
  приятный язык. Сам инструмент и командная строка бесплатны;
  платные — визуальная студия и продвинутый движок раскладки.

Structurizr. Живой, от Саймона Брауна, автора модели C4 (о ней ниже).
  Особенность: вы описываете модель системы один раз, а из неё
  получаете несколько разных диаграмм — по одной на каждый уровень.
  Есть облачный сервис и вариант для локального запуска;
  что именно бесплатно, лучше уточнять на сайте — условия меняются.

ПРАКТИЧЕСКИЙ СОВЕТ. Начинайте с Mermaid, потому что он рисуется сам
там, где вы и так работаете. К остальным переходите, только когда
упрётесь во что-то конкретное.


═══ МОДЕЛЬ C4: КАК ОПИСЫВАТЬ АРХИТЕКТУРУ ═══

Даже с текстовыми схемами остаётся вопрос: ЧТО рисовать. Типичная
ошибка — одна огромная схема, на которой одновременно пользователи,
сервисы, таблицы базы и очереди. Такая схема нечитаема: она отвечает
сразу на все вопросы и поэтому ни на один.

Модель C4 (её придумал и продвигает Саймон Браун, сайт c4model.com,
книга вышла в O'Reilly в июле 2026) предлагает простую идею:
не рисуйте одну схему — рисуйте несколько, с разным приближением.
Как в картах: сначала страна, потом город, потом улица, потом дом.
Каждый уровень отвечает на свой вопрос и своему читателю.

Верхний уровень — самый общий: ваша система как один прямоугольник,
вокруг неё люди и внешние системы, с которыми она общается. Такую схему
понимает менеджер и новый человек в первый день. Следующий уровень
раскрывает систему изнутри: из каких запускаемых частей она состоит —
веб-приложение, API, воркер, база, очередь. Дальше можно спуститься
внутрь одной такой части и показать её крупные внутренние блоки,
а на самом нижнем уровне — уже конкретные классы и файлы.

Практическая рекомендация для обычного проекта: рисуйте первые два
уровня и на этом остановитесь. Они меняются редко, а пользы дают больше
всего. Самый нижний уровень почти всегда не окупается: он устаревает
за неделю, а ту же информацию читатель получит из самого кода
и из подсказок редактора.


═══ ГДЕ ХРАНИТЬ СХЕМЫ ═══

  docs/
  ├── architecture.md        # текст плюс схемы mermaid прямо в нём
  └── diagrams/
      ├── context.mmd        # верхний уровень: система и её окружение
      └── containers.mmd     # из каких частей состоит система

Файлы .mmd можно держать отдельно и вставлять в документы, а можно
писать схему прямо в markdown-файле рядом с абзацем, который она
поясняет. Для двух-трёх схем второй вариант проще.


═══ ЧЕСТНАЯ ЦЕНА ═══

Текстовые схемы менее красивы, чем нарисованные вручную. Раскладка
делается автоматически, и иногда стрелка идёт не там, где вам хотелось
бы, а блоки становятся не в том порядке. Управлять этим можно, но
ограниченно, и повоевать с раскладкой придётся.

Это плохой размен ровно один раз — когда схему рисуют для презентации
заказчику. Для всех остальных случаев он хороший: схема, которая
некрасива, но правдива, полезнее красивой и устаревшей.
`;

  protected readonly docsAsCode = `
# ═══ ДОКУМЕНТАЦИЯ КАК КОД ═══
#
# Мы обошли все инструменты. Осталось собрать их в один подход,
# у которого есть название: docs as code — «документация как код».
#
# Идея в одном предложении: обращаться с документацией ровно так же,
# как с кодом, — теми же инструментами, по тому же процессу.
#
# ЧТО ЭТО ЗНАЧИТ НА ПРАКТИКЕ. Документация:
#   — лежит в том же репозитории, что и код;
#   — правится в ветке и попадает в тот же pull request;
#   — проходит ревью вместе с изменением, которое её вызвало;
#   — проверяется линтером и автоматическими проверками;
#   — собирается в сайт и выкладывается автоматически при вливании.
#
# Про ветки, pull request и ревью — страница про Git.
# Про то, как устроен пайплайн, — страница про CI/CD в «Инфраструктуре».

# ═══ ГДЕ ЧТО ЛЕЖИТ ═══
#
#   repo/
#   ├── README.md                 входная дверь
#   ├── CONTRIBUTING.md           как вносить изменения
#   ├── openapi.yaml              описание API (или генерируется на сборке)
#   ├── docs/
#   │   ├── architecture.md       карта системы плюс схемы
#   │   ├── deploy.md             как выкатывать
#   │   ├── runbook.md            что делать, когда всё легло ночью
#   │   ├── adr/                  решения и почему они такие
#   │   └── diagrams/             схемы текстом
#   └── src/
#       └── ui/Button/
#           ├── Button.tsx
#           └── Button.stories.tsx   документация компонента рядом с ним
#
# ГЛАВНЫЙ ПРИНЦИП РАЗМЕЩЕНИЯ: чем ближе документ к тому, что он
# описывает, тем больше шансов, что его обновят.
# Документация компонента — в его папке. Описание API — в корне сервиса.
# Общая карта — в docs. Ничего на отдельном портале.

# ═══ ПРАВИЛО ОДНОГО PULL REQUEST ═══
#
# Это главная привычка всего подхода, и она стоит дороже любого инструмента.
#
#   Меняешь поведение — меняешь его описание в ТОМ ЖЕ наборе изменений.
#
# Не «потом заведу задачу на документацию». Задача на документацию —
# это способ никогда её не написать: она проиграет любой другой задаче
# в очереди, потому что за неё никто не спросит.
#
# Что это даёт ревьюеру: он видит рядом и код, и текст, и может сказать
# «в описании написано, что поле обязательное, а в коде проверки нет».
# Такие расхождения ловятся только тогда, когда обе части на одном экране.

# ═══ КОГДА НУЖЕН ОТДЕЛЬНЫЙ САЙТ ДОКУМЕНТАЦИИ ═══
#
# Не всегда. Markdown-файлы в репозитории прекрасно читаются прямо
# в интерфейсе GitHub или GitLab: заголовки, ссылки, схемы mermaid —
# всё показывается. Для внутреннего сервиса этого обычно достаточно.
#
# Сайт нужен, когда:
#   — документацию читают люди без доступа к репозиторию;
#   — нужен полнотекстовый поиск по всем документам;
#   — документации много и нужна навигация;
#   — нужны версии документации под версии продукта.
#
# ЧЕМ СОБИРАТЬ (август 2026, все живые):
#
#   Docusaurus (Meta, на React) — самый распространённый, MIT.
#     Стабильная линия 3.x. Умеет версии документации и переводы.
#     Берут, когда документации много и она внешняя.
#
#   VitePress (на Vue) — MIT, лёгкий и очень быстрый.
#     Стабильная линия 1.x, в тестовой ветке готовится вторая.
#     Берут, когда нужен простой быстрый сайт без лишнего.
#
#   Starlight (на Astro) — MIT, быстро растёт.
#     Приятный вид «из коробки», хороший поиск.
#
#   Nextra (на Next.js) — MIT. Логичный выбор, если продукт уже на Next.
#
#   MkDocs Material (на Python) — MIT, очень зрелый и популярный.
#     Естественный выбор, если проект питоновский. Есть платный тариф
#     Insiders с дополнительными возможностями, базовый бесплатен.
#
#   Mintlify — закрытый сервис, не открытый код. С июля 2026 два тарифа:
#     бесплатный Starter (со своим доменом и поиском с ИИ)
#     и корпоративный. Быстро и красиво, но вы зависите от чужого сервиса.
#
# ПРЕДУПРЕЖДЕНИЕ ИЗ СВЕЖЕЙ ИСТОРИИ. В начале 2026 года был архивирован
# Slate — известный генератор документации API с 36 тысячами звёзд.
# Архивированный проект — это только чтение, без исправлений.
# Мораль: проверяйте дату последнего коммита у любого инструмента,
# который советует статья. Даже у очень популярного.

# ═══ ПРИМЕР: СБОРКА САЙТА ДОКУМЕНТАЦИИ В ПАЙПЛАЙНЕ ═══
#
# Файл .github/workflows/docs.yml. Читается сверху вниз.

name: docs

on:
  push:
    branches: [main]
    # ^ Собираем сайт только при вливании в основную ветку.
  pull_request:
    # ^ А проверки гоняем на каждый pull request, чтобы поймать
    #   поломку до вливания, а не после.

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        # ^ Скачать репозиторий на машину сборки.

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci
        # ^ Установить зависимости строго по файлу блокировки.

      - run: npm run docs:lint
        # ^ Линтер текста: битые ссылки внутри документов, неправильные
        #   заголовки, запрещённые формулировки. Подробнее — следующий блок.

      - run: npm run docs:build
        # ^ Собрать сайт в папку build.

      - name: Опубликовать
        if: github.ref == 'refs/heads/main'
        # ^ Выкладываем только из основной ветки. На pull request
        #   шаг пропускается: проверили — и достаточно.
        run: npm run docs:deploy

# ═══ ЧЕСТНАЯ ЦЕНА ═══
#
# РАСХОД 1. PULL REQUEST СТАНОВЯТСЯ ТОЛЩЕ, РЕВЬЮ ДОЛЬШЕ.
#   Это правда, и с этим ничего не сделать. Плата за то,
#   что документация вообще существует в живом виде.
#
# РАСХОД 2. ЕЩЁ ОДНА СБОРКА, КОТОРАЯ УМЕЕТ ЛОМАТЬСЯ.
#   Сайт документации падает, и вы чините его вместо работы.
#   Смягчается тем, что сборка сайта простая и падает редко.
#
# РАСХОД 3. НЕ ВСЕ УМЕЮТ В GIT.
#   Если документацию пишут не только разработчики, требование
#   «правь через pull request» может оказаться барьером.
#   Частично лечится веб-редактором прямо в интерфейсе GitHub:
#   там можно поправить файл и создать pull request без командной строки.
#
# ЧТО ВЗАМЕН. Документация меняется вместе с кодом и в том же ревью —
# единственный известный способ не дать ей соврать.
`;

  protected readonly docsAutomation = `
# ═══ ЧТО АВТОМАТИЗИРОВАТЬ, ЧТОБЫ ДОКУМЕНТАЦИЯ НЕ ВРАЛА ═══
#
# Вернёмся к самому началу страницы. Документация гниёт потому,
# что её никто не обязан обновлять. Обязать может только то,
# что не забывает и не устаёт, — проверка в пайплайне.
#
# Проверок ровно три, и они закрывают три разных вида вранья.
#
#   1. БИТЫЕ ССЫЛКИ    — «тут была страница, а теперь её нет»
#   2. ПРИМЕРЫ КОДА    — «скопировал из документации, не работает»
#   3. СПЕЦИФИКАЦИЯ    — «написано одно, сервер отвечает другое»
#
# Ниже — каждая по отдельности и общий пример пайплайна.

# ═══ ПРОВЕРКА 1. БИТЫЕ ССЫЛКИ ═══
#
# Ссылки гниют быстрее всего: файл переименовали, раздел удалили,
# внешний сайт переехал. Читатель кликает и упирается в 404 —
# и сразу перестаёт верить всей странице.
#
# Проверяются два вида ссылок:
#   — внутренние: на другие файлы репозитория. Ломаются постоянно,
#     при любом переименовании. Проверять обязательно.
#   — внешние: на чужие сайты. Ломаются реже, но проверка бывает
#     нестабильной — сайт может просто не ответить в момент сборки.
#
# ПРАКТИЧЕСКОЕ ПРАВИЛО: внутренние ссылки — ошибка, сборка красная.
# Внешние — отдельная задача по расписанию раз в неделю, с уведомлением,
# но БЕЗ покраснения обычной сборки. Иначе чужой упавший сайт
# будет блокировать вам вливание, и проверку скоро отключат.

# ═══ ПРОВЕРКА 2. ПРИМЕРЫ КОДА КОМПИЛИРУЮТСЯ ═══
#
# Самое обидное враньё: человек копирует пример из документации,
# вставляет и получает ошибку. Доверие теряется мгновенно
# и целиком — раз соврал пример, соврёт и текст.
#
# Причина всегда одна: пример писали руками год назад, а библиотека
# с тех пор переименовала функцию.
#
# ТРИ СПОСОБА, ОТ ПРОСТОГО К НАДЁЖНОМУ:
#
#   СПОСОБ А. Примеры лежат отдельными файлами в examples/,
#     а в документ вставляются ссылкой или включением. Файлы
#     компилируются обычной проверкой типов вместе со всем проектом.
#     Ломается — сборка красная. Просто и работает.
#
#   СПОСОБ Б. Примеры — это тесты. Файл examples/basic-usage.test.ts
#     одновременно и пример из документации, и настоящий тест.
#     Сильнее способа А: проверяется не только что компилируется,
#     но и что РАБОТАЕТ. Про тесты — отдельная страница подраздела.
#
#   СПОСОБ В. Извлекать куски кода из markdown и компилировать их.
#     Надёжнее всего, но требует отдельного инструмента и настройки.
#     Для начала избыточно.
#
# ОТДЕЛЬНО ПРО СПРАВОЧНИК БИБЛИОТЕКИ. Если вы делаете библиотеку,
# справочник по её функциям писать руками не надо совсем: TypeDoc
# читает ваши типы и комментарии JSDoc и собирает сайт сам.
# Лицензия Apache-2.0, релизы регулярные, для библиотек на TypeScript
# это фактический стандарт. Сам синтаксис JSDoc-комментариев понимают
# и TypeScript, и редакторы кода, так что комментарии окупаются
# ещё до всякой генерации — подсказками прямо при наборе.

# ═══ ПРОВЕРКА 3. СПЕЦИФИКАЦИЯ СОВПАДАЕТ С ОТВЕТАМИ СЕРВЕРА ═══
#
# Самая важная и чаще всего пропускаемая. Без неё openapi.yaml —
# просто ещё один текст, который может разойтись с реальностью.
#
# ПОДХОД, КОТОРЫЙ РАБОТАЕТ ВЕЗДЕ И НЕ ТРЕБУЕТ НОВЫХ ИНСТРУМЕНТОВ:
# добавить сверку в те сквозные тесты, которые у вас уже есть.
#
#   1) тест поднимает сервис и делает настоящий запрос;
#   2) берём из openapi.yaml схему ответа для этой ручки;
#   3) прогоняем полученный ответ через валидатор JSON Schema
#      (в 3.1 схемы — это ровно JSON Schema 2020-12, так что подойдёт
#      любая обычная библиотека валидации);
#   4) не совпало — тест красный, с указанием конкретного поля.
#
# Что это ловит: добавили поле в ответ и забыли описать; поменяли тип
# с числа на строку; убрали поле, которое обещали.
#
# ЕСЛИ ВЫ РАБОТАЕТЕ CODE-FIRST, часть этой проверки у вас уже есть:
# схема, из которой генерируется спецификация, и есть схема, которой
# сервер проверяет запрос. Остаётся сверить ОТВЕТЫ — их обычно
# не валидируют на ходу, чтобы не тратить время на каждом запросе.

# ═══ ПОЛНЫЙ ПРИМЕР ШАГА ПАЙПЛАЙНА ═══

name: docs-checks

on: [pull_request]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # ^ Полная история нужна для сравнения с основной веткой
          #   на последнем шаге. Без этого oasdiff не с чем сравнивать.

      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci

      # ── Ссылки внутри репозитория ──
      - name: Битые внутренние ссылки
        run: npx markdown-link-check --config .mlc.json 'docs/**/*.md' README.md
        # ^ В конфиге .mlc.json внешние адреса вынесены в исключения:
        #   их проверяет отдельная задача по расписанию.

      # ── Примеры кода компилируются ──
      - name: Примеры из документации собираются
        run: npx tsc --noEmit --project examples/tsconfig.json
        # ^ Отдельный проект только для примеров. Переименовали функцию
        #   в библиотеке — пример перестал собираться — сборка красная.

      # ── Спецификация валидна и написана по правилам ──
      - name: Линт спецификации
        run: npx spectral lint openapi.yaml --fail-severity=error

      # ── Спецификация не ломает существующих клиентов ──
      - name: Ломающие изменения в API
        run: |
          git show origin/main:openapi.yaml > /tmp/openapi-main.yaml
          npx @oasdiff/cli breaking /tmp/openapi-main.yaml openapi.yaml
        # ^ Достаём старую версию файла прямо из git и сравниваем.
        #   Нашлось ломающее изменение — проверка красная,
        #   и его придётся обсудить в комментариях, а не протащить молча.

      # ── Ответы сервера совпадают со спецификацией ──
      - name: Сквозные тесты со сверкой по схеме
        run: npm run test:e2e
        env:
          VALIDATE_RESPONSES_AGAINST_SPEC: 'true'
          # ^ Флаг включает сверку ответов со схемой только в тестах.
          #   В проде это выключено: лишняя работа на каждом запросе.

# ═══ ПОРЯДОК ВНЕДРЕНИЯ ═══
#
# Не включайте всё сразу — получите пятьдесят ошибок и выключите обратно.
#
#   Неделя 1. Битые внутренние ссылки. Дёшево, чинится за час,
#             срабатывает часто и по делу.
#   Неделя 2. Линт спецификации, но пока с severity: warn.
#             Смотрим, сколько всего вылезло, чиним постепенно.
#   Неделя 3. Примеры кода компилируются.
#   Неделя 4. Проверка ломающих изменений.
#   Позже.    Сверка ответов со схемой в сквозных тестах.
#
# Тот же принцип, что и при внедрении линтера кода: сначала показать,
# потом требовать. Подробнее — на странице про линтеры.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
#
# Каждая проверка — это минуты к времени сборки и иногда ложные
# срабатывания. Внешняя ссылка не ответила — красная сборка на ровном
# месте. Пример перестал компилироваться из-за обновления библиотеки —
# чините пример, а не задачу.
#
# Но выбор тут не между «с проверками» и «без проверок».
# Выбор между «проверка нашла ошибку в pull request» и «ошибку нашёл
# читатель, который скопировал ваш пример и потерял на нём час».
`;

  protected readonly dayOneDocs = `
# ═══ ЧТО ЗАВЕСТИ В ПЕРВЫЙ ДЕНЬ ПРОЕКТА ═══
#
# Вся эта страница может напугать объёмом: OpenAPI, Storybook, ADR,
# диаграммы, три проверки в пайплайне. Кажется, что на это нужна неделя.
#
# Не нужна. Минимальный набор ставится за ПОЛЧАСА, и его достаточно
# на первые месяцы. Всё остальное добавляется по мере надобности —
# и, скорее всего, половина вам никогда не понадобится.
#
# Ниже — ровно то, что делают в первый день. По шагам.

# ═══ ПОЛЧАСА В ПЕРВЫЙ ДЕНЬ ═══

# Шаг 1 (5 минут). Создать структуру.
mkdir -p docs/adr
touch README.md
touch docs/adr/README.md
cp ~/templates/madr-minimal.md docs/adr/0000-template.md
# ^ Шаблон MADR можно просто скопировать с сайта adr.github.io/madr
#   и положить рядом. Никаких инструментов ставить не надо.

# Шаг 2 (15 минут). Написать README по семи разделам из блока выше:
#   что это → зачем → что нужно → как запустить → как тесты →
#   как устроено в один абзац → куда идти дальше.
#
#   Обязательно ПРОВЕРИТЬ команды запуска на чистой машине или хотя бы
#   в свежей папке. README, который не проверяли, врёт с первого дня.

# Шаг 3 (5 минут). Написать первый ADR — про выбор стека.
#   Он у вас уже принят, просто ещё не записан. Заголовок вроде
#   «1. Пишем сервис на Node.js и Fastify». Контекст, решение,
#   плохие последствия, отвергнутые варианты. Полторы страницы.
#
#   ЗАЧЕМ ИМЕННО ПЕРВЫМ ДНЁМ: пока аргументы свежие. Через месяц
#   вы будете помнить только вывод, а ценность в аргументах.

# Шаг 4 (5 минут). Одна схема mermaid прямо в README —
#   верхний уровень C4: система, пользователи, внешние сервисы.
#   Пять-семь строк текста. Она сразу видна в интерфейсе репозитория.

# ВСЁ. Полчаса потрачено, минимальный набор есть.

git add README.md docs/
git commit -m "docs: README, первый ADR и схема системы"

# ═══ ЧТО ДОБАВЛЯТЬ ПОТОМ И ПО КАКОМУ ПОВОДУ ═══
#
# Правило одно: инструмент заводится, когда появилась боль,
# а не «на всякий случай». Заведённое заранее не приживается.
#
#   ПОЯВИЛСЯ ВТОРОЙ ПОТРЕБИТЕЛЬ У ВАШЕГО API
#     → openapi.yaml и сайт документации с песочницей.
#       Начните с code-first: спецификация выведется из схем,
#       которыми сервис уже проверяет запросы.
#
#   НАБРАЛОСЬ ПРИМЕРНО ПЯТНАДЦАТЬ СВОИХ КОМПОНЕНТОВ
#     → Storybook. Раньше — почти наверняка не окупится.
#
#   ПОЯВИЛСЯ ВТОРОЙ СЕРВИС ИЛИ ВТОРАЯ КОМАНДА
#     → docs/architecture.md со схемами двух уровней C4.
#
#   КТО-ТО СЛОМАЛ ЧУЖОЕ ПРИЛОЖЕНИЕ ИЗМЕНЕНИЕМ API
#     → проверка ломающих изменений в пайплайне (oasdiff).
#       Обычно это заводят ровно на следующий день после происшествия.
#
#   ССЫЛКИ В ДОКУМЕНТАХ НАЧАЛИ БИТЬСЯ
#     → проверка ссылок в пайплайне.
#
#   ДОКУМЕНТАЦИЮ ЧИТАЮТ ЛЮДИ БЕЗ ДОСТУПА К РЕПОЗИТОРИЮ
#     → отдельный сайт документации (Docusaurus, VitePress,
#       Starlight, Nextra, MkDocs Material — по вашему стеку).
#
#   ВЫ ДЕЛАЕТЕ БИБЛИОТЕКУ, КОТОРУЮ СТАВЯТ ДРУГИЕ
#     → TypeDoc: справочник соберётся из ваших типов и комментариев.

# ═══ ТРИ ПРИВЫЧКИ, КОТОРЫЕ ДОРОЖЕ ЛЮБОГО ИНСТРУМЕНТА ═══
#
# ПРИВЫЧКА 1. НОВИЧОК ПРАВИТ README В ПЕРВЫЙ ДЕНЬ.
#   Он проходит инструкцию дословно и чинит первое же место, где она
#   соврала. Это его первый pull request — маленький и безопасный.
#   Лучшего момента не будет: через неделю у него появится знание
#   «в голове» и он перестанет замечать пробелы.
#
# ПРИВЫЧКА 2. МЕНЯЕШЬ ПОВЕДЕНИЕ — МЕНЯЕШЬ ОПИСАНИЕ В ТОМ ЖЕ PULL REQUEST.
#   Не «потом заведу задачу». Задача на документацию не будет сделана
#   никогда: она проиграет в приоритете всему остальному.
#
# ПРИВЫЧКА 3. ДОРОГОЕ РЕШЕНИЕ — СРАЗУ ADR, В ТОТ ЖЕ ДЕНЬ.
#   Двадцать минут, пока помните аргументы. Через неделю вы будете
#   помнить только вывод, а вывод без аргументов бесполезен:
#   спорить с ним нельзя, проверить его нельзя.

# ═══ ЧЕГО НЕ ДЕЛАТЬ В ПЕРВЫЙ ДЕНЬ ═══
#
#   — не заводить вики: она отдельно от кода и сгниёт первой;
#   — не ставить Storybook на три компонента;
#   — не писать спецификацию на API, у которого один потребитель
#     и он же автор;
#   — не поднимать сайт документации, пока документов пять штук;
#   — не писать ADR на каждое мелкое решение: порог — «дорого
#     переиграть через год».
#
# ЧТО ЗАПОМНИТЬ. Полчаса в первый день дают README, один ADR и одну
# схему. Этого хватает надолго. Всё остальное — по боли, а не по списку.
`;
}
