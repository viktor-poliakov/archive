import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-intro-glossary',
  imports: [CodeBlock, RouterLink],
  templateUrl: './glossary.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemIntroGlossary {
  protected readonly semver = `// Номер версии пакета — это не одно число, а ТРИ,
// и каждое означает свой уровень изменений.

    5  .  4  .  2
    │     │     │
    │     │     └─ ЗАПЛАТКА (patch): починили ошибку.
    │     │        Ничего не сломается. Обновляйтесь смело.
    │     │
    │     └─ ФУНКЦИЯ (minor): добавили что-то новое.
    │        Старое продолжает работать. Обновляться безопасно.
    │
    └─ ЛОМАЮЩЕЕ (major): что-то убрали или переделали.
       Ваш код может перестать работать. Читайте, что изменилось.

// Правило: цифра слева важнее всех, что справа.
// Переход 5.4.2 → 5.9.0 безопасен. Переход 5.4.2 → 6.0.0 — нет.`;

  protected readonly semverRanges = `// В package.json рядом с версией стоят значки. Они означают
// «какие обновления разрешено ставить автоматически».

"dayjs": "^1.11.0"   // ^ — можно всё, пока не сменилась ПЕРВАЯ цифра
                     //     подойдёт 1.11.5, 1.20.0
                     //     НЕ подойдёт 2.0.0
                     //     это значение по умолчанию, и оно разумное

"dayjs": "~1.11.0"   // ~ — можно менять только ПОСЛЕДНЮЮ цифру
                     //     подойдёт 1.11.9
                     //     НЕ подойдёт 1.12.0
                     //     осторожнее, чем ^

"dayjs": "1.11.0"    // без значка — ровно эта версия и никакая другая
                     //     максимальный контроль, но обновлять руками

// «А если у меня и у коллеги поставятся разные версии?»
// Для этого есть файл package-lock.json — он запоминает ТОЧНЫЕ
// версии всего, что установилось. Его коммитят в репозиторий,
// и тогда у всех в команде и на сервере ставится одно и то же.`;

  protected readonly chatRaw = `Привет! Смёржил твой ПР в дев, но на стейдже отвалилось.
Похоже, регрессия из-за легаси в модуле оплаты — там костыль
с прошлого года. Накатил хотфикс, задеплоил в прод, откатывать
не пришлось. Заведи тикет на рефакторинг, добавь в бэклог.
И линтер ругается, поправь перед следующим ревью.`;

  protected readonly chatDecoded = `// То же самое сообщение, но по-русски:

"Смёржил твой ПР в дев"
  → Я принял твой набор правок (pull request) и влил его
    в основную ветку разработки.

"на стейдже отвалилось"
  → На тестовом сервере (копия боевого, для проверки) сломалось.

"регрессия"
  → Сломалось то, что раньше работало. Самый обидный вид поломки.

"легаси в модуле оплаты"
  → Старый код, который никто не хочет трогать, но он работает.

"костыль"
  → Некрасивое решение, поставленное «на время». Как обычно, навсегда.

"накатил хотфикс, задеплоил в прод"
  → Быстро починил и выложил исправление на боевой сервер —
    туда, где сидят настоящие пользователи.

"откатывать не пришлось"
  → Не понадобилось возвращать предыдущую версию. Хорошая новость.

"заведи тикет, добавь в бэклог"
  → Создай задачу в трекере и положи в список «сделать когда-нибудь».

"рефакторинг"
  → Переписать код так, чтобы он стал понятнее,
    но снаружи работал ТОЧНО ТАК ЖЕ.

"линтер ругается"
  → Программа проверки стиля нашла в коде замечания.

"перед следующим ревью"
  → До того, как коллега снова будет читать твой код.`;
}
