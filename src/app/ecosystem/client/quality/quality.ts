import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-client-quality',
  imports: [CodeBlock, RouterLink],
  templateUrl: './quality.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemClientQuality {
  protected readonly buttonBad = `<!-- ❌ ТАК ДЕЛАТЬ НЕЛЬЗЯ -->

<!-- Внешне это неотличимо от кнопки: серый прямоугольник,
     курсор-палец, при наведении темнеет. Дизайнер доволен.
     Но для браузера это просто прямоугольник с текстом внутри —
     он не знает, что вы имели в виду кнопку. -->
<div class="button" onclick="pay()">Оплатить</div>

<!-- ЧТО СЛОМАЛОСЬ (и человек с мышкой этого не заметит):
     1. Нельзя добраться клавишей Tab — этого блока нет в обходе.
     2. Не срабатывает Enter и пробел — их обрабатывает браузер
        только для настоящих кнопок и ссылок.
     3. Экранный диктор не скажет «кнопка Оплатить» — он скажет
        просто «Оплатить», и незрячий не поймёт, что сюда можно нажать.
     4. Нет состояния «выключена»: атрибут disabled на div не работает.
     5. Голосовое управление («нажми Оплатить») эту кнопку не найдёт. -->`;

  protected readonly buttonGood = `<!-- ✅ ПРАВИЛЬНО: один тег вместо одного тега, а работы стало меньше -->

<button type="button" class="button" onclick="pay()">Оплатить</button>

<!-- Что мы получили БЕСПЛАТНО, не написав ни строчки кода:
     - фокус по Tab и правильное место в порядке обхода;
     - нажатие по Enter и по пробелу;
     - экранный диктор произносит «Оплатить, кнопка»;
     - работает атрибут disabled — и визуально, и для диктора;
     - голосовое управление и режим высокого контраста понимают её. -->

<!-- И то же самое для перехода на другую страницу.
     Ссылка — это ссылка, а не кнопка с обработчиком: -->
<a href="/cart">Перейти в корзину</a>

<!-- ПРОСТОЕ ПРАВИЛО НА КАЖДЫЙ ДЕНЬ:
     что-то ДЕЛАЕТ (отправить, открыть окно, добавить) — это <button>;
     куда-то ВЕДЁТ (другая страница, якорь, файл)      — это <a>.
     Всё, что вы «сделаете сами» на div, вам придётся
     потом дописывать вручную — и вы обязательно что-нибудь забудете. -->`;

  protected readonly focusStyles = `/* ❌ САМАЯ ДОРОГАЯ СТРОЧКА CSS В ИСТОРИИ ВЕБА.
   Её пишут по одной причине: «стандартная обводка некрасивая».
   После неё человек, который ходит по сайту клавишей Tab,
   перестаёт понимать, где он сейчас находится. Представьте,
   что у вас исчез курсор мыши — но экран продолжает работать. */
*:focus {
  outline: none;
}

/* ✅ Если стандартная обводка не вписывается в дизайн —
   её не убирают, а ЗАМЕНЯЮТ на свою, хорошо заметную. */
:focus-visible {
  outline: 3px solid var(--accent);
  outline-offset: 2px;
  border-radius: 4px;
}

/* :focus-visible — это «фокус, который стоит показать».
   Браузер сам разбирается: пришли клавиатурой — обводку видно,
   ткнули мышкой — не видно, чтобы не мешала. Раньше это
   вычисляли вручную скриптами, теперь просто есть в браузере. */

/* Полезная мелочь: ссылка «сразу к содержимому».
   Она первая в документе и обычно спрятана за краем экрана,
   а при фокусе выезжает. Экономит человеку с клавиатурой
   тридцать нажатий Tab по меню на КАЖДОЙ странице. */
.skip-link {
  position: absolute;
  inset-inline-start: 8px;
  top: -100px;
  transition: top 0.15s;
}

.skip-link:focus-visible {
  top: 8px;
}`;

  protected readonly a11yBasics = `<!-- 1. КАРТИНКА СО СМЫСЛОМ.
     alt описывает не файл, а то, что человек упустит, не увидев картинку. -->
<img src="/chart-2026.png" alt="Продажи выросли с 3 до 7 миллионов за 2026 год">

<!-- 2. КАРТИНКА-УКРАШЕНИЕ.
     alt пустой — и диктор её молча пропустит, это правильно.
     А вот убрать alt совсем нельзя: тогда диктор начнёт
     читать вслух имя файла, «декор-вейв-точка-эс-ви-джи». -->
<img src="/decor-wave.svg" alt="">

<!-- 3. У КАЖДОГО ПОЛЯ ЕСТЬ ПОДПИСЬ, связанная с ним через for и id.
     Одного placeholder мало: он исчезает, как только начали печатать,
     и человек забывает, что именно он тут заполняет.
     Бонус: по подписи можно кликнуть, и попадёшь в поле. -->
<label for="email">Электронная почта</label>
<input id="email" type="email" name="email" autocomplete="email">

<!-- 4. ОШИБКА — ЭТО ТЕКСТ, а не только красная рамка.
     Примерно каждый двенадцатый мужчина плохо различает красный
     и зелёный, а диктор цвет рамки не произносит вообще.
     aria-describedby связывает поле с текстом ошибки. -->
<label for="phone">Телефон</label>
<input id="phone" type="tel" aria-invalid="true" aria-describedby="phone-error">
<p id="phone-error" class="error">Введите номер в формате +7 900 000-00-00</p>

<!-- 5. ЗАГОЛОВКИ ИДУТ ПО ПОРЯДКУ, без пропусков: h1, потом h2, потом h3.
     Диктор умеет прыгать по заголовкам — для незрячего это оглавление
     страницы. Пропущенный уровень читается как дыра в оглавлении.
     Заголовок выбирают по СМЫСЛУ, а размер шрифта задают стилями. -->
<h1>Каталог</h1>
<h2>Кружки</h2>
<h3>Керамика ручной работы</h3>`;

  protected readonly ariaUsage = `<!-- ГЛАВНОЕ ПРАВИЛО: если для задачи есть обычный тег — берите его.
     Никакого ARIA лучше, чем плохой ARIA: неверная подпись врёт
     человеку, а отсутствующая просто молчит. Молчание безопаснее. -->

<!-- ❌ Так не нужно: у настоящей кнопки роль «кнопка» уже есть,
     а здесь мы вручную пересобираем то, что и так работало. -->
<div role="button" tabindex="0">Отправить</div>

<!-- ✅ Случай, когда ARIA действительно нужна №1: кнопка без текста.
     Глазами видно крестик, а диктору читать нечего.
     Даём ему невидимую подпись — она не влияет на внешний вид. -->
<button type="button" aria-label="Закрыть окно">×</button>

<!-- ✅ Случай №2: своё раскрывающееся меню.
     aria-expanded сообщает «сейчас закрыто», и диктор произносит это
     вслух. ВАЖНО: значение надо менять из скрипта при каждом открытии.
     Забыли поменять — и подсказка превратилась во враньё. -->
<button type="button" aria-expanded="false" aria-controls="more-menu">Ещё</button>
<ul id="more-menu" hidden>
  <li><a href="/help">Помощь</a></li>
</ul>

<!-- ✅ Случай №3: сообщение, которое появилось само собой.
     Незрячий не увидит всплывашку «Сохранено» — но диктор прочитает её,
     если область помечена как «живая». polite означает
     «скажи, когда договоришь текущую фразу», а не перебивай. -->
<div role="status" aria-live="polite">Изменения сохранены</div>`;

  protected readonly i18nConcatBad = `// ❌ ФРАЗА СКЛЕЕНА ИЗ КУСКОВ.
// В коде это выглядит безобидно, но фраза здесь «зашита» в порядок слов.
const text = 'Найдено ' + count + ' товаров';

// Что произойдёт при переводе:
// - английский:  Found 5 items      — порядок слов ещё совпал, повезло;
// - немецкий:    5 Artikel gefunden — глагол уехал в конец, склейка сломалась;
// - японский:    5件見つかりました   — вообще другая структура фразы.
// Переводчику вы отдаёте три обрывка: 'Найдено ', число, ' товаров'.
// Собрать из них правильную фразу на своём языке он не сможет.

// ❌ Ещё хуже — «умная» склейка через условие:
const label = count === 1 ? 'товар' : 'товара';
// Для 5 это даст «5 товара». А для арабского нужно шесть вариантов,
// и никакое условие из двух веток тут не поможет.

// ✅ ПРАВИЛЬНО: в коде живёт КЛЮЧ, а не текст.
// Ключ — это адрес фразы в словаре. Целую фразу, со всеми её
// перестановками и склонениями, собирает переводчик на своём языке.
const text2 = t('search.found', { count });

// Теперь в коде нет ни одной русской буквы, которую увидит пользователь.
// Это и есть «интернационализация» — подготовка, а не сам перевод.`;

  protected readonly pluralRules = `// В русском языке у существительного при числе ТРИ формы,
// и выбираются они по хитрому правилу с остатками от деления:
//   1, 21, 31   — «1 товар»    (форма one)
//   2, 3, 4, 22 — «2 товара»   (форма few)
//   5..20, 25   — «5 товаров»  (форма many)
// В английском форм две, в японском одна, в арабском шесть.
// Писать это правило руками для каждого языка — гарантированные ошибки.

// Браузер уже знает правила всех языков. Спросим его:
const ru = new Intl.PluralRules('ru-RU');

ru.select(1); // 'one'
ru.select(2); // 'few'
ru.select(5); // 'many'
ru.select(21); // 'one'   ← а вот это условие «=== 1» не поймало бы

const en = new Intl.PluralRules('en-US');
en.select(1); // 'one'
en.select(5); // 'other'  ← в английском всего две формы

// Как этим пользоваться: формы лежат рядом в словаре,
// а код только выбирает нужную по имени.
const forms = {
  one: 'товар',
  few: 'товара',
  many: 'товаров',
};

function items(count) {
  const form = ru.select(count); // 'one' | 'few' | 'many'
  return count + ' ' + forms[form];
}

items(1); // '1 товар'
items(3); // '3 товара'
items(11); // '11 товаров'  ← классическая ловушка: 11, а не «11 товар»

// На практике этот код писать не придётся: библиотеки переводов
// делают ровно то же самое внутри. Но полезно знать, ЧТО там внутри.`;

  protected readonly intlFormat = `// Дата, число и валюта выглядят по-разному в каждой стране.
// Всё это тоже уже встроено в браузер — писать своё не нужно.

const date = new Date(2026, 7, 10, 14, 30); // 10 августа 2026, 14:30

// ДАТЫ. Обратите внимание: 10.08 и 08/10 — это ОДИН И ТОТ ЖЕ день,
// просто в США сначала пишут месяц. Отсюда классическая путаница
// в переписке: «встреча 05.06» понимается двумя людьми по-разному.
new Intl.DateTimeFormat('ru-RU').format(date); // '10.08.2026'
new Intl.DateTimeFormat('en-US').format(date); // '8/10/2026'
new Intl.DateTimeFormat('de-DE').format(date); // '10.8.2026'

// С названием месяца — тоже без ручных списков склонений:
new Intl.DateTimeFormat('ru-RU', { dateStyle: 'long' }).format(date);
// '10 августа 2026 г.'

// ЧИСЛА. Разделители целой части и дробной у стран разные,
// и они меняются местами — это не опечатка, а другая традиция.
new Intl.NumberFormat('ru-RU').format(1234567.89); // '1 234 567,89'
new Intl.NumberFormat('en-US').format(1234567.89); // '1,234,567.89'
new Intl.NumberFormat('de-DE').format(1234567.89); // '1.234.567,89'

// ВАЛЮТА. Символ, его сторона и пробелы — тоже часть языка.
const rub = new Intl.NumberFormat('ru-RU', { style: 'currency', currency: 'RUB' });
const usd = new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' });

rub.format(1490); // '1 490,00 ₽'   ← знак справа
usd.format(1490); // '$1,490.00'    ← знак слева

// «СКОЛЬКО ВРЕМЕНИ НАЗАД» — тоже без самодельных списков:
const rel = new Intl.RelativeTimeFormat('ru', { numeric: 'auto' });
rel.format(-1, 'day'); // 'вчера'
rel.format(-3, 'day'); // '3 дня назад'
rel.format(2, 'hour'); // 'через 2 часа'

// ВАЖНО ПРО ДЕНЬГИ: форматирование — это только показ.
// Настоящий пересчёт цен по курсу делает сервер, а не браузер.`;

  protected readonly logicalCss = `/* Арабский и иврит читают справа налево. Зеркалится не только текст:
   переворачивается вся раскладка — меню, отступы, стрелки, полоса прокрутки. */

/* ❌ Жёстко привязано к «лево» и «право».
   В арабской версии интерфейс отзеркалился, а эти отступы
   остались слева — и аккуратная карточка развалилась. */
.message {
  margin-left: 48px;
  border-left: 3px solid var(--accent);
  padding-right: 12px;
  text-align: left;
}

/* ✅ Логические свойства: не «слева», а «в начале строки».
   Где именно начало — решает направление письма страницы.
   Для русского начало слева, для арабского — справа.
   Один и тот же CSS правильно работает в обеих версиях. */
.message {
  margin-inline-start: 48px;
  border-inline-start: 3px solid var(--accent);
  padding-inline-end: 12px;
  text-align: start;
}

/* Направление задаётся один раз в корне документа:
   <html lang="ar" dir="rtl">
   ...и дальше весь CSS с логическими свойствами подстраивается сам. */

/* Ещё одна засада перевода — ДЛИНА ТЕКСТА.
   Немецкий обычно длиннее русского примерно на треть:
   «Настройки» -> «Einstellungen», «Отмена» -> «Abbrechen».
   Кнопка фиксированной ширины красиво расползётся. */

/* ❌ */
.btn {
  width: 120px;
  height: 40px;
  white-space: nowrap;
}

/* ✅ Ширину диктует содержимое, а высота может вырасти */
.btn {
  min-width: 120px;
  min-height: 40px;
  padding: 10px 16px;
}`;

  protected readonly localeRu = `{
  "cart": {
    "title": "Корзина",
    "empty": "Здесь пока пусто",
    "checkout": "Перейти к оплате",
    "itemsFound_one": "Найден {{count}} товар",
    "itemsFound_few": "Найдено {{count}} товара",
    "itemsFound_many": "Найдено {{count}} товаров"
  },
  "errors": {
    "network": "Не удалось связаться с сервером. Попробуйте ещё раз."
  }
}`;

  protected readonly i18nUsage = `// i18next — самая распространённая библиотека переводов.
// Для Vue есть vue-i18n, у Angular механизм встроенный, идея у всех одна.
import i18next from 'i18next';

import ru from './locales/ru.json';

await i18next.init({
  lng: 'ru', // язык, который показываем сейчас
  fallbackLng: 'en', // на что упасть, если перевод не нашёлся
  resources: { ru: { translation: ru } },
});

// В коде — только ключи. Ни одной строки на русском.
i18next.t('cart.title'); // 'Корзина'
i18next.t('cart.checkout'); // 'Перейти к оплате'

// Множественное число библиотека выбирает сама,
// по тем же правилам Intl.PluralRules, что мы видели выше:
i18next.t('cart.itemsFound', { count: 1 }); // 'Найден 1 товар'
i18next.t('cart.itemsFound', { count: 5 }); // 'Найдено 5 товаров'

// ЛЕНИВАЯ ЗАГРУЗКА ЯЗЫКА.
// Словари всех языков сразу качать не надо: японец никогда
// не прочитает немецкий словарь, а вес страницы вырастет на всех.
// Грузим только тот файл, который человеку действительно нужен.
async function switchLanguage(lang) {
  const messages = await import(\`./locales/\${lang}.json\`);
  i18next.addResourceBundle(lang, 'translation', messages.default);
  await i18next.changeLanguage(lang);

  // Не забыть сказать это самой странице — от языка зависят
  // и правила переноса слов, и направление письма:
  document.documentElement.lang = lang;
  document.documentElement.dir = lang === 'ar' || lang === 'he' ? 'rtl' : 'ltr';
}`;

  protected readonly clsImages = `<!-- ❌ ПОЧЕМУ ВЁРСТКА ПРЫГАЕТ.
     Браузер не знает размер картинки, пока не скачает её.
     Пока качается — места под неё не отведено, текст стоит выше.
     Картинка пришла — и всё, что ниже, уехало вниз.
     Это и есть CLS: сдвиг того, что человек уже видел. -->
<img src="/hero.jpg" alt="Летняя коллекция">

<!-- ✅ Указали width и height — браузер сразу знает пропорции
     и заранее резервирует ровно столько места, сколько нужно.
     Эти атрибуты нужны, ДАЖЕ ЕСЛИ размеры заданы в CSS:
     из них браузер вычисляет соотношение сторон. -->
<img src="/hero.jpg" alt="Летняя коллекция" width="1200" height="630">

<!-- ✅ Главную картинку первого экрана просим загрузить раньше прочих.
     Обычно именно она и есть тот самый LCP, который мы измеряем. -->
<img src="/hero.jpg" alt="Летняя коллекция" width="1200" height="630" fetchpriority="high">

<!-- ✅ А всё, что ниже первого экрана, — лениво:
     браузер начнёт качать эти картинки, только когда человек
     доскроллит до них. Экономия на длинных страницах огромная. -->
<img src="/review-1.jpg" alt="" width="400" height="300" loading="lazy">

<!-- ✅ Место под то, что приедет позже (баннер, реклама, виджет чата),
     резервируем заранее — пустой блок известной высоты.
     Пусть лучше секунду будет пустое место, чем всё подпрыгнет. -->
<div class="promo-slot" style="min-height: 120px"></div>`;

  protected readonly measureVitals = `// Lighthouse в браузере показывает ЛАБОРАТОРНЫЕ цифры:
// один запуск, на вашем быстром ноутбуке, с вашим интернетом.
// Настоящие пользователи сидят на старом телефоне в метро.
// Поэтому метрики собирают ещё и с живых людей.

// Библиотека web-vitals от команды Chrome измеряет ровно те три
// метрики, о которых говорит поисковая система, — и делает это
// у настоящего пользователя, прямо во время его визита.
import { onLCP, onINP, onCLS } from 'web-vitals';

function report(metric) {
  // metric.name   — 'LCP' | 'INP' | 'CLS'
  // metric.value  — число (миллисекунды или, для CLS, доля экрана)
  // metric.rating — 'good' | 'needs-improvement' | 'poor'
  const body = JSON.stringify({
    name: metric.name,
    value: metric.value,
    rating: metric.rating,
    page: location.pathname,
  });

  // sendBeacon отправляет данные «вдогонку», не задерживая уход
  // со страницы. Обычный fetch тут часто не успевает выполниться.
  navigator.sendBeacon('/api/vitals', body);
}

onLCP(report);
onINP(report);
onCLS(report);

// ВАЖНО, КАК СМОТРЕТЬ НА СОБРАННОЕ.
// Среднее значение бесполезно: один быстрый визит компенсирует
// в нём один мучительно медленный. Смотрят на 75-й процентиль —
// «хуже, чем у трёх четвертей визитов». Если он в норме,
// значит, большинству людей действительно хорошо.`;

  protected readonly perfBudget = `# Разовая проверка страницы из терминала: те же цифры,
# что и во вкладке Lighthouse, но без ручных кликов.
npx lighthouse https://example.com --view

# Бюджет производительности в CI — это автоматическая проверка
# при каждом изменении кода. Смысл ровно как у веса багажа
# в аэропорту: перевес — не пускаем, разбирайтесь до вылета.
npx @lhci/cli autorun

# Без такой проверки страница тяжелеет незаметно: каждый разработчик
# добавляет «всего одну маленькую библиотеку», и через полгода
# первый экран весит четыре мегабайта, а виноватого нет.`;
}
