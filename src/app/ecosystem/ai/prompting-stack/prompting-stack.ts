import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-ai-prompting-stack',
  imports: [CodeBlock, RouterLink],
  templateUrl: './prompting-stack.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemAiPromptingStack {
protected readonly badRequest = `

═══ ЗАДАЧА, КОТОРУЮ НАПИСАЛ КОСТЯ ═══

  сделай форму регистрации

  Три слова. Ответ пришёл через несколько секунд: готовый файл, ровные отступы,
  комментарии на английском. И ни одной строчки, которую можно оставить в проекте.


═══ ЧТО ПРИШЛО В ОТВЕТЕ ═══

  • компонент на React, написанный классом                            [!error]
  • самодельная проверка почты регулярным выражением                  [!error]
  • запрос на адрес /api/register, которого в проекте нет             [!error]
  • тексты ошибок по-английски: Invalid email                         [!error]
  • стили заданы прямо в атрибуте style                               [!error]
  • сообщение об ошибке показывается через alert                      [!error]


═══ ЧТО НЕ СКАЗАНО  →  ЧТО ДОДУМАНО  →  ЧТО СЛОМАЛОСЬ ═══

  (1) фреймворк и его версия не названы
      → React: в текстах, на которых училась модель, его больше всего
      → в магазине чая Angular 22; файл некуда подключить вообще      [!error]

  (2) принятый в команде стиль компонента не назван
      → класс с методом render: так писали в статьях 2018-2021 годов
      → в проекте только standalone-компоненты и сигналы              [!error]

  (3) список полей формы не перечислен
      → имя, почта, пароль: самый частый набор в интернете
      → нужны почта, пароль, повтор пароля и согласие на рассылку     [!error]

  (4) правила проверки полей не описаны
      → своя регулярка для почты, пароль от восьми символов
      → правила заданы на бэкенде и обязаны совпадать с ним           [!error]

  (5) адрес запроса и формат ответа не названы
      → POST /api/register, в ответе поле token
      → на самом деле POST /auth/signup, в ответе объект user         [!error]

  (6) язык интерфейса не назван
      → английский: на нём написано большинство примеров
      → магазин русский, покупатель видит Invalid email               [!error]

  (7) не сказано, как показывать ошибки
      → alert прямо в обработчике
      → в проекте есть общий сервис уведомлений, его и надо звать     [!error]

  ИТОГ:  семь умолчаний  →  семь допущений  →  ноль пригодных строк


═══ ТА ЖЕ ЗАДАЧА ПЛЮС ЧЕТЫРЕ СТРОЧКИ ПРО ПРОЕКТ ═══

  Сделай форму регистрации.

  Стек: Angular 22, standalone-компоненты, сигналы, строгий TypeScript.
  Классовых компонентов в проекте нет, any запрещён.
  Поля: почта, пароль, повтор пароля, чекбокс согласия на рассылку.
  Отправка: POST /auth/signup, тело { email, password }; в ответе объект user.
  Тексты для покупателя по-русски; ошибки показывать через существующий
  NotificationService, свой alert не писать.

  ЧТО ИЗМЕНИЛОСЬ В ОТВЕТЕ
    • standalone-компонент Angular вместо React-класса
    • поля ровно те, что нужны, и в нужном порядке
    • запрос уходит по реальному адресу, ответ разбирается правильно
    • тексты ошибок сразу по-русски
    • вместо alert вызывается сервис уведомлений
    → файл открывается в проекте и собирается; остаётся проверить логику

  Задача стала длиннее примерно на одну минуту работы.
  Ответ не стал идеальным — он стал ПРИМЕНИМЫМ.


═══ ПРОВЕРКА ЗАДАЧИ НА ПРОЧНОСТЬ ═══

  Перечитайте свою формулировку и по каждому пункту спросите себя:
  «если этого не сказать, какой вариант помощник выберет сам?»

  на чём написан проект и какой версии   → выберет самый популярный
  как в проекте принято писать такой код → выберет стиль из старых статей
  какие данные приходят и уходят         → придумает структуру сам
  куда идёт запрос                       → придумает адрес сам
  на каком языке тексты                  → напишет по-английски
  что трогать нельзя                     → сочтёт, что можно всё
  как вы поймёте, что готово             → решит сам, что уже готово

  Если хотя бы один из этих ответов вас не устраивает — допишите строчку.
  Одна строчка в задаче дешевле часа разбора чужого ответа.


═══ ПОЧЕМУ ЭТО НЕ «ПЛОХАЯ НЕЙРОСЕТЬ» ═══

  Дайте задачу «сделай форму регистрации» живому человеку, который ничего
  не знает про магазин чая. Он сделает примерно то же самое: возьмёт самый
  ходовой инструмент, самый частый набор полей и придумает недостающее.

  Разница ровно одна: человек, скорее всего, сначала переспросит.
  Помощник переспрашивает редко — он продолжает текст, а не ведёт переговоры.

  ПЛОХО   → сделай форму регистрации
  ЛУЧШЕ   → сделай форму регистрации на Angular 22, standalone
  ХОРОШО  → стек + поля + правила + адрес запроса + язык + ограничения
`;

  protected readonly whatItKnows = `

═══ ДВЕ ЧЕСТНЫЕ СТОПКИ ═══

  ЗНАЕТ (из обучающих данных)        │  НЕ ЗНАЕТ (этого нет нигде, кроме вас)
  ───────────────────────────────────┼────────────────────────────────────────
  синтаксис JavaScript и TypeScript  │  что лежит в вашем репозитории
  популярные библиотеки и их API     │  какие у вас имена сервисов и методов
  типовые решения типовых задач      │  почему год назад отказались от библиотеки
  документацию, попавшую в обучение  │  устные договорённости команды
  тысячи чужих открытых проектов     │  структуру и содержимое вашей базы
  распространённые ошибки и их фиксы │  как устроен ваш деплой и окружения
  общие принципы архитектуры         │  что модуль оплаты сейчас трогать нельзя
  как обычно пишут тесты             │  что у вас уже есть сервис уведомлений

  ВЫВОД: в левой колонке знание «вообще». В правой — знание про вас.
         Правая колонка попадает в ответ ТОЛЬКО если вы её туда положили.


═══ ЧТО МЕНЯЕТСЯ, ЕСЛИ ПОМОЩНИК УМЕЕТ САМ ЧИТАТЬ ФАЙЛЫ ═══

  Современные помощники в редакторе и в терминале это умеют:

    • ищут файлы по дереву проекта и открывают их сами
    • запускают команды и тесты, читают вывод, пробуют исправить ошибку
    • иногда умеют искать в интернете и открывать страницы

  НО:

    • читают то, что сочли нужным ОНИ, а не то, что важно вам
    • могут открыть три файла и не заметить четвёртый, самый важный   [!error]
    • запуск команд, запись файлов и выход в сеть обычно требуют
      вашего подтверждения — «он ничего не делает» часто означает
      «он ждёт, пока вы нажмёте кнопку»

  Вывод не меняется: если вы знаете, какой файл важен, — назовите его.


═══ АНАЛОГИЯ: НОВЫЙ СОТРУДНИК В ПЕРВЫЙ ДЕНЬ ═══

  Похоже:     очень начитан, видел много чужих проектов,
              но про ВАШ знает только то, что ему рассказали
  Ломается:   живой сотрудник запомнит сказанное навсегда,
              а помощник забывает всё между разговорами            [!error]

  Поэтому в новом чате у него снова первый рабочий день.
  Постоянные сведения о проекте не пересказывают каждый раз —
  их один раз записывают в файл соглашений проекта (раздел ниже).


═══ СКОЛЬКО ИНФОРМАЦИИ ДОБАВЛЯЕТ ФРАЗА ═══

  «пожалуйста»                                  → ноль фактов
  «это очень срочно»                            → ноль фактов
  «ты отлично разбираешься в Angular»           → ноль фактов
  «сделай как обычно»                           → ноль фактов          [!error]
  «в нашем стиле»                               → ноль фактов          [!error]
  «по аналогии с соседним компонентом»          → ноль, пока не назван
  ─────────────────────────────────────────────────────────────────────
  «Angular 22, standalone, сигналы»             → фреймворк, версия, стиль
  «файл src/app/cart/cart-item.ts»              → конкретное место
  «цена не пересчитывается при смене веса»      → конкретный симптом


═══ ПАРА «ПЛОХО → ХОРОШО» ═══

  ПЛОХО
    Пожалуйста, помоги мне, очень срочно нужно поправить компонент
    карточки товара, заранее спасибо!

    Что здесь есть: вежливость.
    Чего нет: фреймворка, версии, файла, симптома, ожидаемого поведения.

  ХОРОШО
    Angular 22, standalone, входные свойства через input().
    Файл src/app/catalog/product-card.ts.
    Симптом: при смене веса упаковки цена на экране не пересчитывается,
    хотя в консоли новое значение приходит.
    Ожидаю: цена обновляется сразу после выбора веса.

  Второе сообщение КОРОЧЕ первого и содержит пять фактов вместо нуля.
`;

  protected readonly cutoff = `

═══ ЧТО ТАКОЕ ОТСЕЧКА ОБУЧЕНИЯ ═══

  Тексты для обучения собрали до некоторой даты. Обучение запустили.
  Всё, что появилось в мире позже, в модель не попало.

  Опасно не само незнание, а его форма:

    ожидание →  «не знаю, это вышло после моей отсечки»
    реальность → подробный уверенный ответ по позапрошлой версии      [!error]

  Устаревший ответ выглядит РОВНО ТАК ЖЕ, как верный:
  та же интонация, те же комментарии, та же структура. Пометки нет.


═══ ШКАЛА СВЕЖЕСТИ: ЧЕМУ МОЖНО ВЕРИТЬ БЕЗ ПРОВЕРКИ ═══

  надёжнее ◄──────────────────────────────────────────────────► опаснее
  ┌──────────────┬──────────────┬───────────────┬────────────────────┐
  │ база языка   │ зрелая       │ свежий мажор  │ библиотека, вышед- │
  │ и платформы  │ библиотека   │ популярного   │ шая недавно        │
  │              │ без ломающих │ инструмента   │                    │
  │              │ изменений    │               │                    │
  └──────────────┴──────────────┴───────────────┴────────────────────┘

  база языка и платформы
    цикл for, Promise, async/await, SELECT с JOIN, HTTP-заголовки
    → описано миллион раз, годами не меняется; верьте почти без проверки

  зрелая библиотека без ломающих изменений
    → ответ скорее всего верен; сверьте пару имён методов с документацией

  свежий мажор популярного инструмента
    → САМАЯ КОВАРНАЯ ЗОНА: тема знакомая, ответ бодрый,
      но написан по прошлой версии                                   [!error]

  библиотека, вышедшая недавно
    → ответ может быть выдуман целиком: несуществующие методы
      и даже несуществующие пакеты                                   [!error]


═══ ИСТОРИЯ МАГАЗИНА ЧАЯ ═══

  Команда обновила тестовый фреймворк до Vitest 5 (вышел 3 сентября 2026).
  Костя попросил добавить тесты к сервису корзины. Пришёл аккуратный файл,
  почти всё позеленело сразу. На общем прогоне два теста упали.

  Ответ был написан по привычкам Vitest 3:
    • моки сбрасывались вручную, хотя в пятой версии clearMocks
      включён по умолчанию                                            [!error]
    • незавершённая асинхронная проверка раньше давала предупреждение,
      а теперь роняет тест                                            [!error]

  Признаков устаревания в тексте ответа не было ни одного.


═══ ЧТО СНИМАЕТ ПРОБЛЕМУ, А ЧТО ТОЛЬКО ЧАСТИЧНО ═══

  поиск в интернете
    помогает:  можно открыть свежую страницу документации
    не спасает: включён не всегда, может потребовать подтверждения,
                по узкому вопросу может ничего не найти,
                а найденное всё равно пересказывается своими словами

  чтение вашего package.json
    помогает:  видно, какие версии стоят у вас
    не спасает: помощник должен САМ решить туда заглянуть

  спросить у модели её дату отсечки
    не помогает: это обычный сгенерированный текст, а не показание
                 системы; дату смотрят в документации модели          [!error]

  Поставщики публикуют даты в документации, иногда сразу две: до какого
  момента знания наиболее полны и где проходит общая граница данных.
  Фактическая граница часто оказывается РАНЬШЕ объявленной — свежие
  веб-архивы содержат много старых материалов.


═══ ПАРА «ПЛОХО → ХОРОШО» ДЛЯ СВЕЖЕЙ БИБЛИОТЕКИ ═══

  ПЛОХО
    Настрой у нас новую библиотеку для графиков, используй последнюю версию.

    «Используй последнюю версию» — это просьба, а не сведения.
    Какая версия последняя, помощник не знает и знать не может.

  ХОРОШО
    Настрой библиотеку графиков, версия 4.2, Angular 22.
    Ниже кусок официального README этой версии.

    --- начало фрагмента README ---
    (сюда вставляется настоящий текст документации: установка,
     сигнатура функции, минимальный пример)
    --- конец фрагмента README ---

    Опирайся только на этот фрагмент; если чего-то в нём нет — скажи об этом,
    а не додумывай.

  Тридцать строк настоящей документации в задаче надёжнее любых уговоров.
`;

  protected readonly versions = `

═══ ПРАВИЛО ═══

  Если в задаче помещается ровно одна техническая подробность —
  пусть это будет ВЕРСИЯ.

  Все цифры ниже — по состоянию на сентябрь 2026 года.
  Свои значения обязательно сверьте с собственным package.json.


═══ ПАРА 1. ANGULAR: СТАРЫЙ МАЖОР ПРОТИВ 22 ═══

  ответ для старой версии          что в новой                как заметить
  ───────────────────────────────  ─────────────────────────  ──────────────
  создайте NgModule и впишите      с 19-й версии компоненты   ошибка сборки,
  компонент в declarations         standalone по умолчанию,   NgModule просто
                                   зависимости в поле         не нужен
                                   imports самого компонента                 [!error]

  Input как декоратор,             современный способ —       падает типизация
  значение читается как name       функция input(),           или в шаблоне
                                   читается как name()        вместо текста
                                                              видно странное   [!error]

  меняем поле объекта и ждём,      в 22-й версии стратегия    экран просто
  что экран сам перерисуется       по умолчанию OnPush        не обновляется   [!error]

  пример на Angular 20             Angular 22 требует строго  сборка не
  с TypeScript 5.8                 TypeScript 6.0.x и         стартует         [!error]
                                   Node.js не ниже 22.22.3


═══ ПАРА 2. REACT 18 ПРОТИВ 19 ═══

  ReactDOM.render для запуска      в 19-й версии удалён       приложение
  приложения                       полностью; нужен           не стартует      [!error]
                                   createRoot из
                                   react-dom/client

  propTypes и defaultProps         удалены для                тихо перестают
  у функционального компонента     функциональных             работать         [!error]
                                   компонентов

  обёртка forwardRef, чтобы        ref приходит обычным       лишний код,
  передать ref внутрь              пропом, обёртка не нужна   который команда
                                   (сама она пока работает)   обещает удалить

  строковый ref и старый контекст  удалены                    падает на старте [!error]


═══ ПАРА 3. EXPRESS 4 ПРОТИВ 5 ═══

  каждый async-обработчик          в пятой версии ошибка      лишние обёртки,
  оборачиваем в catch(next)        уходит в обработчик        код шумит
                                   ошибок сама

  маршрут вида '/*'                теперь пишут '/*splat'     приложение
                                                              падает при
                                                              запуске          [!error]

  проверка if (req.body.name)      req.body равен undefined,  падение на
  без парсера тела                 если тело не разобрано     первом запросе
                                   (раньше был пустой объект) без тела         [!error]


═══ ПАРА 4. TAILWIND CSS 3 ПРОТИВ 4 ═══

  три директивы tailwind           одна строка                стили вообще
  в файле стилей                   import "tailwindcss"       не применяются   [!error]

  настройки в JavaScript-конфиге   тема задаётся блоком       часть опций
                                   theme прямо в CSS;         старого конфига
                                   старый конфиг ещё можно    больше не
                                   подключить директивой      поддерживается
                                   config

  классы shadow-sm, shadow,        shadow-xs, shadow-sm,      СБОРКА МОЛЧИТ,
  rounded, outline-none,           rounded-sm,                вёрстка просто
  bg-opacity-50                    outline-hidden,            выглядит иначе   [!error]
                                   bg-black/50


═══ ГДЕ УЗНАТЬ СВОЮ ВЕРСИЮ ═══

  package.json                 → диапазон вида "крышка 19.0.0", не точная
                                 версия; мажор видно, остальное нет
  package-lock.json            → точная установленная версия
  yarn.lock / pnpm-lock.yaml   → то же для других менеджеров пакетов

  npm ls react                 → установленная версия одного пакета
  npm ls --depth=0             → все прямые зависимости с версиями
  node -v  /  npm -v           → версия рантайма и менеджера пакетов
  npx tsc -v                   → версия компилятора TypeScript
  npx vite -v                  → версия сборщика
  npx vitest --version         → версия тестового фреймворка
  npx prisma -v                → версия ORM
  ng version                   → сразу Angular, CLI, Node.js, TypeScript, RxJS

  ВАЖНО: «последняя версия на сайте библиотеки» и «версия у вас» —
  это разные вещи. У вас стоит то, что записано в lock-файле.


═══ ГОТОВАЯ СТРОЧКА «НАШ СТЕК» ═══

  Один раз пишем, дальше вставляем в каждую задачу:

    Наш стек: Angular 22 (standalone-компоненты, сигналы, строгий
    TypeScript 6.0), Node.js 24, Fastify 5, PostgreSQL (версию смотрите
    в docker-compose), Prisma 7, Vitest 5, сборка и деплой через Docker.
    Соглашения команды: классовых компонентов нет, any запрещён,
    запросы к базе только через слой репозиториев, тексты для покупателя
    по-русски.

  Написать — минута. Вставить — секунда. Экономит половину пустых ответов.
  Когда надоест копировать — переносите в файл соглашений проекта.


═══ ЛОВУШКА «НОВЕЕ ЗНАЧИТ ЛУЧШЕ» ═══

  TypeScript 7 вышел и он самый свежий.
  Но Angular 22 требует строго TypeScript 6.0.x.

  «Поставлю семёрку, она же новее» → неработающая сборка              [!error]

  Правильный вопрос не «что самое новое», а «что стоит у меня
  и с чем оно совместимо».
`;

protected readonly anatomy = `

═══ ЗАДАЧА ЦЕЛИКОМ: МИНИ-КОРЗИНА В ШАПКЕ МАГАЗИНА ЧАЯ ═══

  Ниже — одно сообщение помощнику. Слева подписано, к какой из шести
  частей относится каждый кусок. В реальном письме этих подписей нет,
  они добавлены для разбора.


──── 1 · ЦЕЛЬ ─────────────────────────────────────────────────────────

  В шапке сайта нужна мини-корзина. Она всегда показывает количество
  товаров и общую сумму. По клику раскрывается список позиций, каждую
  позицию можно удалить. Число и сумма обновляются сразу после
  добавления товара на любой странице, без перезагрузки.

  (Описано поведение, а не файлы и не классы. Как именно это устроено
   внутри — пусть решает тот, кто знает проект лучше в этот момент.)


──── 2 · СТЕК ─────────────────────────────────────────────────────────

  Фронтенд: Angular 22, standalone-компоненты, сигналы, SCSS,
            строгий TypeScript (strict: true, noImplicitAny).
  Бэкенд:   Node.js, Fastify, PostgreSQL через Prisma.
  Тесты:    Vitest. Сборка и деплой: Docker.

  Точные версии смотрю в package-lock.json, здесь привожу мажорные.
  Данные на сентябрь 2026 — у вас в проекте сверьтесь со своим
  package.json и lock-файлом.


──── 3 · КОНТЕКСТ ─────────────────────────────────────────────────────

  Прилагаю три вещи.

  (а) Тип позиции корзины — с ним стыкуется вывод списка:

      --- начало cart.types.ts ---
      export interface CartItem {
        id: string;
        name: string;          // название чая, уже на русском
        priceKopecks: number;  // цена в копейках, целое число
        quantity: number;
      }
      --- конец cart.types.ts ---

  (б) Публичные методы уже существующего сервиса — их я и вызываю:

      --- фрагмент cart.service.ts, только сигнатуры ---
      readonly items: Signal<CartItem[]>;
      readonly totalKopecks: Signal<number>;
      removeItem(id: string): void;
      --- конец фрагмента ---

  (в) Соседний компонент шапки как образец стиля: header-search.ts
      (приложен целиком, 60 строк). Сделай в том же стиле.


──── 4 · ОГРАНИЧЕНИЯ ──────────────────────────────────────────────────

  • Новых зависимостей не добавлять — только то, что уже установлено.
  • Публичный интерфейс CartService не менять: от него зависят
    страница корзины и страница оформления заказа.
  • any запрещён. Если тип неизвестен — unknown и сужение проверкой.
  • Классовых компонентов нет: standalone + сигналы.
  • Все тексты для пользователя на русском.
  • Файл header-search.ts не трогать вообще, он приложен как образец.


──── 5 · КРИТЕРИЙ ПРИЁМКИ ─────────────────────────────────────────────

  • Корзина пуста → вместо суммы надпись «Корзина пуста»,
    кнопка раскрытия заблокирована.
  • Один товар → «1 товар», сумма равна цене этого товара.
  • Добавили товар на странице каталога → число в шапке изменилось
    без перезагрузки страницы.
  • Сумма показывается как «1 240,50 ₽» — копейки делим на 100
    и округляем до двух знаков, целочисленных ошибок быть не должно.
  • Название длиной 120 символов не ломает вёрстку: обрезается
    многоточием.
  • npm run build проходит без ошибок и предупреждений.


──── 6 · ФОРМАТ ОТВЕТА ────────────────────────────────────────────────

  Сначала план в 5–7 пунктах: какие файлы создаёшь, какие трогаешь,
  что именно вычисляешь. Код не пиши, пока я не отвечу «ок».
  После «ок» — два новых файла целиком (mini-cart.ts, mini-cart.html)
  и отдельно строка, куда именно в header.html вставить тег.


═══ ТА ЖЕ ЗАДАЧА, УРЕЗАННАЯ ДО ТРЁХ ЧАСТЕЙ ═══

  Для мелкого дела шесть частей — перебор. Вот минимальный
  работоспособный вариант для «допиши одну функцию»:

  ЦЕЛЬ    Нужна функция formatPrice: принимает целое число копеек,
          возвращает строку вида «1 240,50 ₽» с неразрывным пробелом
          между разрядами.
  СТЕК    TypeScript 6, strict, Angular 22, без внешних библиотек.
  ПРИЁМКА 0 → «0,00 ₽»; 5 → «0,05 ₽»; 123456789 → «1 234 567,89 ₽»;
          отрицательное значение → минус перед числом.

  Три части, четыре строки, минута работы. Этого достаточно.


═══ ЧТО ЛОМАЕТСЯ, ЕСЛИ УБРАТЬ ОДНУ ИЗ ЧАСТЕЙ ═══

  убрали ЦЕЛЬ          → ответ решает не ту задачу                [!error]
  убрали СТЕК          → React с классовым компонентом            [!error]
  убрали КОНТЕКСТ      → поля title и price вместо name           [!error]
                         и priceKopecks, код не компилируется
  убрали ОГРАНИЧЕНИЯ   → в проект въезжает новый пакет            [!error]
                         и переписанный «заодно» CartService
  убрали ПРИЁМКУ       → на пустой корзине показано «0 ₽»         [!error]
                         вместо надписи, никто этого не заметил
  убрали ФОРМАТ        → простыня на 300 строк, из которой        [!error]
                         нужные правки надо выковыривать руками
`;

  protected readonly acceptance = `

═══ РАСПЛЫВЧАТО → ПРОВЕРЯЕМО ═══

  Расплывчатую формулировку узнают по прилагательному без числа.
  Слева — то, что пишут обычно. Справа — то, что можно проверить,
  не читая код.


  «чтобы работало быстро»                                        [!error]
    → на списке в 10 000 позиций первая отрисовка укладывается
      в 100 мс на production-сборке; повторная фильтрация — в 30 мс

  «обработай ошибки»                                             [!error]
    → сервер ответил 500  → текст «Не удалось загрузить корзину»
                            и кнопка «Повторить»
      сервер молчит 5 с   → тот же экран, запрос отменяется
      пустой массив       → это не ошибка: «Корзина пуста»
      сеть недоступна     → тот же экран, в консоль ничего не льём

  «проверь ввод»                                                 [!error]
    → email пустой        → «Укажите почту» под полем
      email без собачки   → «Проверьте адрес»
      всё валидно         → кнопка «Оформить» становится активной
      кнопка активна только когда оба поля валидны

  «сделай красиво»                                               [!error]
    → используем существующие классы из doc.scss, новых цветов
      не вводим, отступы кратны 4 px, шрифт наследуется

  «чтобы не падало»                                              [!error]
    → если в ответе сервера нет поля discount, компонент всё равно
      рендерится, скидка показывается как 0

  «сделай нормальную пагинацию»                                  [!error]
    → по 20 позиций на страницу; на последней странице кнопка
      «Дальше» заблокирована; при 0 позиций пагинация не рисуется


═══ ЧЕТЫРЕ УРОВНЯ СТРОГОСТИ ═══

  Уровень 1 · СЛОВАМИ
    «При пустом поле поиска показывается ошибка под полем,
     кнопка остаётся заблокированной.»
    Дёшево, быстро, уже в разы лучше, чем ничего.

  Уровень 2 · СПИСКОМ СЛУЧАЕВ
    Перечисляем 3–6 ситуаций и ожидаемое поведение в каждой,
    включая граничные. Побочный эффект: половину дыр в задаче
    вы находите сами, пока пишете список.

  Уровень 3 · ПАДАЮЩИМ ТЕСТОМ
    Прикладываем готовый тест на Vitest и пишем: «сделай так,
    чтобы он прошёл, сам тест не менять». Выполнить наполовину
    невозможно, объявить выполненным словами — тоже.

  Уровень 4 · КОМАНДОЙ
    «После правки проходят: npm run build, npm test, npx eslint src.»
    Работает только с помощником, который умеет запускать команды
    в вашем проекте. Для чата в браузере бесполезно.


═══ ГРАНИЧНЫЕ СЛУЧАИ: ЧЕК-ЛИСТ НА КАЖДЫЙ ДЕНЬ ═══

  Пройдитесь по списку глазами перед отправкой задачи. Пять секунд
  на пункт, и примерно в половине случаев формулировка меняется.

  • пустой список               ничего нет — что показываем?
  • ровно один элемент          нет ли «1 товаров» вместо «1 товар»?
  • очень длинная строка        120 символов названия не рвут вёрстку?
  • отсутствующее поле          сервер не прислал discount — падаем?
  • null вместо объекта         пришёл null там, где ждали объект
  • ноль и отрицательное        0 ₽, скидка 100 %, минусовой остаток
  • дробные деньги              копейки, округление, накопление ошибки
  • повторный клик              два запроса на удаление одной позиции
  • разрыв сети посередине      запрос ушёл, ответ не пришёл
  • две вкладки сразу           в одной удалили, во второй старые данные
  • очень большой объём         10 000 позиций — виртуальный список?
  • чужая локаль                точка вместо запятой в дробной части


═══ КАК КРИТЕРИЙ ВЫГЛЯДИТ ВНУТРИ ЗАДАЧИ ═══

  ...весь предыдущий текст задачи...

  КРИТЕРИЙ ПРИЁМКИ. Я проверю так:
    1) открою страницу с пустой корзиной — вижу «Корзина пуста»,
       кнопка раскрытия заблокирована;
    2) добавлю один товар — вижу «1 товар» и его цену;
    3) добавлю 12 товаров — вижу «12 товаров» и верную сумму;
    4) поставлю имя товара в 120 символов — вёрстка цела,
       текст обрезан многоточием;
    5) запущу npm test — зелёный;
    6) запущу npm run build — без ошибок и предупреждений.

  Если хотя бы один пункт не выполняется — скажи об этом прямо
  в ответе, не пиши «готово».


═══ ЧЕГО НЕ СТОИТ ДЕЛАТЬ ═══

  • писать критерий после того, как увидели ответ               [!error]
    (вы неизбежно подгоните проверку под то, что получилось)
  • верить слову «готово» в конце ответа                        [!error]
    (это конец текста, а не результат проверки)
  • просить «покрой тестами» без списка случаев                 [!error]
    (получите три теста на очевидное и ноль на граничное)
`;

  protected readonly context = `

═══ ВАРИАНТ 1. БЕЗ КОНТЕКСТА ═══

  ЗАДАЧА
    «Сделай компонент списка позиций корзины на Angular 22.»

  ЧТО ПОМОЩНИК НЕ ЗНАЕТ
    • как называется тип позиции и какие у него поля
    • откуда берутся данные: сервис, стор, входной параметр
    • как в проекте принято писать компоненты

  ЧТО ПРИДЁТ
    • свой интерфейс CartItem с полями title, price, qty       [!error]
      (у вас name, priceKopecks, quantity — не компилируется)
    • данные приходят через @Input() вместо сигналов           [!error]
    • цена выводится как есть, без деления на 100              [!error]
    • класс .cart-list со своими цветами вместо ваших          [!error]

  ИТОГ: код выглядит правильным, но не стыкуется ни с чем.
        Правки займут больше времени, чем написание с нуля.


═══ ВАРИАНТ 2. ВЕСЬ ПРОЕКТ В ОДНОМ СООБЩЕНИИ ═══

  ЗАДАЧА
    «Сделай компонент списка позиций корзины. Прикладываю проект.»
    + 26 файлов: все компоненты, все сервисы, схема Prisma,
      package-lock.json, конфиги Docker, папка e2e-тестов.

  ЧТО ПОЙДЁТ НЕ ТАК
    • важное тонет в неважном: из 26 файлов к делу               [!error]
      относятся два, остальные — шум
    • ответ дороже и медленнее: контекстное окно не бездонное,   [!error]
      цена растёт вместе с объёмом отправленного
    • помощник «чинит» то, о чём не просили: увидел в чужом      [!error]
      файле сомнительное место и переписал заодно
    • в сообщение случайно уехала строка подключения к базе      [!error]
      из docker-compose — так делать нельзя никогда

  ИТОГ: ответ на 300 строк, где нужная часть перемешана
        с пятью ненужными правками. Ревью превращается
        в поиск иголки.


═══ ВАРИАНТ 3. ОТОБРАННЫЙ КОНТЕКСТ ═══

  ЗАДАЧА
    Нужен компонент списка позиций корзины: строка на позицию,
    название, количество, цена, кнопка удаления. Angular 22,
    standalone, сигналы, строгий TypeScript.

    Прикладываю ровно то, с чем это будет стыковаться.

    (1) Тип позиции — по нему рисуется строка:

        --- cart.types.ts ---
        export interface CartItem {
          id: string;
          name: string;
          priceKopecks: number;
          quantity: number;
        }
        --- конец ---

    (2) Сигнатуры сервиса, тела вырезаны — мне важен контракт:

        --- cart.service.ts, только публичное ---
        readonly items: Signal<CartItem[]>;
        readonly totalKopecks: Signal<number>;
        removeItem(id: string): void;   // бросает, если id нет
        --- конец ---

    (3) Образец стиля — уже написанный компонент того же рода:

        --- header-search.ts, целиком, 58 строк ---
        ...файл...
        --- конец ---

    (4) Из конфигурации важно только это:

        --- tsconfig.json, фрагмент ---
        "strict": true,
        "noImplicitAny": true,
        "noPropertyAccessFromIndexSignature": true
        --- конец ---

    Сделай в том же стиле, что header-search.ts.

  ИТОГ: около 120 строк контекста вместо 6000. Ответ стыкуется
        с проектом с первого раза.


═══ ПРИЁМЫ СОКРАЩЕНИЯ ═══

  оставить сигнатуры, выбросить тела
    было:  40 строк реализации getCart
    стало: getCart(userId: string): Promise<Cart>
           // бросает NotFoundError, если корзины нет

  длинный массив → две записи и многоточие
    было:  340 объектов товаров в JSON
    стало: два объекта + строка «…ещё 338 таких же»

  большой файл → только нужные методы
    было:  компонент на 400 строк
    стало: два метода + строка «…остальные методы не связаны»

  конфигурация → только влияющие опции
    было:  package.json целиком
    стало: восемь строк из dependencies, которые тут важны


═══ ЧЕГО НЕ ПРИКЛАДЫВАТЬ НИКОГДА ═══

  • секреты: ключи, токены, строки подключения, пароли          [!error]
  • реальные персональные данные клиентов                       [!error]
  • package-lock.json и прочие lock-файлы целиком               [!error]
  • сгенерированный клиент Prisma и папку dist                  [!error]
  • скриншот кода вместо текста: теряются имена и отступы       [!error]


═══ ПОМОЩНИК, КОТОРЫЙ ЧИТАЕТ ФАЙЛЫ САМ ═══

  Даже ему полезно назвать файлы поимённо. Сравните:

  «разберись в проекте и сделай список корзины»                 [!error]
    → выберет файлы по совпадению слов, зацепит старый черновик
      cart-list.old.ts и тест из папки e2e, нужный тип не найдёт

  «посмотри cart.types.ts, публичную часть cart.service.ts
   и header-search.ts как образец стиля — больше ничего
   читать не нужно»
    → читает три файла, отвечает по делу, не тратит время
      и токены на обход всего репозитория
`;

  protected readonly constraints = `

═══ ОГРАНИЧЕНИЯ МАГАЗИНА ЧАЯ: ПОЛНЫЙ БЛОК ═══

  Этот текст почти не меняется от задачи к задаче. Держите его
  отдельно и вставляйте целиком.


──── ГРАНИЦЫ МОДУЛЯ ───────────────────────────────────────────────────

  Публичный интерфейс CartService не менять: сигнатуры items,
  totalKopecks, addItem, removeItem сохранить как есть.
    ПОЧЕМУ: от них зависят страница корзины и оформление заказа,
    изменение сигнатуры уронит сборку в двух других местах.

  Формат ответа API /api/cart не менять.
    ПОЧЕМУ: тот же ответ читает мобильное приложение, которое
    выкатывается отдельно и обновится не раньше чем через месяц.


──── ЗАВИСИМОСТИ ──────────────────────────────────────────────────────

  Новых пакетов не добавлять. Используй только то, что уже есть
  в package.json: Angular 22, RxJS, Prisma, Fastify, Vitest.
    ПОЧЕМУ, три причины сразу:
      1) пакет ради одной функции — это обновления, уязвимости
         и лишний вес сборки на годы вперёд;
      2) предложенный пакет может не поддерживать нашу версию
         фреймворка, и выяснится это только после установки;
      3) пакета может вообще не существовать — про это отдельный
         разговор ниже, и это не курьёз.

  Если без нового пакета совсем никак — напиши об этом словами
  и объясни почему, но код с ним не пиши.


──── СОГЛАШЕНИЯ СТИЛЯ ─────────────────────────────────────────────────

  Компоненты только standalone, состояние — сигналы.
    ПОЧЕМУ: с Angular 19 standalone по умолчанию, NgModule в новом
    коде мы не заводим. Данные на сентябрь 2026, сверьтесь
    со своим package.json.

  any запрещён. Для неизвестного типа — unknown и сужение проверкой.
    ПОЧЕМУ: strict включён, one any тянет за собой цепочку
    непроверенных мест.

  Запросы к базе только через слой репозиториев.
    ПОЧЕМУ: там живут логирование, обработка ошибок и лимиты;
    прямой вызов Prisma мимо репозитория обходит всё это.

  Все тексты для пользователя на русском, идентификаторы в коде
  на английском.
    ПОЧЕМУ: магазин русскоязычный, а кириллические имена переменных
    ломают половину инструментов.


──── СОВМЕСТИМОСТЬ ────────────────────────────────────────────────────

  Рантайм: Node.js 24 (Active LTS на сентябрь 2026), в CI он же.
  Браузеры: последние две версии Chrome и Firefox, Safari 16 и выше.
    ПОЧЕМУ: на Safari 16 приходится заметная доля заказов, а свежие
    возможности языка там поддержаны не все.

  Проверьте свои цифры: node -v в терминале и поле engines
  в package.json — они важнее любого примера со страницы.


──── НЕФУНКЦИОНАЛЬНЫЕ ─────────────────────────────────────────────────

  Не делать запрос в базу внутри цикла — собирать один запрос
  с фильтром по списку идентификаторов.
    ПОЧЕМУ: на корзине из 30 позиций это 30 обращений вместо одного.

  Не выбирать таблицу целиком без ограничения количества.
  Для списков больше 100 позиций — постраничная выборка.


──── ОБЪЁМ ПРАВКИ ─────────────────────────────────────────────────────

  Меняй только то, о чём попросили. Остальной файл верни строка
  в строку как есть: не переформатируй, не переименовывай, не
  добавляй комментарии к существующему коду, не «улучшай» соседние
  методы.
    ПОЧЕМУ: иначе в ревью приходит 200 строк изменений вместо трёх,
    и нужная правка теряется среди косметики.


═══ ЗАПРЕТ → ПОЗИТИВНАЯ ФОРМУЛИРОВКА ═══

  Запрет говорит, куда нельзя. Позитивная формулировка говорит,
  куда можно, — и потому выполняется надёжнее.

  «не используй any»                                             [!error]
    → типы строгие; если тип действительно неизвестен, используй
      unknown и сузь проверкой перед обращением к полям

  «не пиши сырой SQL»                                            [!error]
    → все запросы к базе через методы репозитория,
      образец — cart.repository.ts

  «не ставь новые пакеты»                                        [!error]
    → используй только то, что уже есть в package.json;
      список прилагаю ниже

  «не трогай лишнее»                                             [!error]
    → меняй только метод toggleCart; остальной файл верни
      без единого изменения, включая пробелы

  «без классовых компонентов»                                    [!error]
    → standalone-компонент с сигналами, как в header-search.ts


═══ КАК ЭТО ВЫГЛЯДИТ В ГОТОВОЙ ЗАДАЧЕ ═══

  ...цель, стек и контекст выше...

  ОГРАНИЧЕНИЯ
    • новых зависимостей нет — только package.json проекта;
    • публичный интерфейс CartService не меняем;
    • standalone + сигналы, any запрещён, unknown с сужением;
    • база только через репозитории;
    • тексты пользователю на русском;
    • меняешь только mini-cart.ts и mini-cart.html, header.html
      трогаешь одной строкой вставки, больше ничего.

  Если какое-то из ограничений мешает решить задачу — скажи
  словами, какое именно и почему, и предложи вариант.
  Нарушать молча не надо.
`;

protected readonly rulesFile = `

═══ ЧТО ВЫ ПЕЧАТАЕТЕ В КАЖДОЙ ЗАДАЧЕ, ЕСЛИ ФАЙЛА СОГЛАШЕНИЙ НЕТ ═══

  «У нас Angular 22, standalone-компоненты, сигналы, строгий TypeScript,
   any запрещён. Бэкенд — Fastify и Prisma, к базе ходим только через слой
   репозиториев. Тесты — Vitest, запуск npm test. Тексты для пользователя
   по-русски. Теперь добавь фильтр по сорту чая в каталог.»

  • полезного в этом запросе — одна последняя строка
  • первые четыре вы напечатаете ещё сто раз                             [!error]
  • однажды в спешке сократите — и ответ приедет на React                [!error]
  • и вы даже не свяжете одно с другим                                   [!error]


═══ ТО ЖЕ САМОЕ, НО ОДИН РАЗ И В РЕПОЗИТОРИИ ═══

  Кладём файл AGENTS.md в корень проекта. Это обычный Markdown, никаких
  обязательных полей и схемы у формата нет. Помощник читает его сам —
  упоминать файл в задаче не нужно. После этого задача выглядит так:

  «Добавь фильтр по сорту чая в каталог.»

--- начало файла AGENTS.md ---

# Чайный магазин — памятка для ассистента

## Что это за проект

Интернет-магазин рассыпного чая. Монорепозиторий из трёх частей:
apps/web — витрина, apps/api — бэкенд, packages/shared — общие типы.
Оплата и доставка подключены внешними сервисами, их обёртки лежат
в apps/api/src/integrations и трогать их без просьбы нельзя.

## Стек и версии

- Фронтенд: Angular 22, standalone-компоненты, сигналы, SCSS
- В проекте нет ни одного NgModule и ни одного классового компонента
- Язык: TypeScript 6, strict включён полностью
- Бэкенд: Node.js 24, Fastify 5, PostgreSQL 17, ORM — Prisma
- Тесты: Vitest
- Сборка и запуск окружения: Docker, docker compose up

## Команды

- npm run dev — витрина на localhost:4200
- npm run build — прод-сборка, она же основная проверка типов
- npm test — все тесты
- npm test -- src/cart — тесты одной папки
- npm run lint — ESLint; правила линтера менять нельзя
- npm run db:migrate — применить миграции Prisma

## Соглашения кода

- any запрещён. Если тип неизвестен — unknown и явное сужение.
- Состояние компонента — сигналы (signal, computed), а не RxJS-субъекты.
- Запросы к базе только через репозитории в apps/api/src/repositories.
  Прямой вызов prisma из обработчика маршрута — отказ на ревью.
- Все тексты, которые видит покупатель, — по-русски и в apps/web/src/i18n/ru.ts.
  В компонентах строк на русском быть не должно.
- Деньги везде — целые копейки в number. Рублей с дробной частью в коде нет.
- Имена файлов без суффикса component: cart-page.ts, cart-page.html.

## Куда класть новое

- Новая страница витрины: apps/web/src/app/pages/<имя>/
- Новый маршрут API: apps/api/src/routes/<ресурс>.ts плюс репозиторий рядом
- Тип, нужный и витрине, и бэкенду: packages/shared/src/types/
- Тест лежит рядом с файлом, который проверяет: cart.ts и cart.spec.ts

## Чего делать нельзя

- Добавлять новые зависимости без отдельного обсуждения.
- Править apps/api/src/generated — этот код генерирует Prisma.
- Менять schema.prisma без миграции в той же правке.
- Трогать интеграции оплаты без явной просьбы.

## Если сомневаетесь

Сначала спросите. Три вопроса лучше, чем двести строк не в ту сторону.

--- конец файла AGENTS.md ---


═══ ЧЕГО В ЭТОМ ФАЙЛЕ НАМЕРЕННО НЕТ ═══

  • паролей, токенов и строк подключения к базе                         [!error]
  • пересказа документации Angular на две страницы                      [!error]
  • пожеланий вроде «пиши хороший код» и «думай о производительности»   [!error]
  • сведений, которые устарели полгода назад                            [!error]
      устаревший файл соглашений хуже отсутствующего: ему верят,
      и вы получите аккуратный код по правилам, которых больше нет

  • перечисления всех 300 файлов проекта                                [!error]
      длинный файл и контекст съедает, и соблюдается хуже


═══ КОГДА ЗАВОДИТЬ И КАК РАСТИТЬ ═══

  завести        → в тот момент, когда второй раз печатаете одно и то же
  дополнять      → каждый раз, когда поправили помощника: правку,
                   которую вы сделали руками, допишите одной строкой в файл
  разносить      → когда корневой файл распух: в монорепозитории свой файл
                   кладут рядом с подпроектом, ближайший к коду побеждает
  выбрасывать    → при смене версии фреймворка первым делом перечитать файл
`;

  protected readonly planFirst = `

═══ ЗАПРОС БЕЗ ПЛАНА: ТАК ДЕЛАЮТ ЧАЩЕ ВСЕГО ═══

  «Добавь избранное: покупатель сохраняет чай в список и смотрит его
   на отдельной странице.»

  ЧТО ПРОИСХОДИТ ДАЛЬШЕ
    • приезжает 400 строк в семи файлах одним куском
    • в базе появляется таблица, о которой вы не просили               [!error]
    • избранное заодно пишется в localStorage «чтобы работало гостям»  [!error]
    • читать это целиком вы не станете — и что-то пройдёт мимо         [!error]
    • а переписывать уже жалко: работа же сделана                      [!error]


═══ ТОТ ЖЕ ЗАПРОС, НО СНАЧАЛА ПЛАН ═══

  «Код пока не пиши. Опиши план: какие файлы затронешь и что в каждом
   изменится, какие решения ты принимаешь и почему, что осознанно
   оставляешь за рамками. Нумерованный список, не длиннее 20 строк.
   После плана остановись и жди моего ответа.»

  ТРИ ФРАЗЫ, КОТОРЫЕ ДЕЛАЮТ ЭТОТ ЗАПРОС РАБОЧИМ
    → «код пока не пиши»      иначе план приедет вместе с кодом,
                              и спорить будет уже поздно
    → «какие файлы»           план без имён файлов проверить нельзя:
                              это будет пересказ вашей же задачи
    → «остановись и жди»      иначе сразу после плана начнётся работа


═══ ЧТО ПРИШЛО В ОТВЕТ ═══

  План: избранное

  1. packages/shared/src/types/favorite.ts — тип Favorite:
     userId, teaId, createdAt.
  2. prisma/schema.prisma — таблица favorites, миграция.
  3. apps/api/src/repositories/favorite.repository.ts — методы
     list, add, remove.
  4. apps/api/src/routes/favorites.ts — GET, POST и DELETE /favorites.
  5. apps/web/src/app/pages/favorites/ — страница со списком.
  6. apps/web/src/app/catalog/tea-card.ts — кнопка-сердечко. Для гостя
     пишем в localStorage, при входе переносим в базу.
  7. Тесты не пишу — за рамками задачи.


═══ ЧТО В ЭТОМ ПЛАНЕ ДОЛЖНО НАСТОРОЖИТЬ ═══

  пункт 6   → решение, о котором не просили: два хранилища сразу.
              Кто побеждает при конфликте? Что с чужим компьютером?    [!error]
  пункт 7   → молчание там, где у вас есть правило про тесты           [!error]
  пункт 2   → миграция базы стоит вторым шагом, а это самый дорогой
              и хуже всего откатываемый кусок работы                   [!error]
  везде     → ни слова про пустой список и про удалённый товар         [!error]

  ТРИ ВОПРОСА, КОТОРЫЕ ВЫ ЗАДАЁТЕ ЛЮБОМУ ПЛАНУ
    1. Есть ли здесь файлы, которых я не ожидал увидеть?
    2. Есть ли решения, которые я не просил принимать?
    3. О чём план молчит, хотя меня это волнует?

  Разбор занял минуту: перед вами двадцать строк текста.
  Тот же разбор по 400 строкам кода — полчаса, и то если повезёт.


═══ ОТВЕТ НА ПЛАН: ЗДЕСЬ И ЭКОНОМИТСЯ ДЕНЬ ═══

  «Пункт 6 убираем: избранное только для вошедших, гостю кнопка ведёт
   на страницу входа. Пункт 7 — тест на репозиторий обязателен, это
   правило проекта. Сделай сначала пункты 1 и 2 и остановись: миграцию
   я хочу посмотреть до того, как появится остальное.»


═══ ОТДЕЛЬНЫЙ ПРИЁМ: ПОПРОСИТЬ ВОПРОСЫ ═══

  «Прежде чем планировать, задай мне ровно пять вопросов, ответы
   на которые сильнее всего меняют решение. Пронумеруй их.
   Ни плана, ни кода пока не пиши.»

  • без «ровно пять» вопросов не будет вовсе либо будет один дежурный
  • без «кода пока не пиши» вопросы приедут вместе с готовым решением
  • иногда просьбу приходится повторить: помощник тянет сразу к делу


═══ ЧЕГО ПЛАН НЕ ДАЁТ ═══

  • это не обещание: код может разойтись с собственным планом           [!error]
  • это не проверка: план бывает складным, стройным и неверным          [!error]
  • это не замена чтению кода — только способ читать его осмысленно
`;

  protected readonly splitting = `

═══ ТАК СТАВИТЬ ЗАДАЧУ НЕЛЬЗЯ ═══

  «Сделай личный кабинет: профиль, история заказов, адреса доставки,
   смена пароля.»

  ПОЧЕМУ ЭТО ОБРЕЧЕНО
    • за вас примут несколько десятков решений, и узнаете вы о них
      только из кода — по одному на каждый вопрос, который вы не задали
    • ответ на 700 строк в одиннадцати файлах вы не прочитаете целиком  [!error]
    • ошибка в типе Order разойдётся сразу по четырём файлам            [!error]
    • результат «почти работает» невозможно ни принять, ни отклонить    [!error]
    • откатывать придётся всё разом: промежуточных точек нет            [!error]


═══ ТА ЖЕ ЗАДАЧА, РАЗЛОЖЕННАЯ НА ПЯТЬ ШАГОВ ═══

  ┌─ ШАГ 1 · контракт: типы заказа и адреса
  │    просим: только типы в packages/shared/src/types/order.ts
  │            и по комментарию на каждое поле — что оно означает
  │    почему первым: на эти типы обопрутся все следующие шаги
  │    читаю сам: деньги в копейках? статусы те же, что у API?
  │               нет ли any? нет ли выдуманных полей?
  │    фиксирую: npm run build, коммит «заказы: типы»
  │
  ├─ ШАГ 2 · слой запросов к базе
  │    просим: репозиторий apps/api/src/repositories/order.repository.ts
  │            с методами listByUser и getById, на типах из шага 1
  │    читаю сам: нет ли prisma в обработчике маршрута?
  │               что вернётся, если заказов нет — пустой массив или null?
  │    фиксирую: npm test, коммит «заказы: репозиторий»
  │
  ├─ ШАГ 3 · компонент списка заказов
  │    просим: standalone-компонент на сигналах, только отображение,
  │            данные приходят входным сигналом
  │    читаю сам: нет ли запроса к API прямо из шаблона?
  │               русские тексты вынесены в файл переводов?
  │    фиксирую: npm run build, коммит «заказы: список»
  │
  ├─ ШАГ 4 · загрузка, пустой список и ошибка
  │    просим: три состояния экрана и что показывать в каждом
  │    читаю сам: что увидит покупатель при упавшем запросе?
  │               не крутится ли спиннер вечно?
  │    фиксирую: проверка руками в браузере, коммит «заказы: состояния»
  │
  └─ ШАГ 5 · тесты и граничные случаи
       просим: тесты на репозиторий и на пустой список
       читаю сам: проверяются значения или только «что-то вернулось»?
       фиксирую: npm test, коммит «заказы: тесты»


═══ РАЗМЕР ОДНОГО ШАГА ═══

  норма    → один-два файла, один осмысленный кусок поведения,
             примерно 15 минут вашего чтения
  слишком  → «и заодно перепиши корзину»
  мелко    → «переименуй переменную» — быстрее сделать руками

  Верный признак, что шаг был велик: читая ответ, вы ловите себя
  на мысли «ладно, вот с этим куском разберусь потом».                 [!error]
  «Потом» не наступает. Этот кусок и окажется тем самым.               [!error]


═══ ПОЧЕМУ ПОРЯДОК ИМЕННО ТАКОЙ ═══

  сначала то, от чего зависит остальное → типы, контракты, имена
  потом то, что на это опирается        → репозиторий, компонент, тесты

  Если не зафиксировать тип Order на шаге 1, на шаге 2 помощник
  придумает свой — похожий, но с другим полем для суммы. Расхождение
  вы найдёте на шаге 4, когда чинить его дороже всего.                 [!error]


═══ КОГДА ПОРА НАЧАТЬ НОВЫЙ РАЗГОВОР ═══

  Длинный диалог постепенно заполняется мусором: отменённые решения,
  три версии одного файла, ваши «нет, не так» и «верни как было».
  Помощник начинает путаться и возвращает вариант, который вы отменили
  полчаса назад.                                                       [!error]

  ЧТО ВЗЯТЬ С СОБОЙ В НОВЫЙ РАЗГОВОР
    • текущее состояние файлов — оно уже в репозитории
    • пять строк принятых решений: избранное только для вошедших,
      суммы в копейках, статусы такие-то
    • следующий шаг
  ЧЕГО НЕ БРАТЬ
    • всю переписку целиком                                            [!error]
`;

  protected readonly testFirst = `

═══ ПОСТАНОВКА СЛОВАМИ ═══

  «Напиши расчёт доставки: бесплатно от 3000 рублей, иначе 350,
   самовывоз бесплатно, в дальние регионы дороже.»

  СКОЛЬКО ЗДЕСЬ ТОЛКОВАНИЙ
    • «от 3000» — ровно 3000 это уже бесплатно или ещё нет?            [!error]
    • сумма считается до скидки или после?                             [!error]
    • «дальние регионы» — это какие и насколько дороже?                [!error]
    • что вернуть для пустой корзины — ноль или ошибку?                [!error]

  Каждое из этих толкований помощник выберет за вас — молча
  и уверенно. Проверить выбор вы сможете только по коду.


═══ ТА ЖЕ ПОСТАНОВКА, НО ТЕСТОМ ═══

  Файл apps/web/src/app/checkout/delivery.spec.ts

--- начало файла ---


// Все суммы — целые копейки: 300000 это 3000 рублей.
// Функции ещё нет, тест красный. Это и есть задача.

describe('calcDelivery', () => {
  it('пустая корзина: доставки нет', () => {
    const cost = calcDelivery({
      subtotalKop: 0,
      method: 'courier',
      zone: 'city',
    });
    expect(cost).toBe(0);
  });

  it('обычный заказ до порога: 350 рублей', () => {
    const cost = calcDelivery({
      subtotalKop: 99000,
      method: 'courier',
      zone: 'city',
    });
    expect(cost).toBe(35000);
  });

  it('ровно порог: доставка уже бесплатна', () => {
    // именно этот случай словами не описывается однозначно
    const cost = calcDelivery({
      subtotalKop: 300000,
      method: 'courier',
      zone: 'city',
    });
    expect(cost).toBe(0);
  });

  it('самовывоз бесплатен при любой сумме', () => {
    const cost = calcDelivery({
      subtotalKop: 10000,
      method: 'pickup',
      zone: 'city',
    });
    expect(cost).toBe(0);
  });

  it('дальняя зона: 350 плюс 250, порог не действует', () => {
    const cost = calcDelivery({
      subtotalKop: 500000,
      method: 'courier',
      zone: 'remote',
    });
    expect(cost).toBe(60000);
  });
});

--- конец файла ---

  ЗАПРОС РЯДОМ С ТЕСТОМ
  «Вот тест delivery.spec.ts. Напиши delivery.ts так, чтобы он проходил.
   Тест не меняй: если какой-то случай кажется тебе неверным — скажи
   об этом словами, но правку в тест не вноси.»


═══ КАК НЕ НАДО: ТЕСТЫ, КОТОРЫЕ НИЧЕГО НЕ ПРОВЕРЯЮТ ═══

--- начало файла ---

it('работает', () => {
  expect(calcDelivery(order)).toBeDefined();  // пройдёт и с NaN         [!error]
});

it('вызывает поиск зоны', () => {
  const spy = vi.spyOn(zone, 'lookup');
  calcDelivery(order);
  expect(spy).toHaveBeenCalled();  // пересказ реализации                [!error]
});

it('считает доставку', () => {
  const expected = calcDelivery(order);
  expect(calcDelivery(order)).toBe(expected);  // всегда зелёный         [!error]
});

--- конец файла ---

  ЧТО ЗДЕСЬ СЛОМАНО
    • toBeDefined проходит, даже если вернулось NaN или минус тысяча     [!error]
    • «вызвал такую-то функцию» ломается при первом же рефакторинге       [!error]
    • сравнение результата с самим собой зелено всегда                    [!error]
    • ни одного числа из требований: 0, 35000, 60000 нигде нет            [!error]


═══ ДВА НАПРАВЛЕНИЯ ПРИЁМА ═══

  вы пишете тест → просите реализацию
    надёжнее: критерий приёмки придумали вы, подменить его нечем

  просите тесты по описанию → читаете → потом просите код
    быстрее, но читать тесты надо придирчиво:
      • есть ли граничные случаи, а не только «нормальный»?
      • стоят ли в ожиданиях конкретные числа?
      • не проверяет ли тест, что код устроен так, а не иначе?


═══ КТО ЗАПУСКАЕТ ТЕСТ ═══

  «Все тесты проходят» в ответе помощника — это утверждение.           [!error]
  Фактом оно становится, когда npm test прогнали вы и увидели вывод.
`;

protected readonly askingAboutError = `

═══ КАК СПРАШИВАЮТ ЧАЩЕ ВСЕГО ═══

  «Не собирается после обновления Prisma. Пишет, что что-то с типами.
   Что делать?»

  ЧЕГО В ЭТОМ ВОПРОСЕ НЕТ
    • самого текста ошибки — есть только пересказ
    • имени файла и номера строки
    • кода, на который ошибка указывает
    • версий: какая была, какая стала
    • что уже пробовали и чем это кончилось
    • чего вы ожидали вместо ошибки

  ЧТО ПРИДЁТ В ОТВЕТ
    • «убедитесь, что типы совпадают»                            [!error]
    • «выполните prisma generate»                                [!error]
    • «удалите node_modules и поставьте зависимости заново»      [!error]
    • ещё пять общих причин, из которых ни одна не ваша          [!error]

  Каждый из этих советов занимает у вас 2-5 минут. Ни один не попадает.


═══ ТОТ ЖЕ ВОПРОС, ЗАДАННЫЙ НОРМАЛЬНО ═══

  Сборка падает после обновления Prisma. Нужен разбор конкретной ошибки,
  а не список общих причин.

  ОКРУЖЕНИЕ
    Node 22, pnpm 9, TypeScript 5.9, Fastify 5
    Prisma 6.4 — обновили сегодня, до этого была 5.22
    Команда, которой получена ошибка: pnpm build  (под ней tsc -p tsconfig.json)

  ТЕКСТ ОШИБКИ ЦЕЛИКОМ, СКОПИРОВАН КАК ЕСТЬ
    --- начало вывода ---
    src/orders/order.repository.ts:42:11 - error TS2345: Argument of type
    '{ userId: string; items: OrderItemInput[]; comment: string | undefined; }'
    is not assignable to parameter of type 'OrderCreateInput'.
      Types of property 'comment' are incompatible.
        Type 'string | undefined' is not assignable to type 'string | null'.
          Type 'undefined' is not assignable to type 'string | null'.
    Found 1 error in src/orders/order.repository.ts:42
    --- конец вывода ---

  КОД, НА КОТОРЫЙ УКАЗЫВАЕТ ОШИБКА (order.repository.ts, строки 34-48)
    --- начало фрагмента ---
    async create(input: CreateOrderDto): Promise<Order> {
      return this.prisma.order.create({
        data: {
          userId: input.userId,
          items: input.items,
          comment: input.comment,   // это строка 42
        },
      });
    }
    --- конец фрагмента ---

    Поле comment в CreateOrderDto объявлено как comment?: string
    и приходит из тела HTTP-запроса, где его может не быть.

  ЧТО Я УЖЕ ПРОБОВАЛ
    • написал comment: input.comment ?? null
      → собирается, но я не понимаю, правильно ли это и что теперь
        окажется в базе у старых заказов
    • откатил Prisma на 5.22
      → собирается без правок, значит дело в мажорном обновлении
    • поискал слово "null" в списке изменений Prisma 6
      → нашёл много, однозначного ответа не нашёл

  ЧЕГО Я ОЖИДАЛ
    Что необязательное поле по-прежнему принимает undefined, как в Prisma 5.

  ЧТО НУЖНО В ОТВЕТЕ
    1. Почему тип поменялся именно так — что изменилось в Prisma 6.
    2. Какой из двух вариантов правильный: ?? null в репозитории
       или изменение схемы. Чем они отличаются на уровне базы.
    3. Нужна ли миграция и не сломает ли она существующие строки.
    Проект строгий: any запрещён, к базе ходим только через репозитории.


═══ ЧТО ИМЕННО ИЗМЕНИЛОСЬ МЕЖДУ ДВУМЯ ВОПРОСАМИ ═══

  пересказ ошибки            → точный текст с кодом TS2345 и двумя типами
  «после обновления»         → 5.22 → 6.4, названы обе версии
  ничего про код             → пятнадцать строк вокруг строки 42
  ничего про попытки         → три попытки и результат каждой
  ничего про ожидания        → сказано, какое поведение считалось нормальным
  «что делать?»              → три пронумерованных вопроса
  ничего про соглашения      → any запрещён, доступ к базе через репозитории

  Длиннее? Да, примерно в двадцать раз. Дороже по времени? Три минуты
  на копирование против получаса переписки. Это и есть вся арифметика.


═══ ТРИ КЛАССА ВОПРОСА — И ЧТО ГОТОВИТЬ ДЛЯ КАЖДОГО ═══

  1. НЕ СОБИРАЕТСЯ / ПАДАЕТ ВСЕГДА
     Готовим: текст ошибки, код вокруг, версии, команду.
     Помощник силён: всё воспроизводимо, ответ проверяется одной сборкой.

  2. ПАДАЕТ НА ПРОДЕ ИНОГДА
     Готовим: логи за период, частоту, что общего у упавших запросов,
     время суток, нагрузку, ответ внешнего сервиса.
     Помощник слаб без данных. Сначала сбор фактов, потом вопрос.
     Плохо: «иногда падает оформление заказа, почему?»          [!error]
     Хорошо: «за сутки 14 падений, все между 19:00 и 20:30,
              все на заказах с промокодом, вот три трейса…»

  3. РАБОТАЕТ НЕ ТАК, КАК Я ХОЧУ
     Это не ошибка, а задача. Ошибки нет: код делает то, что написано.
     Готовим: вход, текущий результат, желаемый результат, критерий приёмки.
     Плохо: «сортировка работает неправильно, исправь»           [!error]
     Хорошо: «на входе список из 3 товаров …, сейчас порядок такой,
              нужен такой, правило сортировки — сначала по наличию,
              затем по цене по возрастанию»


═══ КАК НЕ НАДО: ЛОГ, СКОПИРОВАННЫЙ НЕ ГЛЯДЯ ═══

  DATABASE_URL=postgres://shop:S3cr3tPass@db.tea-shop.ru:5432/shop      [!error]
  Authorization: Bearer sk-live-8f21c0a4e9b7d3                          [!error]
  order 10482 user=irina.petrova@example.com phone=+7-916-000-11-22     [!error]

  ЧТО ПОСТАВИТЬ ВМЕСТО
  DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/shop
  Authorization: Bearer TOKEN
  order 10482 user=user-1 phone=phone-1

  На разбор ошибки замена не влияет вообще: помощнику нужна форма строки,
  а не её содержимое. Зато переписка больше не содержит того, чему
  в переписке не место.
`;

  protected readonly refactoring = `

═══ ИСХОДНЫЙ КОД, КОТОРЫЙ ХОЧЕТСЯ ПОЧИСТИТЬ ═══

  --- начало src/cart/cart.service.ts, фрагмент ---
  function calcTotal(items: any[], user: any) {
    let sum = 0;
    for (const it of items) {
      if (it) {
        if (it.price) {
          if (it.qty) {
            sum = sum + it.price * it.qty;
          }
        }
      }
    }
    if (user) {
      if (user.isVip) {
        sum = sum * 0.9;
      }
    }
    return sum;
  }
  --- конец фрагмента ---

  Тестов на эту функцию нет. Она вызывается из корзины и из письма
  с подтверждением заказа. Это важно: у неё два потребителя.


═══ ПРОСЬБА, ПОСЛЕ КОТОРОЙ ПРИДЁТСЯ ОТКАТЫВАТЬСЯ ═══

  «Улучши этот файл»

  ЧТО ВЕРНЁТСЯ
    • функция переименована в calculateOrderTotal                 [!error]
    • появился интерфейс CartItem и класс TotalCalculator          [!error]
    • файл разбит на три новых файла                               [!error]
    • скидка VIP вынесена в «стратегию» с фабрикой                 [!error]
    • заодно добавлено округление до двух знаков —
      поведение изменилось, но об этом никто не сказал             [!error]

  ПОЧЕМУ ТАК ВЫХОДИТ
    У помощника нет ваших критериев «лучше». Он обязан что-то сделать,
    поэтому применяет общие, усреднённые представления о хорошем коде.
    Каждое действие по отдельности разумно. Вместе они дают дифф,
    в котором одно настоящее изменение поведения спрятано среди
    сорока косметических правок — и вы его не найдёте.


═══ ШАГ НОЛЬ: СНАЧАЛА ТЕСТЫ, ПОТОМ РЕФАКТОРИНГ ═══

  Напиши тесты на Vitest для функции calcTotal из src/cart/cart.service.ts.
  Задача теста — зафиксировать поведение КАК ЕСТЬ, включая странное.
  Саму функцию не меняй ни на строку, даже если видишь в ней ошибки.

  Покрой случаи:
    • обычный список из двух позиций
    • позиция с qty = 0 и позиция без поля price
    • пустой список
    • user = null и user.isVip = true
    • отрицательное qty (сейчас оно складывается — так и запиши)

  Имена тестов на русском, идентификаторы английские.
  Файл положи рядом: src/cart/cart.service.spec.ts


═══ ШАГ ОДИН: ОДНА ПРАВКА С НАЗВАННЫМИ ГРАНИЦАМИ ═══

  В функции calcTotal из src/cart/cart.service.ts убери вложенность
  условий: замени пирамиду if на проверки с ранним переходом
  к следующей итерации.

  ДОЛЖНО ОСТАТЬСЯ НЕИЗМЕННЫМ
    • имя функции и порядок параметров
    • результат на всех случаях из cart.service.spec.ts
    • место функции — тот же файл, новых файлов не создавать
    • никаких новых экспортов

  ЧЕГО НЕ ДЕЛАТЬ
    • не добавлять округление
    • не менять размер скидки и условие её применения
    • не трогать вызывающий код

  ОТДАЙ РЕЗУЛЬТАТ ДИФФОМ: только изменённые строки плюс список файлов,
  которых ты касался. Файл целиком не нужен.


═══ ШАГ ДВА: ВТОРАЯ ПРАВКА, ОТДЕЛЬНЫМ СООБЩЕНИЕМ ═══

  Теперь убери any: опиши тип позиции корзины и тип пользователя.
  Тип позиции возьми из уже существующего src/cart/cart.types.ts,
  новых типов не изобретай. Если нужного типа там нет — скажи об этом
  и остановись, ничего не придумывая.

  РЕЗУЛЬТАТ ПОСЛЕ ДВУХ ШАГОВ
  --- начало фрагмента ---
  function calcTotal(items: CartItem[], user: User | null): number {
    let sum = 0;
    for (const item of items) {
      if (!item) continue;
      if (!item.price || !item.qty) continue;
      sum = sum + item.price * item.qty;
    }
    if (user && user.isVip) {
      sum = sum * 0.9;
    }
    return sum;
  }
  --- конец фрагмента ---

  Имя то же, параметры те же, результат тот же, тесты зелёные.
  Дифф читается за минуту. Откатить можно любой из двух шагов отдельно.


═══ СПИСОК ПРОСЬБ, КОТОРЫЕ ФОРМУЛИРУЮТСЯ ОДНОЗНАЧНО ═══

  • вынести повторяющийся фрагмент из двух мест в общую функцию
  • убрать вложенность условий ранним выходом
  • разбить длинную функцию по границам ответственности:
    отдельно чтение данных, отдельно расчёт, отдельно ответ
  • заменить самописную реализацию на возможность стандартной библиотеки
  • заменить any на точные типы, не добавляя новых экспортов
  • вынести числа-константы в именованные константы
  • сделать функцию чистой: обращение к базе поднять на уровень выше

  У каждой есть ответ на вопрос «что именно сделать» и видимая граница.
  У просьбы «улучши» нет ни того, ни другого.


═══ ПРИЁМ: СНАЧАЛА ДИАГНОЗ, ПОТОМ ПРАВКА ═══

  Прочитай src/cart/cart.service.ts и перечисли по пунктам, что здесь
  мешает читать и поддерживать. Только список, ничего не меняй.
  Для каждого пункта — почему это проблема и чем она грозит на практике.
  Отсортируй по важности.

  Дальше вы отвечаете: «делаем пункты 2 и 5, остальное не трогаем —
  пункт 3 у нас сознательно так сделан, пункт 7 не наш случай».
  Решение остаётся у вас, помощник работает руками.
`;

  protected readonly explaining = `

═══ ВОПРОС, КОТОРЫЙ ПОЧТИ НИЧЕГО НЕ ДАЁТ ═══

  «Объясни этот код»

  ЧТО ПРИДЁТ
    «Здесь объявляется переменная total. Далее выполняется цикл
     по массиву items. Внутри цикла проверяется условие…»        [!error]

  Это построчный пересказ того, что вы и так видите. Ноль выводов,
  ноль ответа на вопрос «почему так сделано» и «на что тут смотреть».


═══ ДЕСЯТЬ ВОПРОСОВ, КОТОРЫЕ ДАЮТ ОТВЕТ ═══

  1. Что делает этот файл — двумя предложениями, без перечисления строк.
     ДАЁТ: общую рамку. Дальше читать станет в разы легче.

  2. Перечисли по шагам, что происходит от прихода HTTP-запроса
     до ответа клиенту. Пронумеруй шаги.
     ДАЁТ: порядок выполнения. Незаменимо в асинхронном коде,
     где по тексту видно вызовы, но не видно очерёдности.

  3. Зачем нужен блок со строк 118 по 140? Что он решает?
     ДАЁТ: назначение непонятного куска, а не его перевод на русский.

  4. Что сломается, если убрать этот блок? Приведи конкретный сценарий.
     ДАЁТ: цену вопроса. Часто выясняется, что блок мёртвый.

  5. Какие предположения эта функция делает о входных данных?
     Чего она молча ждёт и не проверяет?
     ДАЁТ: список неявных условий — главный источник падений на проде.

  6. Назови всё внешнее, что этот файл трогает: таблицы базы, сторонние
     сервисы, файловую систему, переменные окружения, текущее время,
     случайные числа, глобальное состояние.
     ДАЁТ: границы модуля. Сразу видно, что мешает его тестировать.

  7. Перечисли неочевидные места: странный порядок аргументов, молчаливое
     приведение типов, возврат разных типов из одной функции, побочный
     эффект там, где его не ждёшь.
     ДАЁТ: карту точек, куда смотреть внимательно.

  8. Где здесь возможна гонка, утечка или необработанное отклонение
     промиса? Для каждого случая — при каких условиях это проявится.
     ДАЁТ: гипотезы, которые можно проверить экспериментом.

  9. Объясни функцию applyPromo как новичку: разверни сокращения,
     назови вещи полными именами, не пропускай очевидное.
     ДАЁТ: то самое «очевидное», которого вы не знали.

 10. Нарисуй схему словами: какие сущности участвуют и кто кого вызывает.
     ДАЁТ: структуру, которую потом можно вклеить в документацию.


═══ КАК ЭТО ВЫГЛЯДИТ ЦЕЛИКОМ: ЧУЖОЙ ОБРАБОТЧИК ЗАКАЗОВ ═══

  Ситуация: магазин чая, файл src/orders/order-legacy.handler.ts,
  около 300 строк, комментариев нет, автор уволился год назад.
  Через файл проходят все заказы с промокодами. Трогать боятся.

  СООБЩЕНИЕ 1
    Прочитай src/orders/order-legacy.handler.ts.
    Ответь ровно на два вопроса, коротко:
      1) что делает этот обработчик — два предложения;
      2) какие входные данные он ждёт и какой формат ответа отдаёт.
    Ничего не предлагай менять.

  СООБЩЕНИЕ 2
    Теперь по шагам: что происходит от прихода POST /api/orders
    до ответа клиенту. Пронумеруй. Отдельно отметь шаги, которые
    выполняются асинхронно и могут завершиться в другом порядке.

  СООБЩЕНИЕ 3
    Назови всё внешнее, что этот файл трогает: таблицы Postgres через
    Prisma, вызовы сторонних сервисов, переменные окружения, время.
    Для каждого пункта — строка, где это происходит.

  СООБЩЕНИЕ 4
    Перечисли 5-7 мест, которые выглядят опасными или неочевидными.
    Для каждого: чем именно опасно и при каких данных проявится.
    Сортируй по риску. Правок не предлагай.

  Через десять минут у вас есть карта файла и список из пяти мест,
  которые надо проверить своими руками. Ни одна строка не изменена.


═══ ОБЯЗАТЕЛЬНЫЙ ШАГ: ПРОВЕРИТЬ ОБЪЯСНЕНИЕ ═══

  Объяснение — такой же сгенерированный текст, как и код,
  и оно бывает уверенно неверным. Три места, где врут чаще всего:

    • поведение конкретной версии библиотеки
      (модель помнит одну версию, у вас в lock-файле другая)
    • то, что происходит в файле, который она не видела
    • окружение: переменные среды, прокси, порядок подключения плагинов

  ЧЕМ ПРОВЕРЯТЬ — маленьким экспериментом, не спором:
    • временный вывод в консоль: совпадает ли реальный порядок
      шагов с обещанным
    • точка останова в отладчике
    • один тест ровно на одно утверждение:
      «если comment не передан, в базу пишется null»

  Пять минут эксперимента стоят дороже страницы уверенного текста.


═══ КАК НЕ НАДО ═══

  «Объясни этот код и заодно исправь, что тебе не понравится»   [!error]
    → правки перемешаны с объяснением, отделить одно от другого
      уже нельзя, а проверить — тем более

  «Объясни весь проект»                                         [!error]
    → в контекст войдёт не весь проект, а сколько поместится;
      про остальное будет придумано

  Поверить объяснению и сразу переписать файл                   [!error]
    → сначала проверка одного-двух ключевых утверждений,
      потом уже правки
`;

  protected readonly iterating = `

═══ ЦИКЛ, В КОТОРОМ ВЫ НАХОДИТЕСЬ ═══

        ┌──────────────────── не то, уточнение ─────────────────┐
        │                                                       │
        ▼                                                       │
     задача ──→ ответ ──→ вы проверяете ──→ не то ──────────────┘
                              │
                              └──→ то, что нужно ──→ коммит, готово

  Важное правило: кругов не больше трёх. После третьего круга
  проблема почти всегда в задаче, а не в ответе.


═══ КРУГ 1: ОТВЕТ ПРИШЁЛ, НО НЕ РАБОТАЕТ ═══

  ПЛОХО
    «не работает»                                               [!error]
    «ошибка»                                                    [!error]
    «всё равно не то»                                           [!error]

  ПОЧЕМУ ПЛОХО
    Помощник не видит вашего экрана. Он не знает, вставили ли вы код
    целиком, запустилось ли оно вообще, что именно вывелось.
    Получив «не работает», он обязан что-то ответить — и начнёт
    перебирать гипотезы наугад.

  ХОРОШО — четыре вещи, три строки текста
    Применил твой вариант функции loadCart целиком, остальной файл
    не трогал. При открытии корзины в консоли:
      TypeError: Cannot read properties of null (reading 'items')
      at loadCart (src/cart/cart.service.ts:28:22)
    Ожидал пустую корзину, а не исключение: сервер на пустой корзине
    отвечает 200 и телом null, а не пустым массивом.
    От себя поменял только путь импорта — у нас он через алиас @cart.

  ЧЕТЫРЕ ОБЯЗАТЕЛЬНЫЕ ЧАСТИ
    1. что именно произошло — текст ошибки или наблюдаемое поведение
    2. чем это отличается от ожидаемого
    3. какую часть ответа вы применили
    4. что вы поменяли от себя ← пропускают чаще всего


═══ КРУГ 2: ПРАВЬ ТОЛЬКО ЭТО ═══

  ПЛОХО
    «Перепиши, чтобы работало»                                  [!error]
    → вернётся новый файл целиком, в нём поменяется всё,
      включая то, что вас устраивало

  ХОРОШО
    В функции loadCart оставь всё как есть. Поменяй ровно одно:
    обработку ответа null — сейчас бросается исключение,
    надо возвращать пустую корзину (items: [], total: 0).
    Сигнатуру не меняй, новых зависимостей не добавляй.
    Верни только изменённый фрагмент, файл целиком не нужен.

  ПОЧЕМУ РАБОТАЕТ
    • названа граница правки — одно место
    • сказано, что остальное неприкосновенно
    • сказано, в каком виде отдать результат
    • вы читаете десять строк вместо трёхсот


═══ КРУГ 3: ПОСЛЕДНЯЯ ПОПЫТКА ═══

  На третьем круге добавляйте не уточнение, а ОГРАНИЧЕНИЕ —
  то, чего не хватало с самого начала.

    Стало лучше, но остался старый вызов в строке 34, а он ходит
    в базу напрямую. У нас в проекте к базе обращаются только
    через слой репозиториев, напрямую из сервиса нельзя.
    Перепиши этот вызов через CartRepository, остальное не трогай.

  Если после этого ответ всё ещё не тот — круги закончились.


═══ ПРИЗНАКИ, ЧТО ПОРА НАЧИНАТЬ ЗАНОВО ═══

  • вернулся вариант, который вы уже отвергли два сообщения назад
  • каждый новый ответ хуже предыдущего
  • помощник извиняется чаще, чем предлагает
  • он ссылается на код, которого у вас на диске уже нет
  • вы объясняете одно и то же в третий раз разными словами

  ВЫВОД: проблема в постановке, а не в исполнении. Ответ уточнением
  не чинится, потому что ломается он не там.


═══ КАК НАЧАТЬ ЗАНОВО ПРАВИЛЬНО ═══

  ПЛОХО: удалить всё и написать ту же задачу заново              [!error]
         → потеряете и рабочую половину, и все выводы,
           сделанные за три круга

  ХОРОШО: новый разговор, в который перенесено ровно три вещи

    1. Текущее состояние кода — тот файл, который лежит у вас
       на диске сейчас, а не тот, что был в начале.
    2. Уточнённая задача — с тем, чего в ней не хватало:
       обычно это критерий приёмки или ограничение.
    3. Короткий список «что уже пробовали и почему не подошло»:
       через репозиторий нельзя — вот причина; через кэш пробовали —
       не помогло, поведение не изменилось.

  ПОЧЕМУ НЕ ПРОДОЛЖАТЬ СТАРЫЙ РАЗГОВОР
    В длинной переписке едут все неудачные версии кода и все
    ошибочные догадки. Помощник опирается на них и с большой
    вероятностью повторяет собственную раннюю ошибку — она же
    у него прямо перед глазами, написана его же словами.
    Плюс каждый круг заново оплачивается целиком.


═══ ЧТО СОХРАНИТЬ, КОГДА ПОЛУЧИЛОСЬ ═══

  Не весь разговор — только финальный текст задачи.

    docs/prompts/new-fastify-endpoint.md
    docs/prompts/angular-list-component.md
    docs/prompts/prisma-migration.md

  В каждом файле: версии стека, ограничения проекта, критерий приёмки,
  формат ответа. Через месяц у вас десяток заготовок под типовые работы,
  собранных не специально, а по одной из удачных случаев.
`;

protected readonly hallucinatedDeps = `

═══ ОТКУДА БЕРЁТСЯ ИМЯ, КОТОРОГО НЕТ ═══

  ВЫ СПРОСИЛИ
    «Как в нашем Fastify-сервисе аккуратно сделать постраничный вывод заказов?»

  ЧТО ПРОИСХОДИТ ВНУТРИ
    модель не ищет пакет в реестре — она продолжает текст
    после слов «удобнее всего взять» статистически ожидается имя библиотеки
    имя собирается из знакомых кусков:  fastify- + prisma- + pagination

  ЧТО ПРИШЛО В ОТВЕТЕ
    npm i fastify-prisma-pagination                                       [!error]
    import { paginate } from 'fastify-prisma-pagination';                 [!error]

  ПОЧЕМУ ЭТО НЕ ЗАМЕТНО ГЛАЗАМ
    • имя построено по тем же правилам, что и настоящие имена
    • рядом настоящие пакеты: fastify-plugin, @prisma/client — фон правдоподобный
    • ответ уверенный, с примером использования и описанием параметров


═══ ДВЕ РАЗНЫЕ ДОРОЖКИ: МЕТОД ПРОТИВ ПАКЕТА ═══

  ВЫДУМАН МЕТОД                          ВЫДУМАН ПАКЕТ
  ───────────────────────────────        ───────────────────────────────
  prisma.order.findManyWithItems()       npm i fastify-prisma-pagination
  → красное подчёркивание в IDE          → имя свободно: установка упала     (повезло)
  → ошибка типов при сборке              → имя занято: установка прошла      [!error]
  → падение при первом запуске           → install-скрипты уже выполнились   [!error]
  → вы узнали об этом за 5 секунд        → чужой код в проекте, в CI, в обра-
                                            зе Docker и на сервере сборки

  ВЫВОД
    выдуманный метод — это опечатка, которую находит машина
    выдуманный пакет — это команда, которую машина послушно выполняет


═══ ЧТО ПРОВЕРИТЬ ДО УСТАНОВКИ: ПЯТЬ ДЕЙСТВИЙ ПО ПОЛМИНУТЫ ═══

  1. СУЩЕСТВУЕТ ЛИ ВООБЩЕ
       npm view fastify-prisma-pagination
       настоящий пакет  → описание, версия, автор, ссылки
       выдуманный       → npm error code E404   и   404 Not Found

  2. КОГДА ПОЯВИЛСЯ И КОГДА ОБНОВЛЯЛСЯ
       npm view express time.created        → 2010-12-29...
       npm view express time.modified       → дата последнего релиза
       имя «популярной библиотеки», созданное на прошлой неделе → стоп

  3. СКОЛЬКО ЕГО СКАЧИВАЮТ  (штатной команды нет, спрашиваем API реестра)
       https://api.npmjs.org/downloads/point/last-week/express
       для несуществующего имени тот же адрес вернёт
       {"error":"package ... not found"}

  4. ЧТО НА СТРАНИЦЕ ПАКЕТА
       • репозиторий указан и открывается, в нём есть коммиты и issues
       • лицензия указана
       • README не из одного абзаца, есть история версий
       • сопровождающих больше одного — необязательно, но приятно

  5. ЗДРАВЫЙ СМЫСЛ
       задача типовая (пагинация, даты, валидация), а библиотека молодая
       и малоизвестная → почти наверняка вы смотрите не туда


═══ ЧЕГО ЭТИ ПРОВЕРКИ НЕ ДЕЛАЮТ ═══

  • число загрузок не доказывает подлинность
      демонстрационный пустой пакет huggingface-cli скачали 30 000+ раз
      за три месяца (эксперимент Lasso Security, The Register, март 2024)
  • «свежий» не равно «вредоносный» — возрастные настройки снижают риск,
      но это не детектор вредоносного кода
  • просьба к самому помощнику «перепроверь себя» проверкой не является


═══ ЗАДАЧА, КОТОРАЯ ПРИВОДИТ ЛИШНИЕ ЗАВИСИМОСТИ ═══

  «Сделай постраничный вывод заказов в ручке GET /api/orders»

  ЧТО ЗДЕСЬ НЕ СКАЗАНО
    • можно ли ставить новые пакеты
    • что уже есть в проекте для этой задачи
    • кто принимает решение о зависимости

  ЧТО ПРИДЁТ
    • новая библиотека для пагинации                                      [!error]
    • ещё одна для валидации query-параметров                             [!error]
    • команда установки, которую вы выполните не глядя                    [!error]


═══ ТА ЖЕ ЗАДАЧА С ОДНОЙ ДОБАВЛЕННОЙ СТРОКОЙ ═══

  «Сделай постраничный вывод заказов в ручке GET /api/orders.
   Стек: Node 22, Fastify 5, Prisma, строгий TypeScript.
   Новые зависимости не добавляй — используй только то, что уже
   есть в package.json. Если считаешь, что без новой библиотеки
   не обойтись, напиши об этом отдельной строкой и НЕ устанавливай её.»

  ЧТО ИЗМЕНИЛОСЬ
    добавлено одно ограничение → закрыт целый класс проблем
    молчаливая установка превратилась в предложение, которое вы читаете

  ЧТО ПРИДЁТ ТЕПЕРЬ
    • take / skip средствами Prisma, без библиотек
    • при желании — строка «для валидации query удобнее было бы X,
      но я её не ставил, решайте сами»


═══ ВЫДУМАННЫМИ БЫВАЮТ НЕ ТОЛЬКО ПАКЕТЫ ═══

  ОБЪЕКТ                ЧЕМ ОПАСЕН                      ЧЕМ ПРОВЕРЯТЬ
  ──────────────────    ────────────────────────────    ──────────────────────
  метод библиотеки      падает сразу                    запуск, типы
  флаг команды          обычно ошибка запуска           справка команды
  поле конфигурации     МОЛЧА игнорируется              [!error]  документация версии
  переменная окружения  МОЛЧА не читается               [!error]  документация версии
  имя пакета            устанавливается                 [!error]  npm view до установки

  Запуск проверяет не всё: конфиг с выдуманным полем даёт зелёную сборку
  и ничего не настраивает. Такие вещи ищут только в документации.
`;

  protected readonly confidence = `

═══ ОДИН И ТОТ ЖЕ ТОН, ДВА РАЗНЫХ ОТВЕТА ═══

  ОТВЕТ А  (верный)
    «Prisma умеет логировать запросы: передайте в конструктор PrismaClient
     массив log со значением 'query'. Вывод пойдёт в stdout.»

  ОТВЕТ Б  (выдуман целиком)
    «Начиная с версии 5.8 в Prisma появился параметр slowQueryThresholdMs.
     По умолчанию он равен 500. Раньше он назывался logSlowQueries,
     старое имя работает для обратной совместимости.»               [!error]

  НАЙДИТЕ РАЗНИЦУ В ИНТОНАЦИИ
    её нет. И там и там: конкретика, числа, историческая справка.
    Ответ Б выглядит ДОСТОВЕРНЕЕ, потому что деталей больше.

  ПОЧЕМУ ТАК
    текст не несёт индикатора достоверности
    уверенность — не переменная, а единственный доступный режим изложения
    человек же читает уверенность как признак компетентности


═══ ЛЕСТНИЦА ПРОВЕРОК: СНИЗУ ВВЕРХ, ПО ЦЕНЕ ═══

  ступень 5   ЧИТАТЬ ИСХОДНИКИ БИБЛИОТЕКИ            ~ 30 минут
              ────────────────────────────────────
              только когда документация противоречит поведению

  ступень 4   ПРОГНАТЬ ТЕСТЫ                          ~ 1-5 минут
              ────────────────────────────────────
              ловит поведение, а не наличие; нужен готовый тест

  ступень 3   ЗАПУСТИТЬ МАЛЕНЬКИЙ КУСОК               ~ 1 минута
              ────────────────────────────────────
              отдельный файл, три строки, один вызов — не вся система

  ступень 2   ПРОЧИТАТЬ СИГНАТУРУ В ТИПАХ             ~ 20 секунд
              ────────────────────────────────────
              переход к определению; какие параметры на самом деле

  ступень 1   ПРОВЕРИТЬ, ЧТО ЭТО ВООБЩЕ СУЩЕСТВУЕТ    ~ 1 секунда
              ────────────────────────────────────
              автодополнение в IDE · npm view имя · справка команды ·
              поиск поля в документации своей версии

  ПРАВИЛО
    начинать всегда со ступени 1
    девять случаев из десяти там же и заканчиваются


═══ КОГДА НАДО ПОДНИМАТЬСЯ ВЫШЕ: ПРИЗНАКИ ═══

  • библиотека или версия очень свежая          → знаний о ней может не быть
  • в ответе точные числа и даты                → «5.8», «по умолчанию 500»
  • редкая комбинация технологий                → мало настоящих примеров
  • фраза «в новой версии это делается так»     → это и есть граница знаний
  • ответ слишком удобно совпал с желанием      → проверять хочется меньше
                                                   всего, а нужно — больше


═══ ЧТО СПРАШИВАТЬ ВМЕСТО «ТЫ УВЕРЕН?» ═══

  ПЛОХО
    «Ты уверен, что такой параметр есть?»
    ЧТО ПОЛУЧИТСЯ
      ответ на этот вопрос — такой же сгенерированный текст            [!error]
      на любое сомнение модель охотно соглашается и меняет ответ       [!error]
      получаются качели, а не проверка                                 [!error]

  ЛУЧШЕ
    «Назови точный раздел документации Prisma, где описан этот параметр,
     и версию, в которой он появился. Если не уверен — так и напиши,
     не подбирай правдоподобное имя.»
    ЧТО ДЕЛАТЬ ДАЛЬШЕ
      открыть страницу самому → ссылки тоже бывают выдуманными
      ценность источника не в том, что его назвали, а в том,
      что вы его прочитали

  ЕЩЁ ЛУЧШЕ (когда речь о поведении, а не о факте)
    «Напиши маленький тест на Vitest, который проверяет ровно это
     поведение. Я его запущу.»
    → проверка переезжает из разговора в код, а код не умеет быть
      уверенным в том, чего нет


═══ ЧТО СЧИТАЕТСЯ ДОКАЗАТЕЛЬСТВОМ ═══

  НЕ доказательство                    Доказательство
  ─────────────────────────────        ─────────────────────────────
  уверенный абзац                      выполнившийся код
  «это стандартный подход»             зелёный тест
  «я проверил» от модели               вывод команды у вас в терминале
  названная ссылка                     открытая и прочитанная страница
  число в ответе                       то же число в документации версии
`;

  protected readonly reviewing = `

═══ ФРАГМЕНТ, КОТОРЫЙ ПРИШЁЛ В ОТВЕТ НА «СДЕЛАЙ РУЧКУ ИСТОРИИ ЗАКАЗОВ» ═══

  --- начало файла src/routes/orders.ts ---

  01  import Fastify from 'fastify';
  02  import { PrismaClient } from '@prisma/client';
  03  import dayjs from 'dayjs';                                        [!error]
  04
  05  const prisma = new PrismaClient();                                [!error]
  06  const app = Fastify();
  07
  08  app.get('/api/orders', async (req: any, reply: any) => {          [!error]
  09    const userId = req.query.userId;                                [!error]
  10    const limit = req.query.limit;                                  [!error]
  11
  12    try {
  13      const orders = await prisma.$queryRawUnsafe(
  14        'SELECT * FROM orders WHERE user_id = ' + userId +
  15        ' ORDER BY created_at DESC LIMIT ' + limit                   [!error]
  16      );
  17
  18      const result = [];
  19      for (const order of orders as any[]) {
  20        const items = await prisma.orderItem.findMany({
  21          where: { orderId: order.id }
  22        });                                                         [!error]
  23        const user = await prisma.user.findUnique({
  24          where: { id: order.user_id }
  25        });
  26        result.push({ ...order, items, user });                     [!error]
  27      }
  28
  29      console.log('orders for', userId, process.env.DATABASE_URL);  [!error]
  30      reply.send(result);
  31    } catch (e) {
  32      reply.send([]);                                               [!error]
  33    }
  34  });

  --- конец файла ---

  Код форматирован, названия осмысленные, комментариев нет, но и не надо.
  Он выглядит нормально. В нём шесть проблем разной цены.


═══ ЧИТАЕМ В ПОРЯДКЕ УБЫВАНИЯ ЦЕНЫ ОШИБКИ ═══

  1. ГРАНИЦЫ — что приходит снаружи и что уходит наружу
     строки 09-10   userId и limit взяты из query как есть:
                    не проверено, что userId вообще прислали
                    не проверено, что limit — число и что оно разумное
                    не проверено, что это заказы ИМЕННО этого пользователя
     строка 26      наружу уходит объект user целиком — вместе с полями,
                    которые клиенту видеть незачем
     цена           утечка данных и чужие заказы в чужом кабинете

  2. ОШИБКИ — что будет, когда что-то сломается
     строки 31-32   любое исключение превращается в пустой массив
                    и статус «всё хорошо»
                    пользователь увидит «у вас нет заказов»
                    мониторинг не увидит ничего
     цена           неправильное поведение молча, неделями

  3. ДАННЫЕ — сколько это стоит под нагрузкой
     строки 19-27   два запроса к базе внутри цикла по заказам
                    10 заказов = 21 запрос, 1000 заказов = 2001 запрос
     строка 14      SELECT * тянет все колонки, включая ненужные
     нет транзакции — здесь не критично, в записи было бы критично
     цена           падение под нагрузкой и счёт за базу

  4. БЕЗОПАСНОСТЬ
     строки 13-15   запрос собран склейкой строк вместо параметров:
                    значение userId попадает прямо в текст SQL
     строка 29      в лог уходит строка подключения к базе — это секрет
     цена           доступ к данным и к базе целиком

  5. СОГЛАШЕНИЯ ПРОЕКТА
     строка 08      any в сигнатуре — в проекте any запрещён
     строки 05,19   прямое обращение к PrismaClient в обход слоя
                    репозиториев — в проекте так не пишут
     цена           код чужой в своём репозитории, его дороже менять

  6. ЛИШНЕЕ, О ЧЁМ НЕ ПРОСИЛИ
     строка 03      импорт dayjs, который нигде не используется —
                    новая зависимость приехала сама
     цена           мелочь, но именно из таких мелочей растёт package.json


═══ ПОЧЕМУ ИМЕННО ТАКОЙ ПОРЯДОК ═══

  утечка данных     не откатывается правкой кода          → читать первым
  молчащая ошибка   обнаруживается через недели           → вторым
  нагрузка          видна по метрикам, чинится позже      → третьим
  соглашения        чинятся рефакторингом                 → в конце
  лишний импорт     удаляется одной строкой               → в конце

  Обратный порядок — самый частый способ потратить ревью впустую:
  полчаса на обсуждение имён переменных и ноль минут на строку 14.


═══ ЗАДАЧА НА ИСПРАВЛЕНИЕ, ПОСТАВЛЕННАЯ НОРМАЛЬНО ═══

  «Перепиши обработчик GET /api/orders. Стек: Node 22, Fastify 5,
   Prisma, строгий TypeScript, any запрещён.
   Требования:
     1. userId бери из проверенной сессии, а не из query;
        limit — число от 1 до 100, значение по умолчанию 20.
     2. Запросы к базе — только через OrderRepository, который уже есть
        в src/repositories/order.repository.ts. Запросы в цикле запрещены:
        товары заказов забирай одним запросом.
     3. Наружу отдавай только поля id, createdAt, total, items —
        объект пользователя не отдавай.
     4. Ошибку не глотай: логируй без секретов и отвечай 500.
     5. Новых зависимостей не добавляй.
   Готово, когда проходит тест tests/orders.route.test.ts.»
`;

  protected readonly antipatterns = `

═══ ДЕВЯТЬ СПОСОБОВ ГАРАНТИРОВАННО ПОЛУЧИТЬ МУСОР ═══

  1. ЗАДАЧА БЕЗ СТЕКА И ВЕРСИЙ
     как звучит   «сделай форму входа»
     что ломается достраивается САМОЕ ВЕРОЯТНОЕ, а не ваше:        [!error]
                  React с классовым компонентом вместо Angular 22
     лечение      «Angular 22, standalone, сигналы, строгий TypeScript» —
                  первой строкой задачи

  2. «УЛУЧШИ» БЕЗ КРИТЕРИЯ
     как звучит   «оптимизируй этот компонент», «сделай код чище»
     что ломается «лучше» не свойство кода, а отношение к цели;
                  выберут самое заметное — косметику                [!error]
     лечение      «убрать повторный расчёт корзины при каждом рендере,
                  поведение не менять»

  3. ВЕСЬ ПРОЕКТ В КОНТЕКСТ
     как звучит   «вот репозиторий, разберись»
     что ломается важное тонет среди лишнего, вы платите за объём   [!error]
                  и упираетесь в предел контекстного окна
     лечение      3-5 файлов: который правим, чей контракт берём,
                  один соседний того же типа как образец стиля

  4. ОГРОМНАЯ ЗАДАЧА ОДНИМ КУСКОМ
     как звучит   «сделай личный кабинет целиком»
     что ломается отклонение в начале утаскивает весь остальной код [!error]
                  и 800 строк невозможно принять внимательно        [!error]
     лечение      резать на шаги, каждый заканчивается проверяемым
                  результатом; принимать по одному

  5. ПЕРЕСКАЗ ОШИБКИ ВМЕСТО ЕЁ ТЕКСТА
     как звучит   «падает с ошибкой про undefined»
     что ломается выброшено ровно то, что решает задачу: файл,      [!error]
                  строка, стек; плюс навязана ваша гипотеза
     лечение      текст ошибки целиком, как есть, со стеком
                  и командой, которой вы её получили

  6. «НЕ РАБОТАЕТ» КАК ЕДИНСТВЕННАЯ ОБРАТНАЯ СВЯЗЬ
     как звучит   «нет, не то», «всё равно ошибка»
     что ломается нулевая информация → начнут гадать и перепишут    [!error]
                  всё заново, потеряв уже верную часть              [!error]
     лечение      что запустил · что ожидал · что получил ·
                  «остальное не трогай, поменяй только X»

  7. ДОВЕРИЕ К ПРАВДОПОДОБНОМУ БЕЗ ПРОВЕРКИ
     как звучит   никак — это тишина, отсутствие действия
     что ломается уверенный тон подменяет проверку; выдуманный
                  метод упадёт (дёшево), выдуманный пакет
                  установится (дорого)                              [!error]
     лечение      одна секунда: существует ли названное —
                  пакет, метод, флаг, поле конфигурации

  8. РАЗРЕШЕНИЕ ДОБАВЛЯТЬ ЛЮБЫЕ ЗАВИСИМОСТИ
     как звучит   отсутствие запрета — вы просто не сказали «нельзя»
     что ломается добавить библиотеку — самый лёгкий путь к ответу; [!error]
                  приедет дубль того, что в проекте уже есть,
                  или имя, которого вчера не существовало           [!error]
     лечение      «новые зависимости не добавляй; если без новой
                  никак — напиши отдельной строкой и не ставь»

  9. ПРИНЯТИЕ КОДА, КОТОРЫЙ ВЫ НЕ МОЖЕТЕ ОБЪЯСНИТЬ
     как звучит   «ну, тесты же зелёные»
     что ломается отлаживать это придётся вам, ночью, и помощник    [!error]
                  не помнит своей прошлой сессии
     лечение      не можете объяснить строку — попросите объяснить,
                  перечитайте и решите заново


═══ СВОДКА: ЧТО ИМЕННО НЕ ПОПАЛО В ТЕКСТ ЗАДАЧИ ═══

  №  антипаттерн                       недостающая информация
  ─  ───────────────────────────────   ─────────────────────────────────────
  1  без стека                         версия фреймворка и стиль
  2  «улучши»                          цель правки и критерий приёмки
  3  весь проект                       какие именно файлы важны
  4  одним куском                      границы шага
  5  пересказ ошибки                   точный текст и стек
  6  «не работает»                     ожидание, факт, место расхождения
  7  без проверки                      подтверждение существования
  8  любые зависимости                 запрет и правило обсуждения
  9  необъяснимый код                  понимание, то есть ваша работа

  Все девять — про одно: информация была у вас в голове
  и не попала в текст. Помощник видит только текст и файлы,
  которые вы дали посмотреть.


═══ ДЕСЯТЫЙ ПУНКТ, ЧЕСТНОСТИ РАДИ: ПЕРЕБОР ═══

  как звучит   двадцать минут оформления задачи с контекстом,
               критериями и ограничениями — ради переименования поля
  что ломается процедура стоит дороже результата; приём перестал работать
  лечение      сделаете сами быстрее, чем опишете, — делайте сами

  ОРИЕНТИР
    правка в одну строку      → одна фраза или вообще руками
    функция или компонент     → 5-6 строк: стек, критерий, ограничения
    фича на несколько файлов  → сначала план, потом шаги, приёмка по шагам
`;

protected readonly templates = `

═══ КАК ЧИТАТЬ ЭТИ ЗАГОТОВКИ ═══

  <в угловых скобках>                 — место, которое заполняете вы
  --- начало путь --- / --- конец --- — границы вставленного вами кода
  пункт, который к вашей задаче не относится, просто удаляйте

  Стек в примерах — тот самый интернет-магазин чая: Angular 22 и сигналы
  на фронте, Node.js с Fastify, PostgreSQL и Prisma на бэке, Vitest
  для тестов. Подставьте свой: версии важнее всего остального текста.


═══ ЗАГОТОВКА 1. НОВАЯ ФУНКЦИЯ ИЛИ КОМПОНЕНТ ═══

  Стек: Angular 22, standalone-компоненты, сигналы, строгий TypeScript, SCSS.
  Тесты: Vitest. Пакетный менеджер: npm.

  Задача: <одно предложение — что должно появиться>

  Вход:  <какие данные приходят и откуда: пропсы, сервис, роутер>
  Выход: <что видит пользователь и что возвращает код наружу>

  Ограничения:
    • новых зависимостей не добавлять
    • только standalone-компоненты и сигналы, классовых компонентов нет
    • any запрещён, включая "as any"
    • тексты для пользователя — по-русски

  Контекст, на который надо опираться:
    --- начало src/app/cart/cart.store.ts ---
    <сюда я вставляю интерфейс стора: поля и методы, без реализации>
    --- конец ---

  Готово, когда: <что я запущу и что увижу: команда + ожидаемый результат>

  Сначала опиши план в 5-7 пунктах, кода не пиши. Я отвечу «пиши».


═══ ЗАГОТОВКА 2. ИСПРАВЛЕНИЕ ОШИБКИ ═══

  Стек: Node.js 22, Fastify 5, PostgreSQL 16, Prisma, строгий TypeScript.

  Что я делаю:    <шаги по порядку, которые приводят к ошибке>
  Что ожидаю:     <какое поведение я считаю правильным>
  Что происходит: <фактическое поведение>

  Текст ошибки целиком, без сокращений:
    --- начало вывода ---
    <сообщение, тип ошибки и стек вызовов>
    --- конец ---

  Код, в котором это происходит:
    --- начало src/orders/order.repository.ts ---
    <функция целиком, а не одна подозрительная строка>
    --- конец ---

  Что я уже проверил и что отпало:
    • <гипотеза 1> — не подтвердилась, потому что <почему>
    • <гипотеза 2> — не подтвердилась, потому что <почему>

  Сначала назови 2-3 вероятные причины и скажи, как каждую проверить.
  Правку не предлагай, пока мы не сойдёмся на причине.


═══ ЗАГОТОВКА 3. РЕФАКТОРИНГ БЕЗ ИЗМЕНЕНИЯ ПОВЕДЕНИЯ ═══

  Стек: <язык и версия, фреймворк и версия>. Файл: <путь>

  Код «как есть»:
    --- начало ---
    <текущий код полностью, вместе с импортами>
    --- конец ---

  Зачем рефакторю: <что конкретно мешает — длина функции, дублирование,
  невозможность написать тест, три вложенных условия>

  Публичный контракт менять НЕЛЬЗЯ:
    • имена и сигнатуры экспортируемых функций остаются те же
    • форма возвращаемых данных остаётся та же
    • тексты ошибок для пользователя остаются те же

  Что менять можно: внутреннее устройство, приватные функции,
  имена локальных переменных, порядок проверок.

  Готово, когда: npm test проходит, и ни один тест я не правил.

  Покажи результат целиком, а под ним — списком: что изменилось и почему.


═══ ЗАГОТОВКА 4. ТЕСТЫ ═══

  Стек: Vitest, строгий TypeScript. Тесты лежат рядом с кодом: *.spec.ts.

  Код, который надо покрыть:
    --- начало src/pricing/discount.ts ---
    <функция полностью>
    --- конец ---

  Правила, которые эта функция обязана соблюдать:
    • <правило 1: скидка никогда не больше 30 процентов>
    • <правило 2: на подарочные наборы скидка не действует>
    • <правило 3: при пустой корзине возвращается ноль, а не ошибка>

  Нужны тесты на:
    • обычный случай
    • границы: ноль, максимум, максимум плюс один
    • пустой вход
    • некорректный вход (отрицательное число, null)

  Моки — только для сети и базы. Внутреннюю логику не мокать.
  Названия тестов — по-русски, в формате «что делаем — что ожидаем».


═══ ЗАГОТОВКА 5. РАЗБОР ЧУЖОГО КОДА ═══

  Я начинающий, объясняй без пропусков и без «как известно».

  Код:
    --- начало ---
    <файл или функция целиком>
    --- конец ---

  Объясни в таком порядке:
    1. что этот код делает — одним предложением
    2. построчно: что происходит и зачем именно так
    3. неочевидные места: почему автор написал это, а не привычный вариант
    4. что сломается, если <изменить конкретную строку>

  Если чего-то нельзя понять без соседних файлов — скажи, какие файлы
  тебе показать. Не догадывайся за них.


═══ ЗАГОТОВКА 6. ОБНОВЛЕНИЕ ЗАВИСИМОСТИ ИЛИ МАЖОРНАЯ МИГРАЦИЯ ═══

  Сейчас: <пакет> <версия>. Хочу: <пакет> <версия>.
  Остальной стек: <версия Node, фреймворка, сборщика>.

  Места, где пакет используется (нашёл поиском по проекту):
    • <путь:строка> — <что там делается>
    • <путь:строка> — <что там делается>

  Сделай по шагам:
    1. перечисли ломающие изменения между моими версиями
    2. отметь, какие из них касаются моих мест использования, а какие нет
    3. для касающихся покажи правку в виде «было → стало»
    4. скажи, что проверить руками после обновления

  Если ты не уверен, что изменение есть именно в этой версии — так и напиши.
  Номера версий и ссылки на список изменений не выдумывай.


═══ ЗАГОТОВКА 7. РЕВЬЮ СОБСТВЕННОГО КОДА ═══

  Стек: <версии>. Соглашения команды: <2-3 пункта, которые у нас жёсткие>

  Мой код:
    --- начало ---
    <изменения или файл целиком>
    --- конец ---

  Посмотри на него как строгий ревьюер и ответь по пунктам:
    • что сломается на границах: пустой массив, null, ноль, очень большое
      число, очень длинная строка
    • где не обработана ошибка и что в этот момент увидит пользователь
    • что здесь лишнее и можно удалить без потери смысла
    • где нарушены мои соглашения

  Файл целиком не переписывай. Дай список замечаний — решать буду я.


═══ МЕЛКАЯ ЗАДАЧА: ТРИ СТРОКИ ВМЕСТО ЗАГОТОВКИ ═══

  Не всякая правка заслуживает пятнадцати строк постановки. Для мелочи
  от заготовки остаётся скелет: стек → результат → как проверю.

  ПЛОХО
    «поправь кнопку в карточке товара»                                  [!error]

  ХОРОШО
    Angular 22, standalone-компоненты, сигналы, строгий TypeScript.
    В CardComponent кнопка «Купить» должна быть disabled, когда товара нет
    в наличии (поле stock равно 0), и получать aria-label
    «Товара нет в наличии».
    Проверю: открою каталог — у товара без остатка кнопка неактивна.

  ПЛОХО
    «почини сортировку, она глючит»                                     [!error]

  ХОРОШО
    Node 22, Fastify 5, Prisma, PostgreSQL 16.
    GET /api/products?sort=price должен отдавать товары по возрастанию цены;
    сейчас при одинаковых ценах порядок каждый раз разный.
    Нужна стабильная сортировка: при равных ценах — по id по возрастанию.
    Проверю: тест в products.spec.ts на двух товарах с ценой 500.


═══ КАК ЭТИМ ПОЛЬЗОВАТЬСЯ ═══

  • заготовка — напоминание, а не анкета: пустые пункты просто выкидывайте
  • для мелкой правки хватает трёх строк:
      стек с версиями → что должно получиться → как я это проверю
  • держите заготовки там, где работаете: сниппет редактора, заметка,
    файл prompts.md в самом репозитории
  • дополняйте своими: каждая задача, которая вернулась мусором, —
    это недостающая строка в вашей заготовке

  ЧТО БУДЕТ, ЕСЛИ ВЫКИНУТЬ ПУНКТ

    убрали версии стека      → ответ по устаревшему API, код не собирается   [!error]
    убрали критерий приёмки  → «готово» непонятно, спорите с моделью словами [!error]
    убрали ограничения       → в проект тихо въезжает новая зависимость      [!error]
    убрали «сначала план»    → 300 строк не туда, и жалко выбрасывать        [!error]
    убрали контекст          → правдоподобный код мимо ваших интерфейсов     [!error]
`;

  protected readonly checklist = `

═══ ЧАСТЬ 1. ОБЯЗАТЕЛЬНО ВСЕГДА (даже для правки в одну строку) ═══

  [ ] 1. НАЗВАН СТЕК С ВЕРСИЯМИ
         язык и версия · фреймворк и мажорная версия · рантайм
         пример: «Angular 22, standalone, сигналы; TypeScript строгий;
                  Node 22, Fastify 5»
         проверка: по моему тексту видно, что это не React и не Angular 15

  [ ] 2. СКАЗАНО, ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ
         результат, а не направление движения
         плохо:  «улучши работу с корзиной»
         хорошо: «при удалении последнего товара корзина показывает пустое
                  состояние с кнопкой "В каталог", а не сумму 0 ₽»

  [ ] 3. СКАЗАНО, КАК Я ЭТО ПРОВЕРЮ
         конкретное действие и конкретный ожидаемый результат
         плохо:  «чтобы работало»
         хорошо: «npm test зелёный; в браузере при пустой корзине вижу текст
                  "Корзина пуста" и кнопку "В каталог"»


═══ ЧАСТЬ 2. ДЛЯ ЗАДАЧ КРУПНЕЕ ОДНОЙ ФУНКЦИИ ═══

  [ ] 4. ПОКАЗАН НУЖНЫЙ КОНТЕКСТ — И ТОЛЬКО НУЖНЫЙ
         типы и интерфейсы, которые придётся использовать
         один соседний файл того же типа — как образец стиля
         фрагмент package.json, если важны версии
         НЕ показываю: весь репозиторий, чужие модули, секреты и ключи

  [ ] 5. НАЗВАНЫ ОГРАНИЧЕНИЯ
         чего нельзя трогать: файлы, публичные сигнатуры, схема базы
         чем нельзя пользоваться: новые пакеты, any, классовые компоненты
         что обязано остаться: тексты ошибок, формат ответа API

  [ ] 6. ЗАПРОШЕН ПЛАН ДО КОДА
         «сначала план в 5-7 пунктах, кода не пиши»
         план читается за минуту, а 300 строк не туда — за полчаса

  [ ] 7. ЗАДАЧА РАЗБИТА НА ШАГИ
         каждый шаг заканчивается чем-то, что можно запустить и проверить
         следующий шаг ставлю только после приёмки предыдущего


═══ ЧАСТЬ 3. ПОСЛЕ ОТВЕТА, ДО КОММИТА ═══

  [ ] 8.  ПРОЧИТАЛ ГРАНИЦЫ И ОБРАБОТКУ ОШИБОК
          пустой массив · null и undefined · ноль · отрицательное число
          что произойдёт, если запрос к базе упадёт
          что увидит пользователь, а не что увидит консоль

  [ ] 9.  ПРОВЕРИЛ КАЖДУЮ НОВУЮ ЗАВИСИМОСТЬ И КАЖДЫЙ НЕЗНАКОМЫЙ МЕТОД
          пакет существует · автор тот, о ком я думаю · версия живая
          метод есть в документации МОЕЙ версии, а не вообще где-то

  [ ] 10. ПРОГНАЛ ТЕСТЫ САМ
          «тесты проходят» в ответе — это не результат запуска
          запускаю линтер и сборку, а не только тесты

  [ ] 11. МОГУ ОБЪЯСНИТЬ КАЖДУЮ СТРОКУ ВСЛУХ
          если строку объяснить не могу — она не едет в мой коммит
          спрашиваю про неё отдельно или пишу сам


═══ ЧАСТЬ 4. КРАСНЫЕ ФЛАГИ — НЕ ПРИМЕНЯТЬ ═══

  незнакомый пакет, которого не было в задаче                         [!error]
  код, который я не понимаю, но «вроде работает»                      [!error]
  правки в файлах, о которых я не просил                              [!error]
  «тесты проходят», хотя я их не запускал                             [!error]
  метод фреймворка, которого нет в документации моей версии           [!error]
  молчаливое "as any", чтобы TypeScript перестал ругаться             [!error]
  ответ, который начинается со слов «в целом это можно сделать так»   [!error]


═══ ЧЕК-ЛИСТ НА ЖИВОЙ ЗАДАЧЕ ═══

  ЧЕРНОВИК В ГОЛОВЕ
    «надо бы сделать фильтр по сорту чая в каталоге»                    [!error]

  ПРОГОН ПО ЧАСТИ 1
    стек?      → Angular 22, standalone, сигналы, строгий TypeScript
    результат? → над списком каталога выпадающий список сортов; выбор
                 фильтрует товары без перезагрузки страницы
    проверка?  → выбираю «улун» — в списке только улуны, счётчик над
                 списком показывает их количество

  ПРОГОН ПО ЧАСТИ 2 (задача трогает три файла — значит, часть нужна)
    контекст?    → интерфейс Product, файл catalog.store.ts и соседний
                   компонент фильтра по цене как образец стиля
    ограничения? → новых пакетов не ставить, запросы к базе только через
                   слой репозиториев, any запрещён
    план?        → «сначала план в 5-7 пунктах, кода не пиши»
    шаги?        → 1) тип фильтра и его состояние в сторе
                   2) компонент выпадающего списка
                   3) подключение к каталогу и счётчик найденного

  ЧТО ПОЛУЧИЛОСЬ
    было: восемь слов, из которых невозможно собрать код
    стало: поставленная задача, результат которой можно принять или
           отклонить, не споря о словах


═══ ЕСЛИ ЗАПОМНИТЬ ТОЛЬКО ОДНО ═══

  Перед Enter спросите себя: «как я проверю, что это готово?»

  Если ответа нет — задача ещё не поставлена, и любой ответ модели
  будет формально подходящим. Если ответ есть — вы почти наверняка
  уже назвали и стек, и результат: без них проверку не сформулировать.
`;
}
