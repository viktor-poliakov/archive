import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-infra-docker',
  imports: [CodeBlock, RouterLink],
  templateUrl: './docker.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemInfraDocker {
  protected readonly whyContainers = `# ═══ ОДНА И ТА ЖЕ КОМАНДА НА ТРЁХ МАШИНАХ ═══
# Проект один: интернет-магазин shop. Репозиторий один, ветка одна,
# команды набраны буква в букву одинаково. Дальше начинается расхождение.

# --- МАШИНА 1. Ноутбук разработчика: macOS, Node 22 ---
node --version
# v22.x
npm ci
npm run test
# Все тесты зелёные. Разработчик говорит: «у меня работает».

# --- МАШИНА 2. Ноутбук коллеги: Windows, Node 18 ---
node --version
# v18.x
npm ci
npm run test
# SyntaxError: Unexpected token
#   в коде использован синтаксис, которого в Node 18 ещё нет.
# И вторая беда, уже не про Node:
#   тест падает на пути к файлу, потому что в коде написано
#   'uploads/products/1.jpg', а Windows отдаёт 'uploads\\products\\1.jpg'.

# --- МАШИНА 3. Боевой сервер: Ubuntu, Node 20 ---
node --version
# v20.x
npm ci
# npm ERR! sharp: Cannot find module '../build/Release/sharp.node'
#   пакет для обработки картинок товаров собирается ПОД СИСТЕМУ.
#   Собранная на macOS версия на Linux не запускается вообще.
npm run start
# Error: libvips.so.42: cannot open shared object file
#   а это уже даже не Node. Это СИСТЕМНАЯ библиотека, которой
#   на сервере просто нет. На ноутбуке она приехала вместе с Homebrew,
#   и никто никогда не замечал, что она вообще нужна.

# ═══ ЧТО ИМЕННО РАЗЪЕХАЛОСЬ ═══
# 1. Версия Node — три разные.
# 2. Операционная система — три разные (пути, права, кодировки).
# 3. Системные библиотеки (libvips, libssl, шрифты, часовые пояса) —
#    они НЕ лежат в вашем репозитории и НЕ ставятся через npm ci.
# 4. Переменные окружения и локальные настройки, о которых все забыли.
#
# package.json фиксирует только четвёртую часть картины — пакеты из npm.
# Всё остальное — «как повезёт на этой машине».

# ═══ ЧТО ДЕЛАЕТ КОНТЕЙНЕР ═══
# Контейнер запечатывает НЕ ТОЛЬКО ваш код, а всё окружение вокруг него:
#   - конкретную версию Node,
#   - конкретный дистрибутив Linux,
#   - конкретные системные библиотеки,
#   - файлы, пути, пользователя, переменные окружения.
#
# Это описывается ТЕКСТОВЫМ файлом (Dockerfile), который лежит рядом с кодом
# в том же репозитории. Из него собирается ОБРАЗ (image) — неизменяемый слепок.
# Запущенный экземпляр образа называется КОНТЕЙНЕР (container).
#
# Образ — как установочный диск. Контейнер — как запущенная с него программа.
# Из одного образа можно запустить хоть двадцать контейнеров.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# Docker НЕ делает следующего, и это важно понимать сразу:
# 1. Он не чинит кривой код. Гонка в вашей логике переедет в контейнер как есть.
# 2. Он не даёт полной изоляции безопасности. Контейнеры на одной машине
#    делят ОДНО ядро Linux. Это не виртуальная машина.
# 3. Он не бесплатен для мозга: появляется второй слой, в котором тоже
#    надо уметь отлаживать. «Почему не видно файл» теперь имеет два ответа —
#    «его нет на диске» и «он есть, но не попал в контейнер».
# 4. На macOS и Windows Linux-контейнеры крутятся внутри маленькой
#    виртуальной машины, поэтому файловые операции там заметно медленнее,
#    чем на Linux. Это нормально и это надо закладывать в ожидания.`;

  protected readonly firstDockerfile = `# ═══ ПЕРВЫЙ DOCKERFILE. Он рабочий — и он плохой ═══
# Файл называется ровно "Dockerfile" (без расширения), лежит в корне проекта.
# Читается СВЕРХУ ВНИЗ, каждая инструкция выполняется по очереди.

# FROM — с чего начинаем. Это чужой готовый образ из реестра Docker Hub.
# Внутри уже есть Linux и установленный Node. Мы не ставим Node сами.
FROM node

# WORKDIR — рабочая папка ВНУТРИ контейнера. Если её нет — будет создана.
# Все следующие команды выполняются относительно неё.
# Без WORKDIR вы окажетесь в корне файловой системы контейнера — так не делают.
WORKDIR /app

# COPY <что с моей машины> <куда внутрь контейнера>
# Точка слева — это «всё из папки сборки». Точка справа — «в /app».
COPY . .

# RUN — выполнить команду ВО ВРЕМЯ СБОРКИ образа.
# Результат команды остаётся внутри образа навсегда.
RUN npm install

# EXPOSE — это ДОКУМЕНТАЦИЯ, а не открытие порта.
# Строка говорит читателю: «программа внутри слушает 3000».
# Порт наружу открывает не она, а флаг -p при запуске (см. пример с командами).
EXPOSE 3000

# CMD — что запустить, когда контейнер СТАРТУЕТ (а не когда собирается).
# Форма списком, а не строкой: так команда запускается напрямую,
# без промежуточной оболочки, и сигналы остановки доходят до Node.
CMD ["node", "server.js"]

# ═══ ЧТО С НИМ НЕ ТАК. Пять бед, и все пять вылезут ═══

# БЕДА 1. ОБРАЗ РАЗМЕРОМ С ГИГАБАЙТ.
#   Образ "node" без уточнения — полный дистрибутив Debian со всем набором:
#   компиляторы, git, пакетные менеджеры, документация. Порядок величины —
#   около гигабайта против десятков мегабайт у аккуратно собранного образа.
#   Этот гигабайт вы будете качать на каждый сервер при каждой выкладке.

# БЕДА 2. ПРАВКА ОДНОЙ БУКВЫ = ПОЛНАЯ ПЕРЕУСТАНОВКА ЗАВИСИМОСТЕЙ.
#   COPY . . стоит ПЕРЕД npm install. Поменяли пробел в src/routes/orders.js —
#   слой COPY изменился, а значит всё, что ниже, собирается заново,
#   включая npm install. Минуты ожидания вместо секунд. Разбор — в примере
#   про слои и кэш, это самая частая ошибка новичка.

# БЕДА 3. ЗАПУСК ОТ ROOT.
#   По умолчанию процесс внутри контейнера — суперпользователь.
#   Дыра в загрузке картинок товара превращается из «испортили файл»
#   в «полный контроль над содержимым контейнера».

# БЕДА 4. FROM node БЕЗ ВЕРСИИ = ПЛАВАЮЩИЙ latest.
#   Сегодня там Node 22, через полгода Node 24. Соберёте тот же коммит
#   через полгода — получите ДРУГОЙ образ. Воспроизводимость, ради которой
#   всё и затевалось, потеряна ровно в первой строке.

# БЕДА 5. ВНУТРЬ УЕХАЛО ВСЁ ПОДРЯД.
#   COPY . . забрал .git со всей историей, тесты, README, .env с паролями,
#   локальную папку node_modules с вашего ноутбука (собранную под другую ОС —
#   и она перезапишет правильную). Лечится файлом .dockerignore.

# Дальше по шагам чиним каждую беду. Начнём с самой дорогой по времени — с кэша.`;

  protected readonly layerCache = `# ═══ ГЛАВНАЯ ИДЕЯ: ОБРАЗ — ЭТО СТОПКА СЛОЁВ ═══
# Почти каждая инструкция Dockerfile создаёт СЛОЙ — застывший снимок
# изменений файловой системы. Слои складываются друг на друга стопкой.
#
# Docker кэширует слои. При пересборке он идёт сверху вниз и на каждом шаге
# спрашивает: «входные данные этого шага изменились?»
#   НЕТ  -> берёт готовый слой из кэша, шаг не выполняется вообще (мгновенно).
#   ДА   -> выполняет шаг заново И ВСЕ СЛЕДУЮЩИЕ ТОЖЕ, без разбора.
#
# Последнее — ключевое. Кэш ломается не точечно, а «отсюда и до конца».
# Поэтому ПОРЯДОК СТРОК в Dockerfile — это не стиль, это скорость сборки.

# ═══ ВАРИАНТ А. КАК ПИШУТ СНАЧАЛА (медленно) ═══
FROM node:22-slim
WORKDIR /app

COPY . .              # слой 1: сюда попал ВЕСЬ код магазина
RUN npm ci            # слой 2: установка зависимостей
CMD ["node", "server.js"]

# Правим одну строку в src/routes/orders.js и пересобираем:
#   слой 1 (COPY . .)  -> код изменился -> ПЕРЕСОБИРАЕТСЯ
#   слой 2 (npm ci)    -> стоит ниже сломанного -> ПЕРЕСОБИРАЕТСЯ ТОЖЕ,
#                         хотя package.json не тронут ни на байт.
# Итог: полная переустановка зависимостей на каждую правку кода.
# Порядок времени — минуты. И так десятки раз в день.

# ═══ ВАРИАНТ Б. КАК НАДО (быстро) ═══
FROM node:22-slim
WORKDIR /app

# Сначала копируем ТОЛЬКО описание зависимостей — два маленьких файла.
# Они меняются редко: когда добавляют или обновляют пакет.
COPY package.json package-lock.json ./

# Установка стоит СРАЗУ ПОСЛЕ них, до всего остального кода.
# npm ci (а не npm install) ставит строго то, что записано в lock-файле:
# без сюрпризов и без правки lock-файла на ходу.
RUN npm ci

# И только теперь — весь остальной код. Он меняется по сто раз в день,
# но он НИЖЕ установки, поэтому её не задевает.
COPY . .

CMD ["node", "server.js"]

# Правим ту же строку в src/routes/orders.js и пересобираем:
#   FROM                       -> из кэша, мгновенно
#   COPY package*.json         -> файлы не менялись -> из кэша, мгновенно
#   RUN npm ci                 -> вход не менялся   -> ИЗ КЭША, НЕ ВЫПОЛНЯЕТСЯ
#   COPY . .                   -> код изменился     -> выполняется (доли секунды)
# Итог: секунды вместо минут. Разница видна в выводе сборки:

docker build -t shop-api .
# => [2/5] WORKDIR /app                                    CACHED
# => [3/5] COPY package.json package-lock.json ./           CACHED
# => [4/5] RUN npm ci                                       CACHED   <-- вот оно
# => [5/5] COPY . .                                         0.4s

# ═══ КАК DOCKER РЕШАЕТ, «ИЗМЕНИЛСЯ ЛИ ВХОД» ═══
# Для COPY — по содержимому копируемых файлов (их контрольной сумме).
#   Поменялось содержимое хоть одного файла — слой другой.
# Для RUN — ПО ТЕКСТУ КОМАНДЫ, а не по её результату.
#   Это важная ловушка: строка RUN apt-get update даст один и тот же слой
#   из кэша и через год, хотя в интернете пакеты давно обновились.
#   Поэтому apt-get update пишут в одной строке с apt-get install,
#   а не отдельной инструкцией.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Кэш живёт НА МАШИНЕ, где идёт сборка. На чистом сервере сборки
#    (в GitHub Actions каждый запуск начинается с пустого диска) кэша нет,
#    и первая сборка честно долгая. Кэш там надо настраивать отдельно.
# 2. Кэш может ОБМАНУТЬ. Если базовый образ обновился и в нём закрыли
#    уязвимость, ваша сборка всё равно возьмёт старый слой из кэша.
#    Лечится осознанной пересборкой без кэша:
docker build --no-cache -t shop-api .
# 3. Разделение COPY на два шага делает Dockerfile чуть длиннее и требует
#    помнить правило «редко меняющееся — выше». Это осознанная плата
#    за экономию минут на каждой сборке.`;

  protected readonly multiStage = `# ═══ ПРОБЛЕМА: ДЛЯ СБОРКИ И ДЛЯ ЗАПУСКА НУЖНО РАЗНОЕ ═══
# Чтобы СОБРАТЬ приложение магазина, нужны:
#   - devDependencies: TypeScript, сборщик, типы, линтер,
#   - исходники в src/,
#   - иногда компилятор C++ для нативных модулей.
# Чтобы ЗАПУСТИТЬ его, нужны:
#   - собранная папка dist/,
#   - только production-зависимости,
#   - и всё.
#
# Если собирать в один этап, ВСЁ ЛИШНЕЕ останется в итоговом образе навсегда.
# Удалить его потом нельзя: слой, где файл был, уже застыл в стопке.
# rm -rf в следующей строке лишь прячет файлы, но не уменьшает образ.

# ═══ РЕШЕНИЕ: НЕСКОЛЬКО FROM В ОДНОМ ФАЙЛЕ ═══
# Каждый FROM начинает НОВЫЙ этап с чистого листа. Из предыдущего этапа
# можно забрать ровно те файлы, которые нужны, — и больше ничего.

# --- ЭТАП 1. Сборка. Здесь можно мусорить сколько угодно. ---
# "AS build" — имя этапа, чтобы на него сослаться ниже.
FROM node:22-slim AS build
WORKDIR /app

# Зависимости отдельным слоем — правило из примера про кэш работает и здесь.
COPY package.json package-lock.json ./
# Ставим ВСЁ, включая devDependencies: без TypeScript собрать не выйдет.
RUN npm ci

COPY . .
# Компиляция: src/*.ts превращается в dist/*.js
RUN npm run build

# Теперь оставим только production-зависимости.
# npm prune вырезает devDependencies из node_modules прямо здесь,
# в этапе сборки, — чтобы во второй этап уехало уже чистое.
RUN npm prune --omit=dev

# --- ЭТАП 2. Запуск. Начинаем заново, с чистого базового образа. ---
FROM node:22-slim AS runtime
WORKDIR /app
ENV NODE_ENV=production

# COPY --from=build — забрать файлы ИЗ ЭТАПА build.
# Забираем ровно два адреса и ничего больше.
COPY --from=build /app/node_modules ./node_modules
COPY --from=build /app/dist ./dist
COPY package.json ./

CMD ["node", "dist/server.js"]

# ═══ ЧТО ИМЕННО НЕ ПОПАЛО В ИТОГОВЫЙ ОБРАЗ ═══
# Не «удалено», а НИКОГДА ТУДА НЕ ПОПАДАЛО — это разные вещи:
#   - devDependencies (TypeScript, тесты, линтер, типы),
#   - исходники src/ и сами тесты,
#   - кэш npm, который остаётся после установки,
#   - временные файлы компилятора.
# Этап build выбрасывается целиком, как строительные леса после стройки.

# ═══ ПРОВЕРЯЕМ РАЗМЕР ═══
docker build -t shop-api:multi .
docker images shop-api
# Порядок величины: полный образ с исходниками и dev-зависимостями —
# сотни мегабайт; двухэтапный на том же slim-базисе — заметно меньше,
# обычно в разы. Точные цифры зависят от вашего проекта, меряйте у себя.

# ═══ ПОЧЕМУ МАЛЕНЬКИЙ ОБРАЗ — ЭТО НЕ ЭСТЕТИКА ═══
# 1. СКОРОСТЬ ВЫКЛАДКИ. Образ едет по сети на каждый сервер. Лишние сотни
#    мегабайт — это лишние секунды-минуты на каждом узле при каждом релизе.
#    При откате в аварии эти минуты вы будете считать вручную.
# 2. МЕНЬШЕ УЯЗВИМОСТЕЙ. Каждая программа внутри образа — это строка
#    в отчёте сканера безопасности. Компилятор, git и пакетный менеджер
#    внутри боевого образа не нужны никому, кроме атакующего.
# 3. ДЕНЬГИ. Хранение образов в реестре и трафик оплачиваются.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Отлаживать такой образ труднее: внутри нет ни компилятора, ни исходников.
#    Для разбора аварии обычно берут образ этапа build:
docker build --target build -t shop-api:debug .
# 2. Файл длиннее, и надо помнить, что во второй этап нужно перенести ВСЁ
#    необходимое. Забыли скопировать папку с шаблонами писем — узнаете
#    об этом не при сборке, а в бою, когда клиент не получит чек.
# 3. Двухэтапная сборка НЕ уменьшает время сборки. Она уменьшает результат.`;

  protected readonly dockerignore = `# ═══ ФАЙЛ .dockerignore В КОРНЕ ПРОЕКТА, РЯДОМ С Dockerfile ═══
# Работает как .gitignore, но для СБОРКИ образа.
# Что здесь перечислено — не уезжает в контекст сборки и не может попасть
# в образ ни через COPY . ., ни случайно.

# Зависимости с ВАШЕЙ машины. Самый важный пункт списка.
node_modules

# История git целиком.
.git
.gitignore

# Секреты. Их место — в переменных окружения при запуске, не в образе.
.env
.env.*
*.pem
*.key

# Локальные базы и загруженные файлы разработчика.
*.sqlite
*.sqlite3
data/
uploads/

# Результаты прошлых сборок и кэши инструментов.
dist
build
coverage
.cache
.npm

# То, что нужно людям, но не программе.
README.md
docs/
.vscode
.idea
.DS_Store

# Тесты — если итоговый образ их не запускает.
**/*.test.ts
**/*.spec.ts

# Сам Dockerfile и compose-файлы: внутрь образа они не нужны.
Dockerfile*
docker-compose*.yml

# ═══ ЧТО БУДЕТ, ЕСЛИ ЭТОГО ФАЙЛА НЕТ ═══

# 1. node_modules С ЧУЖОЙ ПЛАТФОРМЫ.
#    COPY . . притащит вашу локальную папку node_modules внутрь и ПЕРЕЗАПИШЕТ
#    ту, что честно собралась в контейнере. Нативные модули (sharp для
#    картинок товаров, драйверы баз) собраны под macOS и на Linux не запустятся.
#    Симптом: собралось без единой ошибки, а при старте
#    "invalid ELF header" или "Cannot find module '../build/Release/...'".

# 2. .git СО ВСЕЙ ИСТОРИЕЙ.
#    Это не только лишние сотни мегабайт. Это ещё и все старые коммиты —
#    включая тот, где полгода назад случайно закоммитили ключ платёжного
#    сервиса и «удалили» его следующим коммитом. В истории он остался,
#    и теперь он лежит внутри образа, который вы отдаёте наружу.

# 3. .env С ПАРОЛЯМИ.
#    Пароль к базе магазина и ключ платежей уезжают в образ. Любой,
#    у кого есть образ, читает их одной командой.

# 4. ЛОКАЛЬНЫЕ БАЗЫ И ЗАГРУЗКИ.
#    Ваш dev.sqlite с тестовыми заказами и папка uploads с картинками —
#    это лишний вес и, если там реальные данные, утечка.

# ═══ КАК ПОСМОТРЕТЬ, ЧТО РЕАЛЬНО ПОПАЛО В КОНТЕКСТ ═══
# Первая строка вывода сборки показывает размер контекста — это ровно то,
# что docker забрал с вашего диска перед сборкой:
docker build -t shop-api .
# => [internal] load build context
# => => transferring context: 412.3MB     <-- сотни мегабайт = что-то не так

# Проверить состав можно, собрав крошечный образ и заглянув внутрь:
printf 'FROM busybox\\nCOPY . /ctx\\n' > Dockerfile.ctx
docker build -f Dockerfile.ctx -t ctx-check .
docker run --rm ctx-check du -sh /ctx/* | sort -h | tail -20
# Покажет двадцать самых тяжёлых путей контекста. Обычно там сразу видно
# и node_modules, и .git, и забытую папку с дампами базы.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# .dockerignore ОБРЕЗАЕТ ВХОД, а не чистит образ задним числом. Если секрет
# уже уехал в опубликованный образ, добавление строки в .dockerignore
# ничего не исправляет: старый образ у людей уже есть. Секрет надо
# считать скомпрометированным и менять — это единственное лечение.`;

  protected readonly runAsUser = `# ═══ ЧАСТЬ 1. ROOT ВНУТРИ КОНТЕЙНЕРА ═══
# По умолчанию процесс в контейнере запускается от root.
# Проверить легко:
docker run --rm shop-api id
# uid=0(root) gid=0(root) groups=0(root)

# Почему это плохо, если «контейнер же изолирован»:
# 1. Дыра в вашем коде (например, в загрузке картинок товара можно
#    подсунуть путь ../../) сразу даёт полный доступ ко всему внутри:
#    перезаписать код приложения, подменить сертификаты, поставить себе
#    любые программы через пакетный менеджер.
# 2. Контейнеры делят ОДНО ядро с хостом. Root внутри — это заметно более
#    удобная стартовая площадка для побега наружу через ошибку в ядре.
# 3. При монтировании папки с хоста (bind-mount) root внутри пишет файлы
#    от root снаружи. Знакомая боль: после docker compose up папка dist
#    на вашем ноутбуке принадлежит root, и её нельзя удалить без sudo.

# ═══ КАК СДЕЛАТЬ СВОЕГО ПОЛЬЗОВАТЕЛЯ ═══
FROM node:22.14-slim AS runtime
WORKDIR /app

ENV NODE_ENV=production

# В официальных образах Node уже есть готовый непривилегированный
# пользователь с именем node — проще всего взять его.
# --chown сразу делает его владельцем скопированных файлов, иначе
# приложение не сможет писать даже в свою рабочую папку.
COPY --from=build --chown=node:node /app/node_modules ./node_modules
COPY --from=build --chown=node:node /app/dist ./dist

# Всё, что ниже, выполняется уже НЕ от root: и RUN, и запуск контейнера.
USER node

CMD ["node", "dist/server.js"]

# Если готового пользователя нет (базовый образ не от Node), создаём сами.
# Одной инструкцией RUN, чтобы не плодить лишние слои:
RUN groupadd --gid 1001 shop && useradd --uid 1001 --gid shop --create-home shop
USER shop

# Проверяем результат:
docker run --rm shop-api id
# uid=1001(node) gid=1001(node)

# ЦЕНА: непривилегированный процесс НЕ МОЖЕТ занять порт ниже 1024.
# Слушайте внутри 3000, а наружу отдавайте 80 через -p 80:3000 или
# через обратный прокси. Ещё придётся следить за правами на все папки,
# куда приложение пишет: чужие тома примонтируются с правами root,
# и приложение получит EACCES. Это разовая настройка, но она бывает нудной.

# ═══ ЧАСТЬ 2. ФИКСАЦИЯ ВЕРСИИ БАЗОВОГО ОБРАЗА ═══
FROM node                # ПЛОХО: это latest. Через полгода — другая мажорная
                         # версия Node. Тот же коммит соберётся иначе.
FROM node:22             # ЛУЧШЕ, но тег плавающий: внутри 22.x он меняется.
FROM node:22.14-slim     # ХОРОШО для повседневной работы: мажорная и минорная
                         # версии зафиксированы, патчи безопасности приезжают.

# Максимальная строгость — привязка к контрольной сумме образа.
# Такой FROM всегда даёт БАЙТ В БАЙТ тот же базовый образ:
FROM node:22.14-slim@sha256:<полная_сумма_образа>
# ЦЕНА: обновления безопасности сами не приедут никогда. Сумму придётся
# менять руками (обычно это поручают роботу вроде Dependabot).

# ═══ ЧАСТЬ 3. КАКОЙ БАЗОВЫЙ ОБРАЗ ВЫБРАТЬ ═══

# node:22.14          — полный Debian. Внутри есть всё: git, компиляторы,
#                       пакетный менеджер. Порядок размера — под гигабайт.
#                       ЗА ЧТО ПЛАТИТЕ: вес, трафик и длинный список
#                       уязвимостей в программах, которые вам не нужны.
#                       Когда брать: сложные нативные сборки, отладка.

# node:22.14-slim     — тот же Debian, но урезанный до необходимого.
#                       Порядок размера — сотни мегабайт, но заметно меньше.
#                       ЗА ЧТО ПЛАТИТЕ: части системных пакетов нет,
#                       иногда приходится доставить их руками через apt-get.
#                       Когда брать: РАЗУМНЫЙ ВЫБОР ПО УМОЛЧАНИЮ.

# node:22.14-alpine   — Alpine Linux, десятки мегабайт.
#                       ЗА ЧТО ПЛАТИТЕ, и это главный подвох страницы:
#                       в Alpine ДРУГАЯ СТАНДАРТНАЯ БИБЛИОТЕКА C — musl
#                       вместо привычной glibc. Готовые бинарники нативных
#                       модулей (sharp, драйверы БД) собираются под glibc,
#                       и в Alpine их либо нет, либо они падают. Их придётся
#                       компилировать при сборке — а это минуты времени
#                       и дополнительные пакеты в Dockerfile.
#                       Плюс musl исторически медленнее в работе с памятью,
#                       что для Node иногда заметно.
#                       Когда брать: чистый JS без нативных модулей.

# distroless (например gcr.io/distroless/nodejs22) — только Node и ваши файлы.
#                       Нет ни оболочки, ни ls, ни пакетного менеджера.
#                       ЗА ЧТО ПЛАТИТЕ: внутрь НЕЛЬЗЯ ЗАЙТИ.
#                       docker exec -it ... sh не сработает: sh там просто нет.
#                       Отладка идёт только через логи и метрики.
#                       Когда брать: боевой образ зрелого сервиса, где
#                       вопросы безопасности перевешивают удобство.`;

  protected readonly secretsInBuild = `# ═══ БОЛЬНОЙ КОД. Так делают почти все — и это утечка ═══
# Задача обычная: при сборке надо скачать приватный npm-пакет магазина,
# для этого нужен токен. Первое, что приходит в голову:

# Dockerfile
FROM node:22.14-slim
WORKDIR /app

# ARG — переменная, доступная во время СБОРКИ.
ARG NPM_TOKEN

COPY package.json package-lock.json ./
RUN echo "//registry.npmjs.org/:_authToken=\${NPM_TOKEN}" > .npmrc
RUN npm ci
RUN rm -f .npmrc          # «мы же его удалили, всё чисто»  <-- НЕТ

COPY . .
CMD ["node", "server.js"]

# Сборка:
docker build --build-arg NPM_TOKEN=npm_SecretValue -t shop-api .

# ═══ ПОЧЕМУ ТОКЕН ВСЁ РАВНО УТЁК ═══

# БЕДА 1. СЛОЙ НЕЛЬЗЯ ОТМЕНИТЬ ЗАДНИМ ЧИСЛОМ.
#   Слои складываются стопкой и застывают. RUN rm -f .npmrc создаёт НОВЫЙ
#   слой, который лишь ПРЯЧЕТ файл при просмотре сверху. Слой ниже,
#   где файл с токеном лежит целиком, никуда не делся — он в образе.
#   Достать его может любой, у кого есть образ.

# БЕДА 2. ТЕКСТ КОМАНД ХРАНИТСЯ В ОБРАЗЕ ОТКРЫТО.
#   Docker сохраняет описание каждого слоя. Смотрим:
docker history --no-trunc shop-api
# IMAGE   CREATED   CREATED BY
# ...     ...       RUN /bin/sh -c echo "//registry.npmjs.org/:_authToken=npm_SecretValue" > .npmrc
# ...     ...       ARG NPM_TOKEN=npm_SecretValue
#
#   Токен виден прямо в списке. Никаких хитрых инструментов не нужно —
#   одна команда, и он на экране. Тот же результат даёт docker inspect.

# БЕДА 3. ЭТО УЕЗЖАЕТ В РЕЕСТР.
#   Опубликовали образ — опубликовали и токен. Если реестр публичный,
#   то и всему интернету. Автоматические сканеры находят такое за минуты.

# ═══ ЛЕЧЕНИЕ 1. СЕКРЕТ НУЖЕН ТОЛЬКО ПРИ ЗАПУСКЕ? ЗНАЧИТ, ЕГО НЕТ В ОБРАЗЕ ═══
# Пароль к базе, ключ платежей, ключ подписи сессий — всё это нужно
# РАБОТАЮЩЕМУ приложению, а не сборке. Значит, в Dockerfile их быть не должно.

# Dockerfile просто читает окружение:
ENV NODE_ENV=production
CMD ["node", "dist/server.js"]
# а в коде: const dbUrl = process.env.DATABASE_URL;

# Значения приходят В МОМЕНТ ЗАПУСКА:
docker run -d --name shop-api -e DATABASE_URL=postgres://shop:secret@db:5432/shop shop-api:1.4.2

# Лучше — файлом, чтобы секрет не светился в истории вашей оболочки
# и в выводе ps на сервере:
docker run -d --name shop-api --env-file /etc/shop/api.env shop-api:1.4.2

# ЦЕНА: переменные окружения контейнера видны через docker inspect любому,
# у кого есть доступ к демону Docker. Это НЕ хранилище секретов, это лишь
# способ не запечатывать секрет в образ. Настоящее хранилище (Vault,
# секреты Kubernetes, секреты облака) — отдельная тема.

# ═══ ЛЕЧЕНИЕ 2. СЕКРЕТ ДЕЙСТВИТЕЛЬНО НУЖЕН ПРИ СБОРКЕ ═══
# Для этого есть монтирование секрета: файл появляется внутри ТОЛЬКО
# на время выполнения одной инструкции RUN и НЕ ПОПАДАЕТ НИ В ОДИН СЛОЙ.

# Dockerfile
FROM node:22.14-slim
WORKDIR /app
COPY package.json package-lock.json ./

# id=npmtoken — имя секрета; путь /run/secrets/npmtoken существует
# только внутри этой строки RUN.
RUN --mount=type=secret,id=npmtoken,required=true npm config set "//registry.npmjs.org/:_authToken=$(cat /run/secrets/npmtoken)" && npm ci && rm -f /root/.npmrc

COPY . .
CMD ["node", "server.js"]

# Сборка: секрет передаётся из переменной окружения или из файла.
NPM_TOKEN=npm_SecretValue docker build --secret id=npmtoken,env=NPM_TOKEN -t shop-api .
docker build --secret id=npmtoken,src=./npm_token.txt -t shop-api .

# Проверяем, что теперь чисто:
docker history --no-trunc shop-api | grep -i token
# (пусто — в истории слоёв токена нет)

# ═══ ЧЕСТНАЯ ЦЕНА И ГРАНИЦЫ ═══
# 1. Приём защищает от утечки ЧЕРЕЗ ОБРАЗ. Он не защищает от того, что
#    секрет прочитает сама команда сборки — она его законно использует.
# 2. Если токен уже был в опубликованном образе, никакая пересборка его
#    не отзывает: у людей остались старые копии. Токен надо ОТОЗВАТЬ
#    и выпустить новый. Это единственное настоящее лечение.
# 3. Ту же ошибку легко повторить через COPY .env . — поэтому .env
#    обязан быть в .dockerignore.`;

  protected readonly volumesAndState = `# ═══ ГЛАВНОЕ СВОЙСТВО КОНТЕЙНЕРА: ОН ОДНОРАЗОВЫЙ ═══
# Файловая система контейнера — это стопка слоёв образа плюс тонкий
# записываемый слой сверху. Этот верхний слой живёт РОВНО СТОЛЬКО,
# сколько живёт сам контейнер. Удалили контейнер — слой исчез.

# Показать это можно за минуту:
docker run -d --name shop-api shop-api:1.4.2
docker exec shop-api sh -c 'echo "заказ 1001" > /app/orders.txt'
docker exec shop-api cat /app/orders.txt
# заказ 1001                       <-- файл на месте

docker rm -f shop-api
docker run -d --name shop-api shop-api:1.4.2
docker exec shop-api cat /app/orders.txt
# cat: can't open '/app/orders.txt': No such file or directory
#
# Файла нет. И это НЕ АВАРИЯ, а нормальное поведение. Контейнер всегда
# стартует из состояния образа. Всё, что дописали внутрь, — временное.
# А пересоздают контейнер постоянно: при каждой выкладке, при обновлении
# базового образа, при переезде на другой сервер, при перезапуске узла.

# ═══ КЛАССИЧЕСКАЯ ЛОВУШКА НОВИЧКА ═══
# ЛОВУШКА 1. База SQLite внутри контейнера.
#   В коде: const db = new Database('./shop.sqlite');
#   Локально работает идеально. Первая же выкладка новой версии стирает
#   ВСЕ заказы, всех пользователей и всю корзину. Молча, без ошибок.

# ЛОВУШКА 2. Папка uploads с картинками товаров внутри контейнера.
#   Менеджер загрузил фотографии для новой коллекции, выкатили правку
#   вёрстки — фотографии исчезли. В логах ничего, потому что ничего и не
#   сломалось: контейнер просто стартовал заново из образа.

# ЛОВУШКА 3. Логи в файл /app/logs/app.log.
#   Логи исчезают ровно тогда, когда они нужнее всего, — после падения
#   и перезапуска. Поэтому в контейнерах пишут в stdout (см. docker logs).

# ═══ РЕШЕНИЕ: ТОМА (VOLUMES) ═══
# Том — это кусок диска, который живёт ОТДЕЛЬНО от контейнера
# и подключается внутрь в нужную папку. Есть два вида.

# --- ВИД 1. ИМЕНОВАННЫЙ ТОМ (named volume) ---
# Данными управляет сам Docker, они лежат в его служебной области.
# Вы обращаетесь к тому по ИМЕНИ, а не по пути на диске.
docker volume create shop-uploads
docker run -d --name shop-api -v shop-uploads:/app/uploads shop-api:1.4.2

# Проверяем: пересоздаём контейнер — файлы на месте.
docker exec shop-api sh -c 'echo photo > /app/uploads/product-42.jpg'
docker rm -f shop-api
docker run -d --name shop-api -v shop-uploads:/app/uploads shop-api:1.4.2
docker exec shop-api cat /app/uploads/product-42.jpg
# photo                            <-- пережил пересоздание

docker volume ls                   # список томов
docker volume inspect shop-uploads # где лежит и когда создан

# КОГДА БРАТЬ: данные баз (PostgreSQL, Redis), загруженные файлы —
# всё, что должно пережить контейнер и с чем вы не работаете руками.
# Плюс: на macOS и Windows именованный том заметно быстрее bind-mount,
# потому что не ходит через слой обмена файлами с хостом.

# --- ВИД 2. BIND-MOUNT (папка с вашего компьютера) ---
# Внутрь контейнера подключается КОНКРЕТНАЯ ПАПКА хоста по абсолютному пути.
docker run -d --name shop-api -v /home/deploy/shop/uploads:/app/uploads shop-api:1.4.2

# Главный сценарий — разработка: код с ноутбука виден внутри мгновенно,
# и приложение перезапускается по сохранению файла.
docker run --rm -it -v "$PWD":/app -w /app -p 3000:3000 node:22.14-slim npm run dev

# КОГДА БРАТЬ: разработка, конфигурационные файлы, доступ к данным
# обычными инструментами хоста (бэкап, просмотр, редактор).

# ═══ ЧЕМ ОНИ РЕАЛЬНО ОТЛИЧАЮТСЯ ═══
# Именованный том: путь на диске — забота Docker; переносится между
#   машинами через docker volume; права на файлы Docker подгоняет сам;
#   при первом подключении СОДЕРЖИМОЕ ПАПКИ ИЗ ОБРАЗА КОПИРУЕТСЯ В ТОМ.
# Bind-mount: путь задаёте вы; ничего не копируется — папка хоста ПРОСТО
#   ЗАКРЫВАЕТ СОБОЙ то, что было в образе по этому пути; права как на хосте.
#
# Отсюда самая частая путаница новичка: примонтировали свою папку проекта
# в /app и потеряли node_modules, собранные в образе, — их закрыла ваша
# папка. Лечится «дыркой»: поверх подключают анонимный том.
docker run --rm -it -v "$PWD":/app -v /app/node_modules -p 3000:3000 shop-api:dev

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Том — это состояние, а состояние надо РЕЗЕРВНО КОПИРОВАТЬ. Docker
#    не делает бэкапов. docker volume rm стирает данные без вопросов.
# 2. Том привязан к КОНКРЕТНОЙ МАШИНЕ. Переехали на другой сервер —
#    данные остались на старом. Для нескольких машин нужны сетевые
#    хранилища или управляемая база у провайдера.
# 3. docker system prune с флагом --volumes удаляет неиспользуемые тома.
#    Это самый быстрый способ потерять базу разработки. Читайте флаги.
# 4. Правильный вывод из всего этого: боевую базу данных обычно вообще
#    НЕ ДЕРЖАТ в контейнере на том же сервере. В контейнере — приложение,
#    оно не хранит состояние; база живёт отдельно.`;

  protected readonly composeDev = `# docker-compose.yml — описание НЕСКОЛЬКИХ контейнеров одним файлом.
# Одна команда "docker compose up" поднимает магазин целиком: приложение,
# базу и Redis. Новый разработчик запускает проект в первый день,
# ничего не устанавливая на свой компьютер, кроме Docker.

services:
  # ═══ СЕРВИС 1. НАШЕ ПРИЛОЖЕНИЕ ═══
  api:
    # build — образ СОБИРАЕТСЯ здесь и сейчас из нашего Dockerfile.
    # Используем, когда код наш.
    build:
      context: .                 # какую папку отдать сборщику как контекст
      dockerfile: Dockerfile
      target: build              # остановиться на этапе build: там есть
                                 # devDependencies и режим разработки
    # (Альтернатива — image: ghcr.io/shop/api:1.4.2, то есть ВЗЯТЬ готовый
    #  образ из реестра. Так делают для чужих программ: базы, Redis, прокси.
    #  Правило простое: свой код — build, чужой — image.)

    # ports: "снаружи:внутри". Левое число — порт на ВАШЕМ компьютере,
    # правое — порт внутри контейнера. Без этой строки достучаться
    # из браузера нельзя, даже если в Dockerfile написан EXPOSE.
    ports:
      - '3000:3000'

    environment:
      NODE_ENV: development
      # ВОТ ГЛАВНОЕ МЕСТО ВСЕГО ФАЙЛА: хост базы — "db", а не "localhost".
      DATABASE_URL: postgres://shop:shop_dev_password@db:5432/shop
      REDIS_URL: redis://cache:6379

    # Код с ноутбука виден внутри мгновенно — правка сохраняется,
    # приложение перезапускается само.
    volumes:
      - .:/app
      # Анонимный том поверх: закрывает node_modules хоста, чтобы внутри
      # остались те, что собрались в образе под Linux.
      - /app/node_modules

    # depends_on — ПОРЯДОК ЗАПУСКА. С условием service_healthy compose
    # ждёт, пока healthcheck зависимости не станет успешным.
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_started

    command: npm run dev

  # ═══ СЕРВИС 2. БАЗА ДАННЫХ ═══
  db:
    # Чужая программа — берём готовый образ. Версия зафиксирована.
    image: postgres:16.4
    environment:
      POSTGRES_USER: shop
      POSTGRES_PASSWORD: shop_dev_password
      POSTGRES_DB: shop
    # Порт наружу нужен ТОЛЬКО чтобы подключиться своим клиентом с ноутбука.
    # Приложению он не нужен: внутри сети compose оно и так видит db:5432.
    ports:
      - '5432:5432'
    volumes:
      # Именованный том: данные переживут docker compose down.
      - db-data:/var/lib/postgresql/data
    # healthcheck — команда, которой compose спрашивает «ты уже готова?».
    # pg_isready возвращает успех, когда база принимает подключения.
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U shop -d shop']
      interval: 5s               # как часто спрашивать
      timeout: 3s                # сколько ждать ответа
      retries: 10                # сколько неудач подряд терпеть
      start_period: 10s          # фора на первый запуск, неудачи не считаются

  # ═══ СЕРВИС 3. REDIS ═══
  cache:
    image: redis:7.4
    ports:
      - '6379:6379'
    volumes:
      - cache-data:/data

# Именованные тома объявляются отдельным разделом. Их содержимое
# не удаляется при docker compose down (без флага -v).
volumes:
  db-data:
  cache-data:

# ═══ ПОЧЕМУ "db", А НЕ "localhost" — РАЗБОР ═══
# У каждого контейнера СВОЙ сетевой стек и свой localhost.
# localhost внутри контейнера api — это САМ КОНТЕЙНЕР api, и больше ничто.
# Базы там нет, поэтому попытка подключиться даёт мгновенную ошибку:
#   Error: connect ECONNREFUSED 127.0.0.1:5432
#
# Compose создаёт для всех сервисов общую сеть и поднимает в ней
# внутренний DNS. Имя сервиса из этого файла работает как имя хоста:
#   db     -> адрес контейнера базы
#   cache  -> адрес контейнера Redis
# Порт при этом ВНУТРЕННИЙ (5432), а не тот, что слева в ports.
# Строка "5432:5432" вообще не участвует в связи api <-> db.
# Проверить можно изнутри:
#   docker compose exec api getent hosts db

# ═══ ПОЧЕМУ depends_on БЕЗ healthcheck НЕ СПАСАЕТ ═══
# Голый depends_on: [db] означает только «запусти контейнер db раньше».
# Контейнер стартовал за доли секунды — а сама PostgreSQL внутри него
# ещё несколько секунд разворачивает файлы и не слушает порт.
# Итог: api взлетает первым, падает с ECONNREFUSED на старте, и вы
# смотрите на «случайную» ошибку, которая проявляется через раз
# и никогда — на быстром ноутбуке.
#
# condition: service_healthy заставляет compose ЖДАТЬ успешного
# healthcheck. Отсюда правило: у любой зависимости, к которой вы
# подключаетесь по сети, должен быть healthcheck.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Даже с healthcheck приложение ОБЯЗАНО уметь переживать обрыв связи
#    с базой и переподключаться. В бою база уходит на перезагрузку и без
#    вашего разрешения — compose там ничего не гарантирует.
# 2. Этот файл — ДЛЯ РАЗРАБОТКИ. Пароли открытым текстом, порты баз
#    наружу, монтирование исходников — в бою так нельзя.
# 3. Compose управляет контейнерами на ОДНОЙ машине. Он не умеет
#    ни отказоустойчивости, ни выкладки без простоя, ни распределения
#    по нескольким серверам. Это задача Kubernetes и подобных систем.`;

  protected readonly dockerCommands = `# ═══ СБОРКА ═══
# -t задаёт имя и тег. Точка в конце — это КОНТЕКСТ (папка, содержимое
# которой отдаётся сборщику). Про неё забывают чаще всего.
docker build -t shop-api:1.4.2 .

# Собрать конкретный этап многоступенчатой сборки:
docker build --target build -t shop-api:debug .

# Собрать, полностью игнорируя кэш слоёв (медленно, но честно):
docker build --no-cache -t shop-api:1.4.2 .

# ═══ ЗАПУСК ═══
# -d      — в фоне (detached), терминал остаётся свободным
# --name  — имя контейнера, иначе Docker придумает случайное
# -p      — проброс порта "снаружи:внутри"
# -e      — переменная окружения
# --restart unless-stopped — поднимать заново после падения и перезагрузки
docker run -d --name shop-api -p 3000:3000 -e NODE_ENV=production --restart unless-stopped shop-api:1.4.2

# Разовый запуск в терминале: --rm удалит контейнер сразу после выхода,
# -it даст интерактивный ввод. Так удобно смотреть, что внутри образа.
docker run --rm -it shop-api:1.4.2 sh

# Подменить команду запуска — всё, что после имени образа, заменяет CMD:
docker run --rm shop-api:1.4.2 node --version

# ═══ ПОСМОТРЕТЬ, ЧТО РАБОТАЕТ ═══
docker ps              # только запущенные
docker ps -a           # ВСЕ, включая упавшие. Смотрите сюда при разборе аварии.
# CONTAINER ID  IMAGE           STATUS                     PORTS      NAMES
# a1b2c3d4e5f6  shop-api:1.4.2  Up 2 hours                 3000/tcp   shop-api
# f6e5d4c3b2a1  shop-api:1.4.1  Exited (1) 5 minutes ago               shop-old
# Колонка STATUS — самое ценное. Exited (1) — упал сам. Exited (137) —
# был убит снаружи, чаще всего по нехватке памяти (см. про лимиты).

docker stats           # живое потребление CPU и памяти по контейнерам

# ═══ ЛОГИ ═══
# В контейнерах пишут не в файл, а в stdout/stderr — их собирает Docker.
docker logs shop-api                 # всё, что накопилось
docker logs -f shop-api              # следить в реальном времени
docker logs --tail 100 shop-api      # последние 100 строк
docker logs --since 15m shop-api     # за последние 15 минут
docker logs -f --tail 50 shop-api    # обычное сочетание при разборе

# ═══ ЗАЙТИ ВНУТРЬ ═══
# exec запускает ЕЩЁ ОДИН процесс в УЖЕ РАБОТАЮЩЕМ контейнере.
docker exec -it shop-api sh          # оболочка внутри (в slim/alpine — sh)
docker exec -it shop-api bash        # в полных образах есть и bash
docker exec shop-api ls -la /app     # одна команда без входа внутрь
docker exec shop-api env             # какие переменные реально видит процесс

# ВАЖНО: всё, что вы наменяли через exec, живёт до пересоздания контейнера.
# Чинить боевую аварию правкой файлов внутри — значит гарантированно
# получить ту же аварию при следующей выкладке. Правьте Dockerfile.

# ═══ ОСТАНОВКА И УДАЛЕНИЕ ═══
docker stop shop-api    # вежливо: сигнал SIGTERM, ожидание, потом SIGKILL
docker start shop-api   # запустить остановленный контейнер снова
docker restart shop-api
docker rm shop-api      # удалить ОСТАНОВЛЕННЫЙ контейнер
docker rm -f shop-api   # остановить и удалить одной командой

# ═══ ОБРАЗЫ И МЕСТО НА ДИСКЕ ═══
docker images                        # список образов и их размер
docker rmi shop-api:1.4.1            # удалить образ
docker system df                     # СКОЛЬКО МЕСТА СЪЕДЕНО и чем именно

docker system prune                  # убрать мусор: остановленные контейнеры,
                                     # висячие образы, неиспользуемые сети
docker system prune -a               # ЖЁСТЧЕ: и все образы, на которые
                                     # не ссылается ни один контейнер
# ОПАСНО: флаг --volumes добавляет к уборке ещё и неиспользуемые тома.
# Это самый быстрый известный способ потерять локальную базу магазина.

# ═══ КАК ОТЛАЖИВАТЬ КОНТЕЙНЕР, КОТОРЫЙ ПАДАЕТ СРАЗУ ═══
# Симптом: docker run отработал, а в docker ps пусто.

# ШАГ 1. Найти контейнер среди мёртвых и прочитать причину.
docker ps -a | head
docker logs shop-api
# Чаще всего ответ уже здесь: не хватило переменной окружения,
# не найден модуль, занят порт, отказ базы.

# ШАГ 2. Посмотреть код выхода — он говорит о характере смерти.
docker inspect --format '{{.State.ExitCode}} {{.State.Error}} {{.State.OOMKilled}}' shop-api
# 1     — приложение упало само (исключение).
# 137   — убит SIGKILL, обычно нехватка памяти; OOMKilled будет true.
# 143   — корректно остановлен снаружи (SIGTERM).
# 126/127 — команда не запускается или не найдена (частая беда: файл
#           без права на исполнение или CMD с опечаткой).

# ШАГ 3. Если логи пусты — запустить образ с ДРУГОЙ командой.
# Контейнер поднимется с оболочкой вместо приложения, и можно осмотреться.
docker run --rm -it --entrypoint sh shop-api:1.4.2
# Внутри проверяем руками то, что подозреваем:
#   ls -la /app/dist        — а собранные файлы вообще на месте?
#   cat /app/package.json   — тот ли код мы вообще положили?
#   node dist/server.js     — теперь ошибка видна прямо в терминале
#   id                      — от кого мы работаем и хватает ли прав

# ШАГ 4. Если приложение живёт доли секунды и логи не успевают доехать,
# держите контейнер живым принудительно и запускайте приложение вручную:
docker run -d --name shop-debug --entrypoint sleep shop-api:1.4.2 3600
docker exec -it shop-debug sh

# ЦЕНА ЭТОГО ПРИЁМА: в образах distroless шага 3 и 4 не будет — там нет
# ни sh, ни sleep. Остаются только логи и метрики, поэтому осмысленные
# сообщения об ошибках при старте приложения надо писать заранее.`;

  protected readonly imageTags = `# ═══ ИЗ ЧЕГО СОСТОИТ ПОЛНОЕ ИМЯ ОБРАЗА ═══
# ghcr.io/shop-team/shop-api:1.4.2
# ^^^^^^^ ^^^^^^^^^^ ^^^^^^^^ ^^^^^
# реестр  владелец   имя      тег
#
# Если реестр не указан, подразумевается Docker Hub. Если тег не указан —
# подразумевается latest, и это не «самая свежая версия», а просто
# ТЕГ С ТАКИМ НАЗВАНИЕМ. Никакой магии в нём нет.

# ═══ СБОРКА, ПОМЕТКА, ПУБЛИКАЦИЯ ═══
# Один и тот же образ можно пометить несколькими тегами — это просто
# несколько имён для одних и тех же байтов, места они не занимают.

GIT_SHA=$(git rev-parse --short HEAD)     # например 9f3c1ab
VERSION=1.4.2

docker build -t ghcr.io/shop-team/shop-api:$GIT_SHA .
docker tag ghcr.io/shop-team/shop-api:$GIT_SHA ghcr.io/shop-team/shop-api:$VERSION
docker tag ghcr.io/shop-team/shop-api:$GIT_SHA ghcr.io/shop-team/shop-api:latest

# Вход в реестр. Пароль подаём через stdin, чтобы он не осел
# в истории команд оболочки.
echo "$REGISTRY_TOKEN" | docker login ghcr.io -u shop-ci --password-stdin

# Публикация всех тегов сразу:
docker push --all-tags ghcr.io/shop-team/shop-api

# На сервере:
docker pull ghcr.io/shop-team/shop-api:$GIT_SHA
docker run -d --name shop-api -p 3000:3000 ghcr.io/shop-team/shop-api:$GIT_SHA

# ═══ ПОЧЕМУ НА БОЕВОМ СЕРВЕРЕ НЕЛЬЗЯ latest ═══

# БЕДА 1. НЕПОНЯТНО, ЧТО ЗАПУЩЕНО.
#   docker ps показывает shop-api:latest на всех трёх серверах.
#   Но latest перезаписывается при каждой публикации, а pull делали
#   в разное время. На сервере 1 может работать вчерашний код,
#   на сервере 2 — сегодняшний. Внешне они неразличимы.
#   Вопрос «какая версия в бою?» остаётся без ответа.

# БЕДА 2. ОТКАТИТЬСЯ НЕЧЕМ.
#   Ночью выкатили релиз, заказы перестали оформляться, надо срочно
#   вернуть предыдущую версию. А как? Предыдущий latest затёрт новым.
#   Имени, по которому можно взять «то, что работало час назад», нет.
#   Единственный путь — искать коммит, собирать заново и ждать сборку,
#   пока магазин не работает.

# БЕДА 3. НЕВОСПРОИЗВОДИМАЯ ВЫКЛАДКА.
#   docker run с latest на новом сервере через месяц поднимет уже другой
#   код, чем на старых. Вы получите «одинаковые» серверы с разными версиями.

# БЕДА 4. ОБРАЗ МОЖЕТ И НЕ ОБНОВИТЬСЯ.
#   Если образ с таким тегом уже есть локально, docker run его качать
#   заново не станет. Выкладка «прошла успешно», а версия осталась старой.

# ═══ РАБОЧАЯ СХЕМА ТЕГОВ ═══
# Один образ — три роли, три тега:
#
# 1. НЕИЗМЕНЯЕМЫЙ ТЕГ — хэш коммита: 9f3c1ab
#    Это то, чем реально ЗАПУСКАЮТ в бою. Он не перезаписывается никогда.
#    По нему всегда видно, из какого именно коммита собран контейнер,
#    и всегда есть куда откатиться.
#
# 2. ЧЕЛОВЕЧЕСКИЙ ТЕГ — версия: 1.4.2
#    Для людей, релизных заметок и разговоров. Тоже не перезаписывается.
#
# 3. ПЛАВАЮЩИЙ ТЕГ — latest или staging.
#    Только для удобства разработчиков и стендов. В бою не используется.

# Полезно вшить происхождение прямо в образ — потом это спасает:
docker build -t ghcr.io/shop-team/shop-api:$GIT_SHA --label org.opencontainers.image.revision=$GIT_SHA --label org.opencontainers.image.version=$VERSION .

# И прочитать у работающего контейнера:
docker inspect --format '{{index .Config.Labels "org.opencontainers.image.revision"}}' shop-api
# 9f3c1ab

# ═══ ОТКАТ, РАДИ КОТОРОГО ВСЁ ЭТО ═══
# Ровно одна команда, никакой сборки, счёт на секунды:
docker rm -f shop-api
docker run -d --name shop-api -p 3000:3000 ghcr.io/shop-team/shop-api:7d21e0c

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Даже тег с хэшем коммита технически перезаписываем. Абсолютную
#    неизменяемость даёт только обращение по контрольной сумме образа
#    (image@sha256:...) или настройка реестра, запрещающая перезапись тегов.
# 2. Тегов и образов становится много, реестр растёт. Нужна политика
#    хранения: сколько последних сборок держать, что чистить.
# 3. Схема тегов ничего не стоит, если выкладка делается руками и
#    «по-быстрому». Она работает только когда встроена в автоматику
#    (например, в GitHub Actions), где хэш коммита берётся сам.`;

  protected readonly resourceLimits = `# ═══ ЗАЧЕМ ОГРАНИЧИВАТЬ ═══
# По умолчанию контейнер видит ВСЮ память и ВСЕ процессоры машины.
# Значит, один контейнер с утечкой памяти способен положить весь сервер:
# ядро начнёт убивать процессы подряд, и вместе с виноватым улетят
# и база, и nginx, и все соседние контейнеры магазина.
# Лимит превращает «упал весь сервер» в «упал один контейнер».

# ═══ КАК ЗАДАТЬ ═══
docker run -d --name shop-api --memory=512m --memory-swap=512m --cpus=1.5 shop-api:1.4.2
# --memory       максимум оперативной памяти
# --memory-swap  память + подкачка. Равное значение = подкачку не давать.
#                Без этого контейнер начнёт свопиться и станет очень
#                медленным вместо того, чтобы честно упасть.
# --cpus=1.5     полтора процессорных ядра по времени

# То же самое в docker compose:
#   deploy:
#     resources:
#       limits:
#         memory: 512M
#         cpus: '1.5'

# Проверить, что лимиты применились:
docker stats shop-api
# NAME      CPU %   MEM USAGE / LIMIT   MEM %
# shop-api  38.4%   410MiB / 512MiB     80.1%     <-- LIMIT виден, всё верно

# ═══ ЧТО ТАКОЕ OOMKilled ═══
# OOM = Out Of Memory. Когда процесс внутри просит больше памяти, чем
# разрешено лимитом, ядро Linux НЕ ждёт и НЕ просит освободить память.
# Оно убивает процесс сигналом SIGKILL — мгновенно, без шанса
# что-то записать в лог, закрыть соединение или дописать заказ в базу.

# КАК ЭТО ВЫГЛЯДИТ. Самое коварное: в логах приложения НИЧЕГО НЕТ.
docker logs shop-api --tail 20
# ... обычные строки про обработку заказов ...
# и обрыв на полуслове. Ни стека, ни исключения, ни «shutting down».

# Признаки надо искать не в логах приложения, а в состоянии контейнера:
docker ps -a
# STATUS: Exited (137) 12 seconds ago      <-- 137 = 128 + 9 (SIGKILL)

docker inspect --format '{{.State.OOMKilled}} {{.State.ExitCode}}' shop-api
# true 137                                 <-- вот прямое подтверждение

# А если стоял --restart unless-stopped, картина ещё запутаннее:
# контейнер молча перезапускается по кругу, пользователи ловят обрывы
# в оформлении заказа, а в docker ps всё выглядит «работающим».
docker inspect --format '{{.RestartCount}}' shop-api
# 27                                       <-- 27 перезапусков. Это диагноз.

# ═══ ПОЧЕМУ NODE НАДО ОТДЕЛЬНО СКАЗАТЬ ПРО ЛИМИТ ═══
# Сборщик мусора V8 решает, когда убирать мусор, исходя из СВОЕГО
# представления о доступной памяти. Исторически это представление
# опиралось на память всей машины, а не на лимит контейнера.
#
# Получается вилка: на сервере 32 ГБ, контейнеру выдано 512 МБ.
# Node видит «памяти много», не торопится с уборкой, спокойно набирает
# больше 512 МБ — и ядро убивает его раньше, чем сборщик мусора вообще
# соберётся поработать. Итог: OOMKilled при том, что утечки НЕТ.
#
# Лечение — сказать V8 предел явно. Значение берут заметно НИЖЕ лимита
# контейнера: помимо кучи JavaScript, процессу нужна память на сам Node,
# буферы, нативные модули и стек.
docker run -d --name shop-api --memory=512m -e NODE_OPTIONS=--max-old-space-size=384 shop-api:1.4.2

# Или прямо в Dockerfile:
ENV NODE_OPTIONS=--max-old-space-size=384

# Проверить, что Node действительно видит нужный предел:
docker exec shop-api node -e "console.log(v8.getHeapStatistics().heap_size_limit / 1024 / 1024)"

# ЗАПАС ОРИЕНТИРОВОЧНО: под кучу отдают порядка 70-80% лимита контейнера.
# Точное число подбирается по вашей нагрузке, универсального нет.

# ═══ ЧТО ПРОИСХОДИТ ПРИ ОГРАНИЧЕНИИ CPU — ЭТО ДРУГОЕ ═══
# С процессором ядро НИКОГО НЕ УБИВАЕТ. Оно просто РАСТЯГИВАЕТ время.
# Контейнеру выдаётся квота на короткий период; выбрал квоту — ждёт
# начала следующего периода. Это называется throttling.
#
# Симптомы: ошибок нет, всё «работает», но:
#   - время ответа /api/orders выросло с десятков миллисекунд до сотен,
#   - хвост задержек стал рваным: большинство запросов быстрые,
#     а часть — в разы медленнее,
#   - CPU в docker stats упирается в потолок и стоит там ровно.
#
# Главное отличие, ради которого этот абзац:
#   мало памяти -> ВНЕЗАПНАЯ СМЕРТЬ без следов в логах;
#   мало CPU    -> ТИХОЕ ЗАМЕДЛЕНИЕ, которое ищут неделями,
#                  подозревая базу, сеть и чужие сервисы.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1. Лимит НЕ ЧИНИТ утечку памяти. Он делает её заметной и локальной:
#    падает один контейнер, а не весь сервер. Причину искать всё равно вам.
# 2. Слишком низкий лимит вреден так же, как его отсутствие: вы получите
#    перезапуски на ровном месте под обычной нагрузкой. Сначала измеряйте
#    реальное потребление (docker stats под нагрузкой), потом ставьте лимит
#    с запасом, а не наоборот.
# 3. Лимиты — про ОДНУ машину. Автоматически добавить контейнеров, когда
#    нагрузка выросла, Docker сам не умеет. Это уже задача оркестратора
#    вроде Kubernetes.
# 4. Число --cpus не «закрепляет ядра» за контейнером. Это доля
#    процессорного ВРЕМЕНИ. Контейнер может исполняться то на одном ядре,
#    то на другом — суммарно не больше выданной доли.`;
}
