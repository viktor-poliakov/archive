import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

import { CodeBlock } from '../../../code/code-block';

@Component({
  selector: 'app-ecosystem-infra-nginx',
  imports: [CodeBlock, RouterLink],
  templateUrl: './nginx.html',
  styleUrls: ['../../../content/doc.scss'],
})
export class EcosystemInfraNginx {
  protected readonly whyProxy = `# БОЛЬНОЙ ВАРИАНТ. Приложение на Node смотрит в интернет само, напрямую.
# Так поднимают первый сервер почти все, и выглядит это совершенно нормально:
# программа написана, порт открыт, сайт открывается. Что ещё нужно?

# на сервере
node server.js
# внутри server.js:  app.listen(80)

# Сайт работает. А теперь — что случится с ним дальше.

# ═══ БЕДА 1. ПОРТ 80 ТРЕБУЕТ ПРАВ СУПЕРПОЛЬЗОВАТЕЛЯ ═══
# Порты меньше 1024 в Linux считаются "привилегированными": обычному
# пользователю их занять нельзя.

node server.js
# Error: listen EACCES: permission denied 0.0.0.0:80

# Первое, что делает новичок, — запускает под root:
sudo node server.js
# Теперь работает. И теперь ЛЮБАЯ дыра в вашем коде или в любой из
# восьмисот библиотек из node_modules — это дыра с правами root:
# чтение чужих файлов, запись в системные каталоги, установка чего угодно.
# Одна опечатка в обработке пути к файлу — и сервер больше не ваш.

# ═══ БЕДА 2. TLS (ЗАМОЧЕК В АДРЕСНОЙ СТРОКЕ) ПРИДЁТСЯ ДЕЛАТЬ ВНУТРИ КОДА ═══
# Без прокси шифрованием занимается само приложение. Это значит, что в вашем
# репозитории появляется работа с сертификатами:
#   - прочитать файлы сертификата и ключа с диска;
#   - раз в несколько недель их обновить (сертификаты Let's Encrypt живут
#     недолго и продлеваются автоматически) и ПЕРЕЗАПУСТИТЬСЯ, чтобы подхватить;
#   - разрулить, что запросы на http нужно переслать на https;
#   - следить за настройками шифров, чтобы старьё не пролезало.
# Ничего из этого не имеет отношения к вашему магазину. Это работа сервера,
# а не работа кода, который считает корзину.

# ═══ БЕДА 3. СТАТИКУ ОТДАЁТ ВАШ ЕДИНСТВЕННЫЙ ПОТОК ═══
# У Node один основной поток выполнения. Когда вы пишете
#   app.use(express.static('dist'))
# каждая картинка, каждый шрифт и каждый js-файл читается с диска и
# перекладывается в сокет РУКАМИ вашего процесса. Открытие одной страницы —
# это десятки таких файлов. Пока поток занят перекладыванием байтов картинки,
# он не считает корзину и не отвечает на /api/orders.

# ═══ БЕДА 4. ОДИН МЕДЛЕННЫЙ КЛИЕНТ ЗАНИМАЕТ СОЕДИНЕНИЕ ЦЕЛИКОМ ═══
# Пользователь в метро отправляет форму с фотографией. Его телефон отдаёт
# данные крошечными кусочками много секунд подряд. Всё это время соединение
# с вашим приложением открыто и ждёт. Сто таких пользователей — сто открытых
# соединений, которые не делают ничего полезного, но занимают память и лимиты.
# Обратный прокси устроен ровно наоборот: он СНАЧАЛА целиком принимает запрос
# у медленного клиента (для этого он и написан — держать десятки тысяч
# полудохлых соединений почти бесплатно), и только потом одним быстрым
# рывком отдаёт его приложению.

# ═══ БЕДА 5. КОПИЙ ПРИЛОЖЕНИЯ НЕСКОЛЬКО, А АДРЕС ОДИН ═══
# Ядер на сервере, скажем, четыре. Чтобы использовать их все, запускают
# четыре копии приложения — на портах 3000, 3001, 3002, 3003.
# И тут же вопрос: а браузер-то стучится на ОДИН адрес shop.example.com.
# Кто раскидывает запросы по четырём копиям? Само приложение не может:
# оно как раз и есть одна из копий. Нужен кто-то ПЕРЕД ними.

# ═══ ВЫВОД ═══
# Обратный прокси (Nginx, Caddy, Traefik) — это программа, которая стоит
# между интернетом и вашим приложением и снимает с него всю работу,
# которой оно не должно заниматься:
#   порт 80 и 443  ·  сертификаты и https  ·  раздача файлов с диска
#   сжатие ответов  ·  медленные клиенты  ·  распределение по копиям
# Приложение после этого слушает 127.0.0.1:3000, работает от обычного
# пользователя и умеет ровно одно: считать бизнес-логику магазина.

# ЧЕСТНАЯ ЦЕНА. Вы получаете ещё одну программу, которую надо настроить,
# обновлять и уметь читать её логи. Появляется новый источник ошибок:
# теперь "сайт не работает" может означать и "приложение упало",
# и "прокси не туда стучится". Плата за это — всё перечисленное выше,
# и в проде она окупается с первого же дня.`;

  protected readonly firstNginxConf = `# СНАЧАЛА — РАЗНИЦА, КОТОРУЮ ПУТАЮТ ВСЕ.
#
# ПРЯМОЙ прокси (forward proxy) работает НА СТОРОНЕ КЛИЕНТА.
#   Это когда ВЫ настраиваете у себя в браузере "ходи в интернет через вот
#   этот адрес". Прокси знает, кто вы, и ходит по сайтам вместо вас.
#   Сайт не знает, что вы существуете, он видит только прокси.
#   Пример: корпоративный прокси на работе, который режет соцсети.
#
# ОБРАТНЫЙ прокси (reverse proxy) работает НА СТОРОНЕ САЙТА.
#   Это когда МЫ, владельцы shop.example.com, ставим программу перед своим
#   приложением. Браузер ничего не настраивает и вообще не подозревает о её
#   существовании: он думает, что shop.example.com — это и есть сайт.
#   Обратный прокси знает, кто СЕРВЕРЫ, и раздаёт запросы им.
#
# Слово одно, стороны разные. Дальше на этой странице — только обратный.

# ═══ МИНИМАЛЬНЫЙ РАБОЧИЙ КОНФИГ ═══
# Файл кладут в /etc/nginx/conf.d/shop.conf
# (либо в /etc/nginx/sites-available/shop и делают ссылку в sites-enabled).

upstream shop_app {
    # upstream — это ИМЕНОВАННАЯ ГРУППА адресов, куда мы будем слать запросы.
    # Имя shop_app придумали сами, оно нужно только внутри этого файла.
    # Пока в группе один адрес — та самая копия приложения на Node.
    server 127.0.0.1:3000;
}

server {
    # server — описание ОДНОГО сайта. Таких блоков в файлах может быть много:
    # shop.example.com, admin.example.com, api.example.com — каждый свой блок.

    listen 80;
    # На каком порту слушать. 80 — обычный http.
    # Слушает именно Nginx, а не наше приложение: приложение сидит на 3000
    # и из интернета вообще недоступно.

    server_name shop.example.com;
    # Для КАКОГО ИМЕНИ этот блок. Nginx смотрит заголовок Host в запросе
    # и выбирает подходящий server-блок. Именно так десять разных сайтов
    # живут на одном сервере с одним IP-адресом.

    location /api/ {
        # location — правило "если путь запроса начинается так, делай вот это".
        # Правила проверяются для каждого запроса, побеждает самое подходящее.

        proxy_pass http://shop_app;
        # ГЛАВНАЯ СТРОКА ВСЕЙ СТРАНИЦЫ.
        # "Не отвечай сам, а перешли запрос вот в эту группу адресов,
        #  дождись ответа и отдай его обратно браузеру."
        # Здесь мы указали имя upstream. Можно и напрямую: proxy_pass
        # http://127.0.0.1:3000; — тогда upstream-блок не нужен.
    }

    location / {
        # Всё, что не начинается с /api/, попадает сюда.
        # Пока — тоже в приложение. На следующих шагах отдадим отсюда статику.
        proxy_pass http://shop_app;
    }
}

# ═══ ЛОВУШКА СО СЛЕШЕМ В proxy_pass — САМАЯ ЧАСТАЯ ОШИБКА НОВИЧКА ═══
# Запрос пришёл на /api/orders.
#
#   proxy_pass http://shop_app;      # БЕЗ слеша в конце
#   -> приложение получит  /api/orders   (путь передан как есть)
#
#   proxy_pass http://shop_app/;     # СО слешем в конце
#   -> приложение получит  /orders       (кусок из location ОТРЕЗАН)
#
# Оба варианта правильные — но для разных приложений. Если в коде маршруты
# описаны как app.get('/api/orders'), нужен первый. Если как app.get('/orders'),
# нужен второй. Симптом ошибки одинаковый и обманчивый: сплошные 404,
# при том что "локально всё работает".

# ═══ ПРОВЕРКА И ПРИМЕНЕНИЕ. НИКОГДА НЕ ДЕЛАЙТЕ restart ВСЛЕПУЮ ═══
sudo nginx -t
# Разбирает конфиг и говорит, где опечатка. Пока не увидели "syntax is ok"
# и "test is successful" — дальше не идём.

sudo systemctl reload nginx
# reload, а НЕ restart. reload поднимает новые процессы с новым конфигом,
# а старым даёт спокойно доработать уже начатые запросы. Никто ничего
# не замечает. restart рвёт живые соединения.

# ЧЕСТНАЯ ЦЕНА этого конфига: он НИЧЕГО не умеет, кроме пересылки.
# Нет https, приложение не знает настоящий адрес пользователя, статика идёт
# через Node, а при падении приложения пользователь увидит голую страницу
# ошибки Nginx. Всё это чиним дальше по очереди.`;

  protected readonly forwardedHeaders = `# БОЛЬНОЙ КОНФИГ. Ровно тот, что был на прошлом шаге. Он работает.
# Сайт открывается, заказы оформляются, вы уходите домой.

location /api/ {
    proxy_pass http://shop_app;
}

# ═══ ЧТО СЛОМАЕТСЯ — И ПОЧЕМУ ВЫ НЕ СВЯЖЕТЕ ЭТО С NGINX ═══
#
# Когда Nginx пересылает запрос приложению, он открывает СВОЁ, НОВОЕ
# соединение с 127.0.0.1:3000. Для приложения источник этого соединения —
# не пользователь, а Nginx. Оно физически не может узнать правду само.
#
# СИМПТОМ 1. В ЛОГАХ У ВСЕХ ПОЛЬЗОВАТЕЛЕЙ ОДИН АДРЕС.
#   req.ip === '127.0.0.1' у всех до единого. Расследовать "кто это сделал"
#   нечем: в журнале десять тысяч строк с одним и тем же адресом.
#
# СИМПТОМ 2. ОГРАНИЧЕНИЕ ЧАСТОТЫ ЗАПРОСОВ ПРЕВРАЩАЕТСЯ В ОРУЖИЕ ПРОТИВ ВАС.
#   Библиотека вроде express-rate-limit считает попытки ПО АДРЕСУ.
#   Адрес у всех один. Значит, лимит "5 попыток входа в минуту" — это
#   пять попыток НА ВЕСЬ САЙТ. Один перебиральщик паролей выбирает лимит
#   за секунду, и дальше форму входа не может открыть никто. Полный отказ
#   сайта, вызванный вашей же защитой.
#
# СИМПТОМ 3. ССЫЛКИ В ПИСЬМАХ ВЕДУТ НА http.
#   Пользователь пришёл по https. Но Nginx расшифровал TLS у себя и пошёл
#   в приложение по обычному http. Приложение смотрит на своё соединение,
#   видит http и честно клеит ссылку подтверждения заказа:
#     http://shop.example.com/orders/1234
#   Браузер получателя ругается на небезопасное соединение, а вы полдня
#   ищете ошибку в коде генерации ссылок. Её там нет.
#
# СИМПТОМ 4. ПРИЛОЖЕНИЕ НЕ ЗНАЕТ СОБСТВЕННОГО ИМЕНИ.
#   Заголовок Host по умолчанию Nginx подменяет на имя upstream.
#   Вместо shop.example.com приложение видит shop_app. Мультидоменность,
#   переадресации и куки, привязанные к домену, ломаются разом.

# ═══ ЛЕЧЕНИЕ: ЧЕТЫРЕ ЗАГОЛОВКА, КОТОРЫЕ НАДО ПЕРЕДАТЬ РУКАМИ ═══

location /api/ {
    proxy_pass http://shop_app;

    proxy_set_header Host $host;
    # Настоящее имя сайта из запроса браузера — shop.example.com.
    # Без этой строки приложение видит имя upstream-группы.

    proxy_set_header X-Real-IP $remote_addr;
    # Адрес того, кто подключился к Nginx. Обычно это и есть пользователь.

    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # Список адресов через запятую: "кто был до нас".
    # Переменная $proxy_add_x_forwarded_for — это удобное сокращение:
    # "возьми пришедший X-Forwarded-For и допиши в конец $remote_addr".
    # Если перед вами стоит ещё и Cloudflare, получится примерно так:
    #   X-Forwarded-For: 203.0.113.7, 172.16.0.5
    #                    ^ пользователь  ^ Cloudflare

    proxy_set_header X-Forwarded-Proto $scheme;
    # http или https — то, чем пользовался БРАУЗЕР, а не мы внутри.
}

# ═══ ВТОРАЯ ПОЛОВИНА ЛЕЧЕНИЯ: ПРИЛОЖЕНИЕ ДОЛЖНО ЭТИМ ЗАГОЛОВКАМ ПОВЕРИТЬ ═══
# Одного Nginx мало. Express по умолчанию заголовки ИГНОРИРУЕТ — и правильно
# делает: любой желающий может послать вам X-Forwarded-For: 8.8.8.8 и стать
# кем угодно. Доверять им можно, только если вы точно знаете, что перед
# приложением стоит ваш прокси, который эти заголовки ПЕРЕЗАПИСЫВАЕТ.

# в коде приложения (Express):
app.set('trust proxy', 1);
# Число — это "скольким ближайшим прокси я доверяю".
#   1  — перед приложением ровно один прокси (наш Nginx). Обычный случай.
#   2  — Cloudflare, потом наш Nginx.
# После этой строки:
#   req.ip       — настоящий адрес пользователя
#   req.protocol — 'https', если браузер пришёл по https
#   req.secure   — true

# ЧЕСТНАЯ ЦЕНА И ГЛАВНАЯ ОПАСНОСТЬ.
# НИКОГДА не пишите app.set('trust proxy', true) на сервере, который доступен
# из интернета напрямую в обход прокси. true означает "верю любому, кто
# что-нибудь напишет в X-Forwarded-For". Злоумышленник подставит чужой адрес
# и обойдёт ваши лимиты, а в логах останется невиновный.
# Число вместо true — это не придирка, это и есть защита.
# И проверьте руками: curl -H 'X-Forwarded-For: 1.2.3.4' на свой сайт
# не должен менять то, что приложение считает адресом пользователя.`;

  protected readonly staticFiles = `# БОЛЬНОЙ ВАРИАНТ. Собранный фронтенд отдаёт само приложение.

# в коде приложения:
app.use(express.static('dist'));
# в Nginx:
location / {
    proxy_pass http://shop_app;
}

# ═══ ПОЧЕМУ ЭТО ДОРОГО ═══
#
# БЕДА 1. МАРШРУТ ЧЕРЕЗ ЛИШНЮЮ КОМНАТУ.
#   Файл лежит на диске сервера. Nginx стоит на том же сервере и может
#   отдать его сам. Вместо этого он идёт по сети (пусть и локальной)
#   в Node, Node читает файл с диска, копирует его в память JavaScript,
#   отдаёт Nginx, а тот — браузеру. Три пересылки вместо одной.
#
# БЕДА 2. ЭТО ЗАНИМАЕТ ЕДИНСТВЕННЫЙ ПОТОК.
#   Пока Node перекладывает байты шрифта, он не считает корзину.
#   Одна страница магазина — это легко несколько десятков файлов.
#   Nginx же умеет отдавать файл фактически напрямую из системного кэша,
#   не таская его через прикладной код.
#
# БЕДА 3. ВСЁ, ЧТО ВОКРУГ, ПРИХОДИТСЯ ПИСАТЬ САМОМУ.
#   Заголовки кэширования, ответ 304 на повторный запрос, сжатие,
#   поддержка докачки — всё это в Nginx уже есть и включается строкой.

# ═══ ЗДОРОВЫЙ КОНФИГ ═══

server {
    listen 80;
    server_name shop.example.com;

    root /srv/shop/current;
    # Корень файлов на диске. Запрос /assets/main.a1b2c3.js Nginx превратит
    # в путь /srv/shop/current/assets/main.a1b2c3.js и отдаст файл сам.

    # ─── 1. API уходит в приложение ───
    location /api/ {
        proxy_pass http://shop_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ─── 2. Файлы сборки: имя содержит хэш, кэшируем НАДОЛГО ───
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        # ПОЧЕМУ ЦЕЛЫЙ ГОД — ЭТО НЕ БЕЗУМИЕ.
        # Сборщик (Vite, webpack) кладёт в имя файла хэш содержимого:
        #   main.a1b2c3d4.js
        # Изменился хоть один символ кода — изменился хэш — ИЗМЕНИЛОСЬ ИМЯ.
        # То есть файл main.a1b2c3d4.js не поменяется НИКОГДА: он либо есть
        # ровно такой, либо его больше никто не запрашивает. Значит,
        # обновлять его в кэше браузера незачем.
        # Слово immutable — это прямое "даже не спрашивай, не изменилось ли":
        # без него браузер при обновлении страницы всё равно ходит проверять.
    }

    # ─── 3. Точка входа: НЕ кэшируем никогда ───
    location = /index.html {
        add_header Cache-Control "no-cache";
        # В index.html вписаны имена файлов сборки с хэшами. Если закэшировать
        # его, браузер будет вечно грузить СТАРУЮ версию сайта, хотя новые
        # файлы уже лежат на сервере. Это классическая беда "у меня всё
        # обновилось, а у пользователей нет".
        # no-cache не значит "не хранить": браузер хранит, но каждый раз
        # переспрашивает "не изменилось ли". Ответ 304 весит копейки.
    }

    # ─── 4. Всё остальное — на index.html (одностраничное приложение) ───
    location / {
        try_files $uri $uri/ /index.html;
        # try_files — "попробуй по очереди, отдай первое, что нашлось":
        #   1) $uri   — файл с таким путём (/logo.svg)
        #   2) $uri/  — каталог с index-файлом внутри
        #   3) /index.html — запасной вариант
        # Зачем третий пункт: пользователь обновил страницу на /orders/1234.
        # Такого файла на диске нет — маршрут существует только внутри
        # Angular/React. Без try_files Nginx честно ответит 404, и человек
        # увидит ошибку на живой странице. С try_files он получит index.html,
        # приложение загрузится и само разберёт адрес.
    }

    # ─── 5. Сжатие ───
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    # ВНИМАНИЕ: text/html сжимается всегда, его в список писать не надо.
    gzip_min_length 1024;
    # Мельче килобайта сжимать бессмысленно: выигрыш меньше накладных расходов.
    gzip_comp_level 5;
    # Уровень 1..9. Чем выше, тем меньше файл и тем больше процессорного
    # времени на КАЖДЫЙ ответ. Верхние уровни почти не выигрывают в размере.

    # brotli сжимает текст заметно плотнее gzip, но в стандартной сборке
    # Nginx его нет — нужен отдельный модуль. Строки выглядят так:
    #   brotli on;
    #   brotli_types text/css application/javascript;
    # Есть вариант лучше: заранее, на этапе сборки, положить рядом с main.js
    # файлы main.js.gz и main.js.br и включить отдачу готовых:
    #   gzip_static on;
    # Тогда сервер вообще ничего не сжимает в реальном времени — он просто
    # отдаёт заранее пожатый файл. Максимальное сжатие ценой нуля процессора.
}

# ЧЕСТНАЯ ЦЕНА.
# 1) Файлы теперь должны физически лежать там, куда смотрит root. Появляется
#    шаг развёртывания "скопировать dist на сервер" — раньше он ехал вместе
#    с приложением.
# 2) Права доступа: пользователь, под которым работает Nginx, должен иметь
#    право читать эти файлы. Половина ошибок 403 на новом сервере — про это.
# 3) Сжатие НЕ бесплатное: оно тратит процессор на каждый ответ.
# 4) Кэш на год — обещание, которое нельзя нарушить. Если вы вдруг соберёте
#    файл БЕЗ хэша в имени и положите его в /assets/, у части пользователей
#    он останется старым очень надолго, и сделать с этим будет нечего.`;

  protected readonly loadBalancing = `# ЗАДАЧА. Одна копия приложения на Node использует одно ядро. Ядер четыре.
# Значит, запускаем четыре копии на портах 3000..3003 и ставим перед ними
# Nginx, который будет раскидывать запросы.

# ═══ ВАРИАНТ ПО УМОЛЧАНИЮ: ПО ОЧЕРЕДИ (round-robin) ═══
upstream shop_app {
    server 127.0.0.1:3000;
    server 127.0.0.1:3001;
    server 127.0.0.1:3002;
    server 127.0.0.1:3003;
    # Никаких дополнительных слов не написано — значит, запросы уходят
    # по кругу: первый в 3000, второй в 3001, третий в 3002 и так далее.
    # Просто, честно и в большинстве случаев достаточно.
}

# ═══ ВЕСА: ЕСЛИ СЕРВЕРЫ РАЗНЫЕ ═══
upstream shop_app {
    server 10.0.0.11:3000 weight=3;   # мощная машина, ей втрое больше
    server 10.0.0.12:3000 weight=1;   # старенькая, ей поменьше
}

# ═══ ПО ЧИСЛУ АКТИВНЫХ СОЕДИНЕНИЙ ═══
upstream shop_app {
    least_conn;
    server 127.0.0.1:3000;
    server 127.0.0.1:3001;
    # "Отдай тому, кто сейчас меньше всех занят."
    # Когда это лучше очереди: если запросы РАЗНОЙ длительности.
    # Пример из магазина: обычный /api/products отвечает за миллисекунды,
    # а /api/reports/sales считает минуту. При круговой раздаче копия,
    # которой не повезло получить три отчёта подряд, встанет колом,
    # но очередь всё равно будет исправно слать ей каждый четвёртый запрос.
}

# ═══ ПО ХЭШУ АДРЕСА КЛИЕНТА ═══
upstream shop_app {
    ip_hash;
    server 127.0.0.1:3000;
    server 127.0.0.1:3001;
    # Из адреса пользователя считается число, по нему выбирается копия.
    # Один и тот же человек всегда попадает на одну и ту же копию —
    # это и есть "липкие сессии".
}

# ═══ ЗАПАСНОЙ СЕРВЕР И ОТКЛЮЧЕНИЕ БОЛЬНЫХ ═══
upstream shop_app {
    server 127.0.0.1:3000 max_fails=3 fail_timeout=30s;
    server 127.0.0.1:3001 max_fails=3 fail_timeout=30s;
    # max_fails=3 fail_timeout=30s читается так:
    # "если за 30 секунд копия трижды не ответила — считаем её мёртвой
    #  и не шлём ей ничего следующие 30 секунд, потом осторожно пробуем снова".
    # Без этого Nginx будет упорно слать каждый второй запрос в упавшую копию,
    # и половина пользователей будет видеть ошибку.

    server 127.0.0.1:3010 backup;
    # backup — "не трогать, пока жив хоть кто-то из основных".
    # Сюда ставят, например, копию на резервной машине или заглушку,
    # показывающую вежливое "идут технические работы".
}

# ═══ ЛИПКИЕ СЕССИИ: ЗАЧЕМ ИХ ПРОСЯТ И ПОЧЕМУ ЭТО ЛОВУШКА ═══
#
# БОЛЬНАЯ АРХИТЕКТУРА. Приложение хранит корзину В ПАМЯТИ процесса:
#   const carts = new Map();          // ключ — id пользователя
# Пока копия одна — работает идеально. Копий стало четыре — и корзина
# "то есть, то нет": положил товар (попал в 3000), обновил страницу
# (попал в 3001) — корзина пуста. Пользователь в бешенстве.
#
# БЫСТРОЕ ЛЕЧЕНИЕ: написать ip_hash и привязать человека к копии.
# Работает. И приносит четыре новые беды:
#   1. Копия перезапустилась при выкладке новой версии — все корзины
#      её пользователей исчезли разом. А выкладки бывают каждый день.
#   2. Нагрузка размазывается неровно: за одним корпоративным адресом
#      могут сидеть сотни людей, и все попадут в одну копию.
#   3. Мобильный клиент переключился с Wi-Fi на сотовую сеть — сменился
#      адрес — сменилась копия — сессия потеряна.
#   4. Масштабироваться больно: добавили пятую копию, и хэш пересчитался
#      для всех, кто был привязан.
#
# ПРАВИЛЬНОЕ ЛЕЧЕНИЕ: сделать так, чтобы липкость была НЕ НУЖНА.
# Приложение не должно хранить в своей памяти ничего, чего нельзя потерять.
# Состояние выносится наружу:
#   - сессии и корзины — в Redis (общий для всех копий);
#   - загруженные файлы — в объектное хранилище, а не в локальную папку;
#   - фоновые задачи — в очередь, а не в переменную процесса.
# После этого КАЖДАЯ копия может ответить на ЛЮБОЙ запрос. Копию можно
# убить, перезапустить, добавить десять новых — никто ничего не заметит.
# Такое приложение называют "не хранящим состояние" (stateless).

# ЧЕСТНАЯ ЦЕНА БАЛАНСИРОВКИ.
# 1) Nginx по умолчанию НЕ проверяет здоровье копий заранее — он замечает
#    поломку только по неудачным запросам. Значит, первые несколько
#    пользователей после падения копии всё же получат ошибку.
#    Активные проверки (health checks) есть в платной версии Nginx Plus;
#    в бесплатной их роль частично играют max_fails/fail_timeout.
# 2) Логи теперь размазаны по четырём процессам. Без общего сбора логов
#    искать причину ошибки становится заметно тяжелее.
# 3) Четыре копии — это четыре набора соединений с базой. Пул на 20
#    соединений превращается в 80. База может этого и не оценить.
# 4) Балансировщик сам стал единственной точкой отказа: упал Nginx —
#    упало всё. Для по-настоящему серьёзных систем его тоже дублируют.`;

  protected readonly timeouts502 = `# ДВЕ ОШИБКИ, КОТОРЫЕ ПУТАЮТ ЧАЩЕ ВСЕГО.
# Их выдаёт НЕ ваше приложение. Их выдаёт Nginx, когда не смог получить
# от приложения нормальный ответ. Различие — ровно в этом:
#
#   502 Bad Gateway     — "я постучался, и мне НЕ ОТКРЫЛИ".
#       Соединение отвергнуто, оборвано, либо в ответ пришёл мусор.
#       Обычно означает: приложение упало, ещё не запустилось,
#       слушает другой порт, или процесс убит системой из-за памяти.
#
#   504 Gateway Timeout — "я постучался, мне открыли и МОЛЧАТ".
#       Соединение установлено, запрос ушёл, ответа нет слишком долго.
#       Обычно означает: приложение живо, но зависло на медленном запросе
#       к базе, на внешнем сервисе, или считает что-то очень долгое.
#
# Разница практическая: при 502 идите смотреть, жив ли процесс.
# При 504 процесс точно жив — идите смотреть, чем он занят.

# ═══ ТИПИЧНЫЙ СЦЕНАРИЙ, ОТ КОТОРОГО СЕДЕЮТ ═══
# Менеджер магазина открывает /api/reports/sales за год.
# Через минуту в браузере появляется "504 Gateway Timeout".
# Вы идёте в логи приложения — а там ВСЁ ХОРОШО:
#   [info] report generated, rows: 812340, duration: 94s
#   POST /api/reports/sales 200
# Отчёт посчитан, ответ 200 отдан. Никакой ошибки нет.
#
# ЧТО ПРОИЗОШЛО НА САМОМ ДЕЛЕ.
# По умолчанию Nginx ждёт очередную порцию ответа 60 секунд
# (директива proxy_read_timeout, значение по умолчанию — 60s).
# На 60-й секунде он перестал ждать, оборвал соединение с приложением
# и отдал браузеру 504. Приложение об этом не узнало: оно спокойно
# досчитало отчёт и записало результат в никуда — соединения уже нет.
# Отсюда и расхождение: у пользователя ошибка, в логах успех.

# ═══ ЛЕЧЕНИЕ 1 (БЫСТРОЕ): РАЗДВИНУТЬ ТАЙМАУТЫ ═══
location /api/reports/ {
    proxy_pass http://shop_app;

    proxy_connect_timeout 5s;
    # Сколько ждать УСТАНОВЛЕНИЯ соединения с приложением.
    # Это должно занимать миллисекунды — приложение рядом, на этой же машине.
    # Держите значение МАЛЕНЬКИМ: если за 5 секунд не подключились,
    # копия мертва, и ждать дольше бессмысленно — лучше быстро уйти
    # к следующей копии из upstream.

    proxy_send_timeout 60s;
    # Пауза между записями, когда мы ОТПРАВЛЯЕМ тело запроса приложению.

    proxy_read_timeout 300s;
    # Пауза между порциями, когда мы ЧИТАЕМ ответ. Вот этот и виноват.
    # Обратите внимание: это НЕ "общее время запроса". Это максимальная
    # тишина МЕЖДУ порциями данных. Если приложение раз в 10 секунд шлёт
    # хоть байт, таймаут не сработает никогда.

    # ВАЖНО: сузили правило до /api/reports/. Не надо ставить 300 секунд
    # на весь сайт: тогда любой зависший запрос будет держать соединение
    # пять минут, и при всплеске нагрузки они просто закончатся.
}

# ═══ ЛЕЧЕНИЕ 2: БУФЕРЫ (ОТКУДА БЕРЁТСЯ 502 НА БОЛЬШИХ ЗАГОЛОВКАХ) ═══
location /api/ {
    proxy_pass http://shop_app;

    proxy_buffer_size 16k;
    # Размер буфера под ПЕРВУЮ часть ответа — строку статуса и заголовки.
    # Если заголовки не влезли, Nginx отдаёт 502 с записью в error.log:
    #   upstream sent too big header while reading response header
    # Как в это вляпаться: положить в куку большой токен авторизации
    # или вернуть длинный заголовок с данными. Симптом обманчив —
    # выглядит как "приложение упало", хотя оно ответило нормально.

    proxy_buffers 8 16k;
    # Восемь буферов по 16 килобайт под ТЕЛО ответа.
    # Nginx старается вычитать ответ у приложения целиком и как можно
    # быстрее, чтобы освободить процесс Node, а дальше уже сам, не спеша,
    # отдаёт данные медленному клиенту. То, что не влезло в буферы,
    # уходит во временный файл на диске.

    proxy_busy_buffers_size 32k;
    # Сколько можно отдавать клиенту, пока чтение из приложения ещё идёт.
}

# ═══ ЛЕЧЕНИЕ 3 (ПРАВИЛЬНОЕ): ДОЛГОЙ РАБОТЕ НЕ МЕСТО В HTTP-ЗАПРОСЕ ═══
# Раздвинуть таймаут — значит замести проблему под ковёр. Пятиминутный
# запрос плох сам по себе, и вот почему:
#   - пользователь смотрит на белый экран пять минут и жмёт F5,
#     запуская ВТОРОЙ такой же отчёт;
#   - выкладка новой версии обрывает работу на середине;
#   - между браузером и вами могут стоять чужие таймауты (Cloudflare,
#     корпоративный прокси, мобильный оператор), и вы ими не управляете;
#   - соединение занято всё это время, а их количество конечно.
#
# Как надо: приложение принимает запрос, кладёт задачу в очередь
# и НЕМЕДЛЕННО отвечает "принято, вот номер":
#
#   POST /api/reports  ->  202 Accepted  { "reportId": "r_8412" }
#
# Дальше фоновый работник считает отчёт (иногда и правда минуты),
# складывает готовый файл в хранилище и помечает задачу выполненной.
# Клиент либо переспрашивает GET /api/reports/r_8412 раз в несколько
# секунд, либо получает уведомление. Ни одного долгого HTTP-запроса.
# Подробно про очереди и работников — на отдельной странице раздела.

# ЧЕСТНАЯ ЦЕНА.
# Таймауты — это НЕ ускорение. Вы лишь решаете, сколько ждать перед тем,
# как показать ошибку. Большие значения прячут медленный код и съедают
# соединения; маленькие превращают редкую задержку в частую ошибку.
# И помните: у ошибки 504 всегда есть настоящая причина ниже по течению —
# медленный запрос к базе, внешний сервис, блокировка. Правка конфига
# Nginx её не лечит, она лишь меняет момент, когда о ней узнают.`;

  protected readonly websocketUpgrade = `# ЗАДАЧА. В магазине есть живой чат с поддержкой и уведомление
# "ваш заказ собран" без обновления страницы. Под этим — WebSocket:
# соединение, которое открывается один раз и держится часами,
# а данные ходят в обе стороны когда угодно.

# ═══ БОЛЬНОЙ КОНФИГ. Обычное проксирование, всё как для API ═══
location /socket.io/ {
    proxy_pass http://shop_app;
    proxy_set_header Host $host;
}

# СИМПТОМ, ПО КОТОРОМУ ЕГО УЗНАЮТ БЕЗОШИБОЧНО:
#   - соединение открывается и ТУТ ЖЕ закрывается;
#   - в консоли браузера бесконечной лентой идёт
#       WebSocket connection to 'wss://shop.example.com/...' failed
#     и клиентская библиотека переподключается снова и снова
#     (Socket.IO при этом обычно молча откатывается на медленный
#      запасной режим с постоянными опросами — и всё "как бы работает",
#      только сервер ложится под нагрузкой);
#   - локально, без Nginx, всё идеально. Именно это и сбивает с толку.

# ═══ ПОЧЕМУ ТАК ═══
# WebSocket начинается как обычный HTTP-запрос со специальной просьбой:
#   GET /socket.io/ HTTP/1.1
#   Upgrade: websocket
#   Connection: Upgrade
# Сервер отвечает "101 Switching Protocols", и с этого момента внутри
# того же соединения говорят уже не по HTTP.
#
# Проблема в двух вещах.
# 1) Заголовок Connection — "хоп-бай-хоп": он относится к ОДНОМУ участку
#    пути, а не ко всему маршруту. Прокси обязан его не передавать дальше.
#    Nginx честно этого и не делает — и просьба о переключении не доезжает.
# 2) Nginx по умолчанию ходит в upstream по HTTP/1.0, а в нём механизма
#    переключения протокола попросту нет.
# Итог: приложение получает обычный запрос без просьбы, отвечает обычным
# ответом, соединение закрывается. Приложение виноватым себя не считает
# и ничего в логи не пишет.

# ═══ ЗДОРОВЫЙ КОНФИГ ═══
location /socket.io/ {
    proxy_pass http://shop_app;

    proxy_http_version 1.1;
    # Без этой строки всё остальное бессмысленно: переключение протокола
    # существует только в HTTP/1.1.

    proxy_set_header Upgrade $http_upgrade;
    # Пробрасываем просьбу браузера как есть.

    proxy_set_header Connection "upgrade";
    # А вот этот заголовок мы обязаны СОЗДАТЬ заново — исходный до нас
    # не дошёл по правилам протокола.

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout 3600s;
    # ВТОРАЯ ПОЛОВИНА ЛЕЧЕНИЯ, ПРО КОТОРУЮ ЗАБЫВАЮТ.
    # Соединение живо, но в чате может не быть сообщений полчаса.
    # Для Nginx тишина в 60 секунд (значение по умолчанию) — повод
    # закрыть соединение. Симптом получается ещё коварнее первого:
    # чат работает, вы его тестируете, всё отлично, а у пользователей
    # он "отваливается, если отойти попить кофе".

    proxy_send_timeout 3600s;
}

# ═══ АККУРАТНЫЙ ВАРИАНТ: РАЗРЕШАТЬ ПЕРЕКЛЮЧЕНИЕ ТОЛЬКО КОГДА ПРОСЯТ ═══
# Если один и тот же location обслуживает и обычные запросы, и WebSocket,
# жёсткое Connection "upgrade" мешает переиспользованию соединений.
# Стандартное решение — карта соответствий (пишется на уровне http):

map $http_upgrade $connection_upgrade {
    default upgrade;   # браузер попросил переключение -> Connection: upgrade
    ''      close;     # обычный запрос                -> Connection: close
}

location /socket.io/ {
    proxy_pass http://shop_app;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_read_timeout 3600s;
}

# ЧЕСТНАЯ ЦЕНА И ЧЕГО ЭТОТ ПРИЁМ НЕ ДЕЛАЕТ.
# 1) Длинный proxy_read_timeout — это обещание держать соединение час.
#    Тысяча открытых чатов — тысяча висящих соединений и на прокси,
#    и в приложении. Память и лимиты открытых файлов кончаются молча.
#    Лучше не задирать таймаут до суток, а слать пустые сообщения-пинги
#    с клиента раз в несколько десятков секунд: тогда тишины не бывает,
#    и заодно вы быстро узнаёте о мёртвых соединениях.
# 2) Проксирование НЕ решает вопрос нескольких копий приложения.
#    Пользователь A подключён к копии на 3000, пользователь B — к 3001.
#    Сообщение из A в B само не долетит: копии не знают друг о друге.
#    Нужен общий посредник — например, режим публикации/подписки в Redis.
#    Никакая настройка Nginx этого не заменит.
# 3) Липкость здесь всё же нужна ЧАСТИЧНО: если клиентская библиотека
#    начинает с обычных запросов и лишь потом переключается (так делает
#    Socket.IO по умолчанию), все шаги рукопожатия должны попасть в одну
#    копию. Либо ip_hash, либо принудительно только режим websocket.`;

  protected readonly bodySizeAndRate = `# ═══ ЧАСТЬ 1. ЗАГАДОЧНАЯ ОШИБКА 413 ═══
#
# ИСТОРИЯ. В админке магазина есть загрузка фотографий товара.
# Локально всё работает. На сервере маленькие картинки грузятся,
# а фотография с телефона (несколько мегабайт) даёт ошибку:
#   413 Request Entity Too Large
# В логах приложения — НИ ОДНОЙ СТРОЧКИ. Как будто запроса не было.
#
# Так и есть: запроса действительно не было. Nginx по умолчанию
# отказывается принимать тело запроса больше 1 мегабайта
# (директива client_max_body_size, значение по умолчанию — 1m)
# и отвергает его САМ, даже не начав пересылать приложению.
# Поэтому искать причину в коде можно очень долго и безрезультатно.

server {
    client_max_body_size 25m;
    # Ставится на уровне server (или конкретного location).
    # 0 означает "без ограничения" — так делать не надо: это открытая
    # дверь для того, чтобы забить вам диск и память одним запросом.

    location /api/upload/ {
        client_max_body_size 100m;
        # Разрешаем много ТОЛЬКО там, где это осмысленно.
        # Остальному API большие тела не нужны — и нечего им их разрешать.
        proxy_pass http://shop_app;

        proxy_request_buffering off;
        # Тонкость. По умолчанию Nginx СНАЧАЛА принимает файл целиком
        # (в память и во временный файл на диске) и только потом начинает
        # отдавать его приложению. Для загрузки больших файлов это лишний
        # круг по диску. Выключив буферизацию, вы пускаете поток напрямую —
        # но теряете защиту от медленного клиента: теперь ваше приложение
        # будет по капле принимать данные от телефона в метро.
        # Выбирайте осознанно, а не копированием из интернета.
    }
}

# ВАЖНО ПОМНИТЬ: лимит нужно поднять в ДВУХ местах. Если Nginx разрешает
# 100 мегабайт, а библиотека разбора форм в приложении (multer, busboy)
# настроена на 5, ошибка просто переедет на этаж ниже — и будет уже 400
# или 500 вместо 413. Симптомы разные, причина одна.

# ═══ ЧАСТЬ 2. ОГРАНИЧЕНИЕ ЧАСТОТЫ ЗАПРОСОВ ═══
#
# ВОПРОС, КОТОРЫЙ ЗАДАЮТ ВСЕГДА: "У меня уже стоит express-rate-limit
# в приложении. Зачем то же самое ещё и в Nginx?"
#
# ОТВЕТ. Лимит внутри приложения срабатывает ПОСЛЕ того, как приложение
# уже потратилось на запрос: приняло соединение, разобрало заголовки,
# распарсило JSON, сходило в Redis за счётчиком, собрало ответ.
# При потоке в тысячи запросов в секунду ваша защита сама становится
# нагрузкой и падает вместе с приложением.
# Лимит в Nginx отсекает лишнее ДО приложения, стоит копейки и работает,
# даже когда приложение уже лежит.
# Это два разных рубежа, а не дубликаты: грубый и дешёвый снаружи,
# точный и умный внутри (про роли, тарифы, ключи API).

# --- на уровне http (общий файл nginx.conf) ---
limit_req_zone $binary_remote_addr zone=api_zone:10m rate=10r/s;
# Разбираем по частям:
#   $binary_remote_addr — ПО ЧЕМУ считаем. Адрес клиента в компактном
#     двоичном виде (занимает меньше памяти, чем текстовый).
#   zone=api_zone:10m   — имя хранилища счётчиков и его размер.
#     10 мегабайт хватает на очень большое число адресов одновременно.
#     Кончится место — Nginx начнёт вытеснять старые записи.
#   rate=10r/s          — десять запросов в секунду с одного адреса.
#     Внутри это означает "не чаще одного запроса раз в одну десятую
#     секунды", а не "любые десять в течение секунды".

limit_conn_zone $binary_remote_addr zone=conn_zone:10m;
# Отдельное хранилище — для лимита на число ОДНОВРЕМЕННЫХ соединений.

# --- на уровне location ---
location /api/ {
    limit_req zone=api_zone burst=20 nodelay;
    # burst=20 — очередь на 20 запросов. Всплеск сверх нормы не отвергается
    #   сразу, а становится в очередь. Это важно: браузер честного
    #   пользователя может открыть десяток запросов разом при загрузке
    #   страницы, и без запаса вы будете бить по своим.
    # nodelay  — запросы из очереди выполняются НЕМЕДЛЕННО, а не
    #   размазываются по времени. Без nodelay всплеск не отвергается,
    #   но каждому придётся подождать, и страница будет ощутимо тормозить.

    limit_conn conn_zone 20;
    # Не больше двадцати одновременных соединений с одного адреса.
    # Защищает от другого класса злоупотреблений: не частых запросов,
    # а множества открытых и намеренно медленных.

    limit_req_status 429;
    # По умолчанию Nginx отдаёт 503. Правильный ответ для превышения
    # лимита — 429 Too Many Requests: клиентские библиотеки понимают его
    # как "подожди и повтори", а 503 — как "сервер сломан".

    proxy_pass http://shop_app;
}

location /api/auth/login {
    limit_req zone=login_zone burst=3 nodelay;
    # Для входа в аккаунт лимит должен быть НАМНОГО строже: здесь
    # перебирают пароли. Отдельная зона со своим rate, например 1r/s.
    proxy_pass http://shop_app;
}

# ЧЕСТНАЯ ЦЕНА И ЧЕГО ЭТОТ ПРИЁМ НЕ ДЕЛАЕТ.
# 1) Счёт по адресу бьёт по своим. За одним адресом сидит целый офис,
#    школа или мобильный оператор через общий шлюз. Заблокировав адрес,
#    вы заблокировали всех разом. Слишком строгий лимит на /api/ —
#    это отказ обслуживания, устроенный вами самими.
# 2) Это НЕ защита от распределённой атаки. Тысяча адресов по десять
#    запросов в секунду каждый — это десять тысяч запросов в секунду,
#    и каждый из них полностью в рамках лимита. Против такого нужен
#    внешний рубеж (например, Cloudflare), а не строчка в конфиге.
# 3) Если вы стоите за другим прокси, $binary_remote_addr — это адрес
#    ТОГО прокси, а не пользователя. Считать надо по настоящему адресу
#    из X-Forwarded-For, иначе весь трафик сложится в один счётчик
#    и лимит выключится для всех сразу (см. раздел про заголовки).
# 4) Отвергнутый запрос всё равно надо УВИДЕТЬ. Заведите привычку
#    смотреть, сколько раз за день сработал лимит: резкий рост — это
#    либо атака, либо ваш собственный фронтенд, зациклившийся на опросе.`;

  protected readonly caddyfile = `# ТОТ ЖЕ САМЫЙ САЙТ, ЧТО МЫ СОБИРАЛИ ВЫШЕ. Файл называется Caddyfile,
# лежит обычно в /etc/caddy/Caddyfile.

shop.example.com {
    # Одна эта строка — уже настоящий https с валидным сертификатом.
    # Caddy сам сходит в Let's Encrypt, докажет, что домен ваш,
    # получит сертификат, положит его на диск и будет продлевать
    # заранее, без перезапуска и без вашего участия.
    # Обязательное условие: домен shop.example.com должен УЖЕ указывать
    # на этот сервер, а порты 80 и 443 — быть доступны из интернета.

    encode zstd gzip
    # Сжатие. Порядок слов — это порядок предпочтения: если браузер
    # понимает zstd, используем его, иначе gzip.

    handle /api/* {
        reverse_proxy 127.0.0.1:3000 127.0.0.1:3001
        # Обратное проксирование. Два адреса через пробел — это уже
        # балансировка (по умолчанию — случайный выбор).
        # И вот тут главное отличие от Nginx: заголовки Host, X-Real-IP,
        # X-Forwarded-For и X-Forwarded-Proto Caddy проставляет САМ,
        # писать их руками не надо. WebSocket тоже работает без
        # дополнительных строк — переключение протокола поддержано из
        # коробки. Половина граблей с предыдущих шагов здесь просто
        # не существует как класс.
    }

    handle {
        root * /srv/shop/current
        try_files {path} /index.html
        file_server
        # То же самое, что try_files в Nginx: сначала ищем файл,
        # если не нашли — отдаём index.html, чтобы одностраничное
        # приложение само разобрало адрес.
    }

    header /assets/* Cache-Control "public, max-age=31536000, immutable"
    # Год кэша для файлов сборки с хэшем в имени — ровно та же логика,
    # что и в Nginx.

    log {
        output file /var/log/caddy/shop.log
        # Логи в формате JSON — их удобно разбирать программой,
        # но неудобно читать глазами без вспомогательных утилит.
    }
}

# ═══ ПРОВЕРКА И ПРИМЕНЕНИЕ ═══
caddy fmt --overwrite /etc/caddy/Caddyfile   # выровнять отступы
caddy validate --config /etc/caddy/Caddyfile # проверить синтаксис
sudo systemctl reload caddy                  # применить без обрыва

# ═══ ЧТО ИМЕННО CADDY ДЕЛАЕТ САМ ═══
#   - получает и продлевает сертификаты (Let's Encrypt или ZeroSSL);
#   - слушает 80 и 443 и переадресует http на https;
#   - подставляет X-Forwarded-* заголовки при проксировании;
#   - поддерживает WebSocket без отдельной настройки;
#   - включает современные версии HTTP;
#   - при проверке конфига отказывается применять неверный.
# Практический итог: конфиг на 20 строк вместо 120, и в нём нет
# ни одной из ловушек, разобранных выше на этой странице.

# ═══ ЧЕСТНАЯ ЦЕНА. В ЧЁМ ОГРАНИЧЕНИЯ ═══
# 1) АВТОМАТИКА ТРЕБУЕТ ПРАВИЛЬНОГО МИРА ВОКРУГ. Домен не указывает на
#    сервер, порт 80 закрыт файрволом, вы за чужим прокси — и сертификат
#    не выдадут. Caddy будет упорно пытаться и, если вы это делали много
#    раз подряд, упрётся в ограничения выдачи со стороны Let's Encrypt.
#    Для отладки существует тестовый сервер выдачи (staging) —
#    экспериментируйте на нём, а не на боевом.
# 2) ЭКОСИСТЕМА МЕНЬШЕ. Nginx стоит почти везде: на любой вопрос уже есть
#    десять ответов, готовые конфиги, знакомый всем коллегам синтаксис.
#    По Caddy ответов и примеров кратно меньше.
# 3) ТОНКАЯ НАСТРОЙКА — ЧЕРЕЗ МОДУЛИ. Многие возможности живут в
#    подключаемых модулях, и чтобы их получить, Caddy надо ПЕРЕСОБРАТЬ
#    (инструментом xcaddy). Это не "поставил пакет и готово".
# 4) НАСТРОЙКИ ПО УМОЛЧАНИЮ — БЛАГО И ЛОВУШКА ОДНОВРЕМЕННО. Пока вас
#    устраивает поведение из коробки, всё прекрасно. Как только нужно
#    что-то нестандартное, приходится узнавать, что именно Caddy делал
#    за вас — а это знание всё равно про Nginx-подобные механизмы.
#
# ВЫВОД БЕЗ РЕКЛАМЫ: для типового сайта и небольшой команды Caddy почти
# всегда выгоднее — он экономит дни возни с сертификатами. Nginx выбирают,
# когда нужны тонкие настройки, когда так принято в компании или когда
# конфиг достался по наследству и работает.`;

  protected readonly traefikLabels = `# TRAEFIK. ТРЕТИЙ ПОДХОД, УСТРОЕННЫЙ ПРИНЦИПИАЛЬНО ИНАЧЕ.
#
# У Nginx и Caddy есть ФАЙЛ КОНФИГА, где перечислены сайты. Добавили
# сервис — правьте файл, проверяйте, перезагружайте.
# Traefik же СМОТРИТ НА ЗАПУЩЕННЫЕ КОНТЕЙНЕРЫ и берёт настройки маршрута
# из МЕТОК (labels) самого контейнера. Поднялся новый контейнер с метками —
# маршрут появился сам, за секунды. Остановился — маршрут исчез.
# Никто ничего не перезагружал.

# файл docker-compose.yml
services:
  # ─────────────────────────────────────────────────────────────
  # САМ TRAEFIK. Единственный контейнер, который смотрит в интернет.
  # ─────────────────────────────────────────────────────────────
  proxy:
    image: traefik:v3
    # Тег версии закрепляйте явно (не latest), иначе однажды прокси
    # обновится сам и перестанет понимать ваши метки.
    command:
      - --providers.docker=true
      # "Источник настроек — Docker". Traefik подключается к Docker
      # и подписывается на события "контейнер поднялся / упал".
      - --providers.docker.exposedbydefault=false
      # ОЧЕНЬ ВАЖНАЯ СТРОКА, И ЕЁ ПОСТОЯННО ЗАБЫВАЮТ.
      # Без неё Traefik публикует В ИНТЕРНЕТ вообще ВСЕ контейнеры,
      # включая базу данных и Redis. С ней — только те, у которых
      # явно написано traefik.enable=true.
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      # "Точки входа" — это порты, которые слушает Traefik.
      # Имена web и websecure придуманы нами, дальше ссылаемся на них.
      - --certificatesresolvers.le.acme.email=ops@example.com
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.le.acme.tlschallenge=true
      # Автоматические сертификаты Let's Encrypt, как у Caddy.
      # Имя le — тоже наше, на него ссылаются метки ниже.
    ports:
      - '80:80'
      - '443:443'
      # НАРУЖУ ТОРЧАТ ТОЛЬКО ЭТИ ДВА ПОРТА. У остальных сервисов
      # секции ports нет вовсе — и это правильно: снаружи к ним
      # не подключиться никак, только через прокси.
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Так Traefik видит список контейнеров.
      # :ro — только чтение. Обязательно.
      # ЧЕСТНОЕ ПРЕДУПРЕЖДЕНИЕ: доступ к этому сокету равносилен
      # правам root на хосте. Контейнер, который его получил, может
      # запустить другой контейнер с любыми правами. Именно поэтому
      # его дают ОДНОМУ доверенному контейнеру и только на чтение.
      - ./letsencrypt:/letsencrypt

  # ─────────────────────────────────────────────────────────────
  # ПРИЛОЖЕНИЕ МАГАЗИНА. Про Traefik оно не знает ничего.
  # ─────────────────────────────────────────────────────────────
  shop-api:
    image: registry.example.com/shop-api:2026.03.14
    environment:
      NODE_ENV: production
    labels:
      - traefik.enable=true
      # Разрешение публиковать этот контейнер. Без него — невидим.

      - traefik.http.routers.shop-api.rule=Host(\`shop.example.com\`) && PathPrefix(\`/api\`)
      # РАЗБИРАЕМ ПО СЛОВАМ, потому что вид у метки пугающий:
      #   traefik.http    — говорим про обычный http-трафик;
      #   .routers        — раздел "маршруты", то есть "какой запрос куда";
      #   .shop-api       — ИМЯ маршрута. Мы его придумали. Все метки
      #                     с одинаковым именем описывают один маршрут;
      #   .rule=...       — условие, при котором маршрут срабатывает.
      # Здесь: имя сайта shop.example.com И путь начинается с /api.
      # Полный аналог связки server_name + location в Nginx.

      - traefik.http.routers.shop-api.entrypoints=websecure
      # Слушать на точке входа websecure, то есть на 443.

      - traefik.http.routers.shop-api.tls.certresolver=le
      # Сертификат брать у резолвера с именем le, описанного выше.
      # Дальше всё автоматически, как в Caddy.

      - traefik.http.services.shop-api.loadbalancer.server.port=3000
      # На КАКОЙ ПОРТ ВНУТРИ КОНТЕЙНЕРА слать запросы.
      # Адрес контейнера Traefik узнаёт у Docker сам — его не пишут
      # вообще нигде. Именно поэтому масштабирование работает даром:
      #   docker compose up --scale shop-api=4
      # поднимет четыре контейнера, Traefik увидит все четыре и начнёт
      # раскидывать запросы между ними. Ни строчки конфига не изменилось.

      - traefik.http.middlewares.api-limit.ratelimit.average=10
      - traefik.http.middlewares.api-limit.ratelimit.burst=20
      - traefik.http.routers.shop-api.middlewares=api-limit
      # Промежуточные обработчики (middlewares) — аналог limit_req и
      # прочих директив Nginx: ограничение частоты, сжатие, базовая
      # авторизация, переписывание путей. Описываем метками и
      # подключаем к маршруту по имени.

  # ─────────────────────────────────────────────────────────────
  # БАЗА ДАННЫХ. Ни одной метки traefik — и снаружи её не существует.
  # ─────────────────────────────────────────────────────────────
  postgres:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD_FILE: /run/secrets/db_password
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:

# ═══ КОГДА TRAEFIK УДОБНЕЕ СТАТИЧЕСКОГО КОНФИГА ═══
# 1) Сервисов много и они меняются. Десяток контейнеров, у каждого своя
#    команда и свой график выкладок — править общий файл прокси при
#    каждом изменении быстро становится узким местом.
# 2) Выкладка без простоя. Поднялся новый контейнер — Traefik добавил
#    его в балансировку; старый останавливается — убрал. Само.
# 3) Настройки маршрута живут РЯДОМ С СЕРВИСОМ, в его compose-файле,
#    а не в чужом файле на сервере, куда у команды может не быть доступа.
# 4) В Kubernetes тот же принцип возведён в правило: маршруты описывают
#    объекты Ingress/IngressRoute, а прокси лишь читает их и подстраивается.

# ═══ ЧЕСТНАЯ ЦЕНА ═══
# 1) ОТЛАЖИВАТЬ ТЯЖЕЛЕЕ. В Nginx можно открыть файл и прочитать глазами
#    все маршруты. Здесь маршрут собран из десятка меток, разбросанных
#    по разным compose-файлам. Опечатка в имени маршрута не даёт ошибки —
#    она молча создаёт ВТОРОЙ, пустой маршрут, и сервис просто недоступен.
#    Первым делом смотрите панель Traefik (dashboard) — она показывает,
#    какие маршруты и сервисы он реально увидел.
# 2) СИНТАКСИС МЕТОК ДЛИННЫЙ И НЕПРОЩАЮЩИЙ. Имена routers, services,
#    middlewares надо держать в голове согласованными вручную.
# 3) ДОСТУП К СОКЕТУ DOCKER — реальный риск, а не формальность.
#    В серьёзных установках между Traefik и сокетом ставят посредника,
#    пропускающего только безопасные запросы на чтение.
# 4) БЕЗ КОНТЕЙНЕРОВ ВЫИГРЫША НЕТ. Если у вас одно приложение под
#    systemd на одном сервере, Traefik добавит сложности и не даст
#    ничего взамен — там уместнее Nginx или Caddy.`;
}
